# macOS 桌面小组件专项第四轮审查报告

> 审查日期：2026-07-30
>
> 审查基线：`dev@60246d0`
>
> 审查重点：开发者账号恢复后的双渠道签名、Widget 调试入口、运行时诊断、
> 构建版本一致性、专项文档和 checklist。

---

## 1. 结论

本轮重新执行了 Store / Direct 全新 DerivedData 签名构建，并复核三个 Widget 调试
Scheme 与 Focus Gallery 占位渲染。第三轮遗留的签名外部阻断已经解除；当前未发现新的
P0 / P1 代码问题，发现 **1 个 P2 文档与进度一致性问题**，修复前不生成最终结果报告。

- P0：0
- P1：0
- P2：1
- 人工验收待办：1 组

---

## 2. 增量修复复核

| 项目 | 状态 | 证据 |
|------|------|------|
| Widget target 继承本地 Team 配置并自动注册 App Group | 已修复 | `3917714b` |
| Host / Extension 构建版本一致 | 已修复 | `61034a2b`，两条渠道均为 `2291` |
| 三个 Widget 可独立选择调试对象 | 已修复 | `f5a102e6`，三个 Scheme 均被 `xcodebuild -list` 识别并构建成功 |
| Focus Gallery 占位列表无重复视图身份 | 已修复 | `60246d0d`，使用 Timeline 内唯一行位置作为 SwiftUI 身份 |

---

## 3. 签名与产物证据

Store 与 Direct 均使用全新 DerivedData，执行未覆盖 `DEVELOPMENT_TEAM`、
`REGISTER_APP_GROUPS` 或 entitlement 的正式 Debug 构建：

| 项目 | Store | Direct |
|------|-------|--------|
| 构建结果 | 通过 | 通过 |
| Host bundle ID | `com.starcat.app.store` | `com.starcat.app.direct` |
| Extension bundle ID | `com.starcat.app.store.widgets` | `com.starcat.app.direct.widgets` |
| Team | `8WCUMGCWMB` | `8WCUMGCWMB` |
| 签名身份 | `Apple Development: liwen gong (MZ4R5J393K)` | 同左 |
| Host / Extension App Group | `group.com.starcat.app.store.widgets` | `group.com.starcat.app.direct.widgets` |
| Host / Extension `CFBundleVersion` | `2291` / `2291` | `2291` / `2291` |
| `codesign --verify --deep --strict` | 通过 | 通过 |

结论：第三轮报告中的账号、provisioning profile 和 App Group 签名阻断已经解除，两个
渠道的 Host / Extension 签名链、容器和构建版本均一致。

---

## 4. 发现项

### P2-01 详细方案与 checklist 仍停留在签名阻断状态

**现状**

- checklist 仍将四个 Host / Extension 签名与 App Group 检查保持未勾选。
- checklist 仍记录 Xcode Team 凭据失效和 provisioning profile 缺失。
- 详细方案仍只给出“尚未取得 profile”时的 ad-hoc 测试命令，没有三个独立 Widget
  Scheme 的调试入口与当前正式签名验证方式。
- 提交索引尚未登记 `3917714b` 至 `60246d0d` 四个增量提交。

**影响**

代码、签名产物和专项文档已经不一致，后续开发者会继续按已失效的阻断状态排查，也无法
直接选择具体 Widget 进行调试。

**修复要求**

1. 回填 Store / Direct 签名与 App Group 检查的真实证据。
2. 删除已失效的账号 / profile 阻断说明，但保留 Gallery、桌面交互、深浅色和
   VoiceOver 人工验收为未完成。
3. 在详细方案中补充三个调试 Scheme、命令行构建验证和人工运行步骤。
4. 回填四个增量提交及第四轮审查记录。

---

## 5. 测试与人工验收边界

- 本轮三个 Widget Scheme、Store Host 和 Direct Host 均完成命令行构建。
- 本轮未重新执行 `xcodebuild test`：Xcode IDE 当前仍在运行，项目规范禁止命令行测试
  与 IDE 抢占 `testmanagerd`；上一轮 29 项 Widget 定向测试和 2061 项全量测试记录保持
  为历史回归证据，不冒充当前提交重新通过。
- Widget Gallery、所有尺寸、真实数据、Deep Link、登出 / 切换账号、双渠道同时安装、
  深浅色和 VoiceOver 仍需要人工观察并保留未勾选。

---

## 6. 下一步

1. 修复 P2-01 并独立提交。
2. 在 Xcode 关闭后重跑 Widget 定向测试；如环境允许，再跑全量测试。
3. 连续执行两轮无新增 P0 / P1 / P2 的复审。
4. 人工验收未完成前，结果报告必须明确区分“自动化完成”和“人工待验收”。
