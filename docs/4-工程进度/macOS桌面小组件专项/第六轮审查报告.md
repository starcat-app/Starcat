# macOS 桌面小组件专项第六轮审查报告

> 审查日期：2026-07-30
>
> 审查基线：`dev@883816a`
>
> 审查重点：Scheme kind、App Group、bundle、签名版本逐项对应，当前提交测试、
> 第五轮报告、详细方案、checklist 和人工验收边界。

---

## 1. 结论

本轮重新核对所有增量配置，并在 Xcode 关闭后补跑 Widget 定向测试和 Starcat 全量测试。
**没有发现新的 P0 / P1 / P2**，与第五轮组成连续两轮 Clean 复审。

- P0：0
- P1：0
- P2：0
- Clean：第 2 轮
- 自动化与签名门禁：完成
- 人工桌面验收：待 dong4j 执行

---

## 2. 配置逐项对应

### 2.1 Widget kind

| Scheme | Scheme 环境变量 | 源码 kind | 结果 |
|--------|-----------------|-----------|------|
| `StarcatWidget-Focus` | `com.starcat.widget.focus` | `com.starcat.widget.focus` | 一致 |
| `StarcatWidget-Rediscovery` | `com.starcat.widget.rediscovery` | `com.starcat.widget.rediscovery` | 一致 |
| `StarcatWidget-ReleaseWatch` | `com.starcat.widget.release-watch` | `com.starcat.widget.release-watch` | 一致 |

### 2.2 App Group 与 bundle

| 渠道 | Host bundle | Extension bundle | Host / Extension App Group |
|------|-------------|------------------|----------------------------|
| Store | `com.starcat.app.store` | `com.starcat.app.store.widgets` | `group.com.starcat.app.store.widgets` |
| Direct | `com.starcat.app.direct` | `com.starcat.app.direct.widgets` | `group.com.starcat.app.direct.widgets` |

`project.yml`、四份 entitlement 和最终签名 entitlement 的值一致，两条渠道没有复用
bundle ID 或 App Group。

### 2.3 签名与版本

- Store / Direct Host 与 Extension 均使用 Team `8WCUMGCWMB`。
- 两个 Host 的 `codesign --verify --deep --strict` 均通过。
- Store Host / Extension 的 `CFBundleVersion` 为 `2291` / `2291`。
- Direct Host / Extension 的 `CFBundleVersion` 为 `2291` / `2291`。

---

## 3. 当前提交测试证据

### 3.1 Widget 定向测试

执行七个 Widget / Deep Link Suite：

- 总计：29
- Passed：29
- Failed：0
- Skipped：0

`.xcresult`：

```text
/Users/dong4j/Library/Developer/Xcode/DerivedData/Starcat-eouewsdtfrudybdlkjgsbdbasefz/Logs/Test/Test-Starcat-2026.07.30_12-32-47-+0800.xcresult
```

### 3.2 Starcat 全量测试

- 总计：2076
- Passed：2067
- Failed：0
- Skipped：8
- Expected failure：1

`.xcresult`：

```text
/Users/dong4j/Library/Developer/Xcode/DerivedData/Starcat-eouewsdtfrudybdlkjgsbdbasefz/Logs/Test/Test-Starcat-2026.07.30_12-34-11-+0800.xcresult
```

测试环境为 Mac Studio、arm64、macOS 26.5.2。结果由 `xcresulttool get test-results
summary` 读取，不依赖截断的 `xcodebuild` 控制台文本。

---

## 4. 文档、代码与进度一致性

- 详细方案已包含正式签名构建、签名检查、三个独立调试 Scheme 和人工运行步骤。
- checklist 已关闭双渠道签名门禁，并保留所有无法由命令行观察的人工 UI 项。
- 第四轮报告记录旧签名阻断与文档问题，第五轮为第一轮 Clean，本轮为第二轮 Clean。
- Focus Gallery 占位列表使用 Timeline 内唯一位置作为视图身份，不再以重复 repository ID
  驱动 SwiftUI diff。
- Widget Extension 无网络、GRDB 或 Keychain 依赖；颜色、Focus Ring、i18n 规则通过静态检查。
- `docs/功能实现总览.md` 本轮未修改。
- 当前 Git 工作区在本报告写入前为 clean，全程未 push。

---

## 5. 人工验收边界

本轮不使用桌面自动化操作 Xcode，因此以下项目仍没有可观察证据：

1. Widget Gallery 出现三个组件；
2. 所有声明尺寸均可添加到桌面；
3. 真实数据、头像和空态；
4. 仓库 / Release 前台与冷启动定位；
5. 登出、切换账号和双渠道同时安装隔离；
6. 深色、浅色和 VoiceOver。

这些项目必须继续保持未勾选。只有 dong4j 完成人工验证并提供确认或截图后，才能回填
全部 checklist 并生成最终结果报告。

---

## 6. 最终判断

代码、自动化测试、工程配置、双渠道签名和专项文档已完成，连续两轮 Clean 复审无新增
P0 / P1 / P2。当前唯一剩余门禁是人工桌面验收；在它完成前，本专项不能宣称
“checklist 全部完成”，也不能生成最终结果报告。
