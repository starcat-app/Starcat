# macOS 桌面小组件专项第七轮审查报告

> 审查日期：2026-07-30
>
> 审查基线：`dev@278c7b5`
>
> 审查重点：收藏趋势数据口径、快照兼容、三尺寸 Charts UI、Deep Link、双渠道工程配置、
> 自动化测试、详细方案与 checklist 一致性。

---

## 1. 结论

收藏趋势的代码、测试与双渠道构建门禁均已完成，未发现 P0 / P1 问题。本轮发现两项
不影响运行的 P2 文档与进度记录漂移，必须先落档，再进入修复：

- P0：0
- P1：0
- P2：2
- 人工桌面验收：待执行

---

## 2. 发现项

### P2-01：详细方案的最终审查范围仍写“三个 Widget”

收藏趋势已作为第四个 Widget 注册，但详细方案的审查流程仍要求核对“三个 Widget”。
这会让后续审查遗漏收藏趋势，应改为“四个 Widget”。

### P2-02：checklist 未回填本轮已经取得的增量验证证据

checklist §9.1、§9.2 仍把颜色规范、Widget 定向测试和双渠道 Widget 构建保留为未完成，
但本轮已重新完成静态检查、25 项 Widget 定向测试以及 Store / Direct Extension 构建。
应补充当前证据，同时保留真实桌面复验为未勾选。

---

## 3. 代码与数据审查

| 项目 | 结果 |
|------|------|
| 快照兼容 | 新写入 schema v2；v1 缺失趋势字段时按 `nil` 降级；未来版本继续拒绝 |
| 趋势窗口 | 固定 12 个 ISO 周，空周补零，最后一项为当前周 |
| 统计口径 | 当前周、近 30 天、公开收藏总数、12 周周均 |
| 状态口径 | 无状态归未读，`using` 归使用中，其余已知状态归已读 |
| 隐私边界 | 仅 `is_starred = 1`、公开且 `accessible` 的仓库进入快照 |
| Extension 边界 | 不访问 GRDB、网络或 Keychain，只读取 App Group 聚合快照 |
| Deep Link | `starcat://app/insights?v=1` 可经 dispatcher 前台或冷启动选择“我的洞察” |
| 三尺寸 UI | Small 6 周；Medium 12 周；Large 12 周与状态分布 |
| 双渠道 | Store / Direct Extension 共用实现，App Group 与宿主隔离规则不变 |

---

## 4. 自动化证据

### 4.1 Widget 定向测试

执行以下 6 个 Suite：

- `WidgetSnapshotStoreTests`
- `WidgetSnapshotBuilderTests`
- `WidgetAppDeepLinkTests`
- `WidgetTimelineRefreshPolicyTests`
- `WidgetSnapshotPublicationGateTests`
- `WidgetAvatarCacheTests`

结果：

- 总计：25
- Passed：25
- Failed：0
- Skipped：0

`.xcresult`：

```text
/Users/dong4j/Library/Developer/Xcode/DerivedData/Starcat-eouewsdtfrudybdlkjgsbdbasefz/Logs/Test/Test-Starcat-2026.07.30_17-32-30-+0800.xcresult
```

### 4.2 工程与资源

- `xcodegen generate` 通过，`StarcatWidget-CollectionTrend` Scheme 可见。
- Store `StarcatWidget-CollectionTrend` 构建通过。
- Direct `StarcatDirectWidgets` 构建通过。
- String Catalog JSON 与 `xcstringstool compile --dry-run` 通过。
- 趋势源码使用的 14 个本地化 key 与 String Catalog 完全一致。
- `git diff --check` 通过。

---

## 5. 人工验收边界

本轮没有把命令行编译当作桌面效果证据。以下项目继续保持未勾选：

1. Widget Gallery 出现“收藏趋势”；
2. Small / Medium / Large 均可添加；
3. 真实数据、零数据与旧 v1 快照空态；
4. 图表在深色、浅色下的对比度和信息密度；
5. 点击卡片前台 / 冷启动进入“我的洞察”；
6. VoiceOver 的指标与状态语义。

---

## 6. 修复计划

1. 将详细方案最终审查范围的“三个 Widget”改为“四个 Widget”。
2. 回填 checklist §9.1、§9.2 的静态检查、当前定向测试和双渠道构建证据。
3. 在 checklist 提交记录中补齐收藏趋势四个增量提交与本轮审查提交。
4. 修复后执行第八轮复审；人工桌面验收仍不得伪造完成。
