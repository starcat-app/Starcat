# macOS 桌面小组件专项第五轮审查报告

> 审查日期：2026-07-30
>
> 审查基线：`dev@1ae18d2`
>
> 审查重点：第四轮 P2 修复、源码边界、UI 强制规范、签名产物、详细方案、
> checklist 与 Git 状态。

---

## 1. 结论

第四轮发现的 P2 文档与进度不一致已经关闭。本轮从当前提交重新核对代码、工程配置、
签名产物和专项文档，**没有发现新的 P0 / P1 / P2**，记为第一轮 Clean 复审。

- P0：0
- P1：0
- P2：0
- Clean：第 1 轮
- 人工验收待办：1 组

---

## 2. 第四轮问题关闭验证

| 第四轮发现项 | 状态 | 证据 |
|--------------|------|------|
| P2-01 详细方案与 checklist 停留在旧签名阻断状态 | 已修复 | `1ae18d2`；签名证据、三个 Scheme、人工运行步骤和提交索引已同步 |

---

## 3. 复审证据

| 项目 | 结果 |
|------|------|
| Git 工作区 | clean，当前分支 `dev`，未 push |
| `git diff --check` | 通过 |
| Widget Extension 边界 | 无 `URLSession`、`URLRequest`、GRDB 或 Keychain 引用 |
| UI 颜色规范 | Widget 源码无 `.tertiary` |
| Focus Ring 规范 | 两处 `.buttonStyle(.plain)` 后均有 `.focusEffectDisabled()` |
| 四份 entitlement | `plutil -lint` 全部通过 |
| 本地化目录 | JSON 合法；Widget 源码无 `String(localized:)` / `NSLocalizedString` |
| 三个调试 Scheme | `xcodebuild -list` 可见，独立构建均通过 |
| Store / Direct 签名链 | `codesign --verify --deep --strict` 均通过 |
| Host / Extension App Group | 两条渠道分别一致且互不复用 |
| Host / Extension 构建版本 | 两条渠道均为 `2291` / `2291` |
| `docs/功能实现总览.md` | 本轮未修改 |

---

## 4. 测试边界复核

Xcode IDE 仍在运行（PID `49348`）。按项目测试规范，本轮没有关闭或操作用户的 Xcode，
也没有启动会与 IDE 抢占 `testmanagerd` 的 `xcodebuild test`。以下证据分层记录：

- 当前增量提交：三个 Widget Scheme、Store Host、Direct Host 均完成编译；
- 既有自动化基线：29 项 Widget 定向测试、2061 项全量测试无失败；
- 尚未补充：当前提交关闭 Xcode 后的 test host 重跑。

本轮不把既有测试记录表述为当前提交重新执行。

---

## 5. 人工验收状态

签名阻断已经解除，但本轮没有使用桌面自动化操作 Xcode。Widget Gallery、所有尺寸、
真实数据、Deep Link、账号隔离、双渠道同时安装、深浅色和 VoiceOver 继续保持未勾选，
等待 dong4j 按详细方案 §10.2.1 / §10.3 手工验证。

---

## 6. 收口判断

自动化可观察范围内没有新的 P0 / P1 / P2。下一轮需要重新检查本报告、checklist、
详细方案、提交历史和签名证据；若仍无新增问题，可记为第二轮连续 Clean。最终结果报告
必须继续区分自动化完成项与人工待验收项。
