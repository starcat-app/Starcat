# 精选发布台第 1 轮审查报告

- 审查时间：2026-08-13 22:36 CST
- 审查范围：需求说明、技术设计、Checklist、精选发布领域模型、Resolver、管理员 API、Session、SwiftUI 窗口、19 项定向测试与真实窗口截图
- 审查结论：发现 1 项高严重度问题、1 项中严重度文档偏差；修复后进入下一轮审查

## 检查项目

- 文档审查：逐条比对需求流程、API 鉴权、状态恢复与当前代码。
- 代码审查：检查入口/执行层权限、密钥隔离、地址核验、幂等提交、轮询生命周期和 UI 状态守卫。
- 测试审查：核对 4 个新增测试套件的 19 项测试及覆盖边界。
- 工程进度审查：核对专项 Checklist 与实际提交、构建和 UI 验收证据。

## 发现的问题与修复计划

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
| --- | --- | --- | --- | --- |
| R1-01 | 高 | `startPolling` 在第一次瞬时网络错误后立即停止，短暂断网会导致非终态批次不再自动追踪。 | 对瞬时错误执行有限连续重试，保留 batch ID；达到上限后允许维护者手动恢复状态查询，并补会话测试。 | 已修复 |
| R1-02 | 中 | 技术设计写成 `X-Admin-Key`，与现有 weekly-api 和客户端实现的 `Authorization: Bearer` 不一致。 | 更正文档并再次核对 API 契约测试。 | 已修复 |

## 已确认无问题项

- GitHub 数字 ID 同时约束入口与 Session 执行层。
- 管理员密钥使用独立 `weekly-admin` namespace，未进入 UserDefaults、日志或源码。
- 最终地址编辑会撤销 GitHub 核验和人工确认。
- `partial_success` 被建模为终态，逐项错误可显示。
- 真实 1080×720 独立窗口已完成截图与可访问性树验收；无密钥时发布操作不可用。
