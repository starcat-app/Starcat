# 精选发布台第 7 轮审查报告

- 审查时间：2026-08-14 00:26 CST
- 审查范围：精选发布全目录静态引用、AI/Weekly 依赖边界、废弃代码、测试归属与文档术语
- 审查结论：发现 1 项中严重度架构残留，已完成清理并安排回归

## 检查项目

- 文档审查：检查是否仍把 GitHub-only Resolver 描述为当前识别主链路。
- 代码审查：搜索 `CuratedProjectResolver`、`CuratedProjectResolving` 和旧 resolution 类型的全部引用。
- 测试审查：确认旧 Resolver 测试是否还代表当前产品行为。
- 工程进度审查：核对“识别不只依赖 GitHub API”的需求是否在源码结构上也唯一成立。

## 发现的问题与修复计划

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
| --- | --- | --- | --- | --- |
| R7-01 | 中 | 旧 `CuratedProjectResolver` 已不再被生产装配，但源文件和专属测试仍编入工程，形成 GitHub-only 的平行废弃实现，容易让后续维护误接回旧路径。 | 删除旧 Resolver 与旧测试；把 AI 服务仍需要的 `repositoryNotFound` 错误移入 `CuratedProjectIdentificationError`；重新生成工程并回归。 | 已修复 |

## 修复说明

- 生产识别路径现在只有 `CuratedProjectIdentificationService`。
- `GitHubRepositoryAddress` 仍作为严格地址解析和值对象保留；它不是自然语言识别器。
- 旧 Resolver 的地址解析覆盖已由领域模型测试覆盖，AI 证据与核验由 AI 服务/会话测试覆盖。

## 验证状态

- 删除后重新执行 `xcodegen generate`，工程不再包含旧 Resolver 源文件和测试。
- 精选发布台 5 个 Suite 共 25 项测试通过。
- Starcat 全量 2206 项、259 个 Suite 通过，保留 1 个项目既有 known issue。
- `rg` 确认生产与测试目录不再存在旧 Resolver 类型引用。
