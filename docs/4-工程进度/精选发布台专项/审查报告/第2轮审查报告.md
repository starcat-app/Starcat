# 精选发布台第 2 轮审查报告

- 审查时间：2026-08-13 22:42 CST
- 审查范围：第 1 轮修复、需求交互约束、Resolver/Session 数据流、UI 候选选择、API DTO 与 weekly-api Go 模型
- 审查结论：发现 1 项高严重度交互问题、1 项低严重度文档偏差；修复后进入全量验证

## 检查项目

- 文档审查：核对“多候选显式选择”、状态流和 Bearer 鉴权描述。
- 代码审查：复核 GitHub 精确匹配、多候选合并、编辑失效、轮询重试和 API DTO 字段。
- 测试审查：检查多候选是否有会话层回归测试。
- 工程进度审查：核对第 1 轮问题是否已有代码、测试和 commit 闭环。

## 发现的问题与修复计划

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
| --- | --- | --- | --- | --- |
| R2-01 | 高 | 普通线索返回多个候选时，Session 自动应用第一项，可能让维护者在未显式选仓库的情况下继续确认与发布。 | 仅在候选唯一时自动应用；多候选保持未核验状态，必须点击候选，并补会话回归测试。 | 已修复 |
| R2-02 | 低 | 技术设计状态流包含不存在的 `resolved` operation，和实际 `idle` + `verifiedCandidate` 投影不一致。 | 按真实状态机更正文档。 | 已修复 |

## 已确认无问题项

- 客户端 DTO 与 weekly-api `IngestBatchAcceptance`、`IngestBatch`、`IngestItem`、`SourceStatus` 字段兼容。
- GitHub 精确核验按规范化后的 `owner/repo` 二次匹配，不采用模糊结果。
- 第 1 轮轮询恢复测试共 7 项通过，报告中的两项问题均已有提交闭环。
