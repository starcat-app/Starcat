# API 聚合与 Kit 抽离专项第 3 轮审查报告

> 日期: 2026-08-08
> 范围: 聚合构建、运行期配置隔离、内部路由、迁库恢复、Sharing 公网契约、客户端契约
> 结论: **本地阻塞项已修复；后续已完成首轮五库种子迁移与解除维护后的功能验证，最终同步与生产复验仍未执行，当前不能宣告最终切流完成**

## 1. 本轮发现与处置

| 级别 | 问题 | 处置 |
|---|---|---|
| P0 | 聚合 Dockerfile 依赖兄弟仓，但 Makefile 从 `starcat-api/` 作为 context 部署 | context 改为 `supports/`；新增白名单 `.dockerignore`，排除 `.env`、数据库、备份与 Git 元数据 |
| P0 | `Pin` 永久暴露全部裸 env，缺少 Discovery Admin 时可能继承 Weekly Admin | 改为临时 `Apply` + restore；仅显式 Pin wiki 四个运行期 key；Discovery Admin 同步改为必填 |
| P0 | 原 SOP 会在业务已打开 SQLite 时覆盖文件，且五次整包恢复会反复清空 `/data` | 新增维护模式、五备份离线合并与完整性检查、聚合一次性恢复门禁 |
| P1 | Weekly→Wiki loopback 请求没有 `X-SC-Svc` | 补头并新增 notifier 单测；secrets 同步同时下发 Weekly Wiki URL / Key |
| P1 | 停旧 Sharing App 前没有验证 `starcat.ink` 公开路由 | SOP 增加代理注头以及分享页、仓库页、OG、字体/Logo 人工门禁 |
| P1 | 旧报告把“本地代码完成”写成“无阻塞遗留” | README、checklist、结果报告改为区分本地证据和生产人工验收 |

## 2. 客户端边界

按 dong4j 决策，不增加客户端切流等待或双轨逻辑。Starcat 仍固定使用 `starcat-api.fly.dev` + `X-SC-Svc`；约束转为发布门禁：聚合服务必须完成迁库、解除维护模式和全链路验收，再发布 Starcat 正式版。

## 3. 自动化证据

- 八个 Go 仓最终 `go test ./...` 全部通过；`starcat-api` 新增 env 隔离 / 维护 handler 测试，`starcat-weekly-api` 新增 Wiki notifier 路由头测试；
- Wiki 的真实 CodeWiki 网络测试在首次并行运行遇到一次上游 `EOF`，隔离重试及全包复跑通过，判定为既有外部集成测试波动；
- 合并恢复脚本使用五个临时 SQLite 归档完成端到端冒烟；三个相关 shell 脚本通过 `bash -n`；
- `StarcatGatewayRoutingTests` 定向测试通过，Starcat Debug build 通过；
- `git diff --check` 通过。

## 4. 人工闸门

- `make -C supports fly-build-check-api` 已在 OrbStack 可用后补跑通过，`Check complete, no warnings found`；
- Fly App、Volume、Secrets、首轮五库种子迁移与解除维护后的六服务功能验证已完成；仍待写入冻结窗口的最终同步和生产复验；
- `starcat.ink` 反向代理切换与公开路由验证；
- 旧 App 停机观察、Volume 保留和最终销毁确认。

以上剩余项未完成前，专项状态只能是“首轮种子迁移与功能验证完成，待最终同步 / 生产复验”。
