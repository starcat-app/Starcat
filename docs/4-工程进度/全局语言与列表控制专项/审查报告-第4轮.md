# 全局语言与列表控制专项审查报告 - 第4轮

时间: 2026-07-04 01:15

## 审查范围

- 排序条件最终口径是否与本次确认一致。
- 代码中是否仍残留旧 Weekly 排序 case。
- 文档、详细设计、工程进度是否同步本次排序二次收敛。
- 测试与验证记录是否覆盖客户端和 weekly-api。

## 审查结果

- 通过: 公共排序顺序已统一为默认、Stars、更新、创建、名称。
- 通过: 模块专属排序已放到公共排序下方,菜单使用分割线隔开。
- 通过: 星标排序包含最高 Health、最高 OpenSSF,未暴露 Star 时间排序入口。
- 通过: 探索-发现不再暴露活跃度排序。
- 通过: 探索-趋势新增上升趋势,并补齐创建时间排序。
- 通过: 探索-新发布只保留最新发布专属排序。
- 通过: 探索-周刊不再显示“最新事件”,默认内部仍按 `latest_event_at desc`。
- 通过: Activity 普通分类保持最新优先 / 最早优先。
- 通过: 正式方案、详细设计、专项进度、主进度索引均已回填。

## 已修复问题

- 修复专项进度文案中“最近星标 / 最早星标”的旧表述,改为“Star 时间排序入口”。
- 修复正式方案和详细设计中旧的“推荐 / 最新事件 / Star 时间”排序方案描述。

## 验证

- `jq empty Starcat/Resources/Localizable.xcstrings` 通过。
- `xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' -only-testing:StarcatTests/AppSettingsTests -only-testing:StarcatTests/WeeklyBulkRepositoryTests -only-testing:StarcatTests/WeeklyDTOTests -only-testing:StarcatTests/RepoRepositoryTests test` 通过。
- `go test ./internal/handler ./internal/store` 于 `supports/starcat-weekly-api` 通过。
- `go test ./...` 于 `supports/starcat-weekly-api` 未完全通过,失败为既有样本缺失: `parser/TestParseIssue399` 读取 `../../docs/issue-399.md` 失败。

## 结论

本轮未发现新增 blocker。剩余风险是没有做点击级 UI 验证,但排序入口、枚举、API 参数、文案和专项文档已完成一致性收敛。
