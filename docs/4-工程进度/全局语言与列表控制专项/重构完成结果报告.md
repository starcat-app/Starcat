# 全局语言与列表控制专项重构完成结果报告

时间: 2026-07-03 22:55

## 完成范围

- Weekly 从 Activity 迁移到 Explore。
- Weekly 语言筛选迁到 Sidebar,中栏保留筛选 / 排序 / 刷新。
- Manage / Explore / Trending / Weekly / Activity 的中栏排序入口统一到 `UnifiedSortMenu`。
- Explore 默认分类、语言、排序等列表偏好按 GitHub 账号本地持久化。
- Settings、菜单栏图标右键菜单、Dock 菜单均提供 `重置列表偏好` 入口,并复用同一通知和同一确认 alert。
- 重置列表偏好只清当前账号列表偏好,不清 repo 缓存、搜索历史、标签、笔记、知识库状态、Smart Collection 规则或草稿。
- 匿名 telemetry 增加非敏感 `list_preferences_reset` 事件。
- 专项 checklist 全部回填。

## 关键实现文件

- `Starcat/Features/Explore/ExploreView.swift`
- `Starcat/Features/Explore/ExploreModels.swift`
- `Starcat/Features/Activity/WeeklyContentView.swift`
- `Starcat/Features/Home/SidebarView.swift`
- `Starcat/Features/Home/RepoListView.swift`
- `Starcat/Features/Home/HomeView.swift`
- `Starcat/Shared/Components/Toolbar/UnifiedSortMenu.swift`
- `Starcat/Core/Settings/AppSettings.swift`
- `Starcat/App/StatusBarController.swift`
- `Starcat/App/AppDelegate.swift`
- `Starcat/Features/Settings/SettingsView.swift`

## 验证结果

- `jq empty Starcat/Resources/Localizable.xcstrings` 通过。
- 相关单测通过:
  `ExploreDiscoveryViewModelTests`,`WeeklyBulkRepositoryTests`,`MultiSelectionStoreTests`,`AppSettingsTests`,`ActivityViewModelTests`。
- `xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' build` 通过。

## 审查结果

- 第 1 轮:发现并修复旧 Activity Weekly 注释与 Weekly 语言来源关系未回填问题。
- 第 2 轮:确认剩余 UI 自动化受限,完成代码路径审查并保留限制说明。
- 第 3 轮:确认 checklist、文档、代码、单测、构建一致,未发现新增问题。

## 已知限制

- 未完成真实点击级 UI 验证。当前报告只确认代码路径、构建运行和可测试逻辑。
- 后续如需像素/点击级验收,需要先启动 Orca runtime 或由人工在当前登录态下逐项点验。
