# 全局语言与列表控制专项审查报告 - 第 3 轮

时间: 2026-07-03 22:55

## 审查范围

- checklist 完成状态。
- 文档、代码、单测、工程进度一致性。
- 最终构建与相关单测。

## 审查结论

1. 专项 checklist 已无未勾选项。
2. 需求讨论、正式方案、详细设计、工程进度专项文档均存在。
3. 代码中已无 `ActivityCategory.weekly` 实现残留;Weekly 入口归属 Explore。
4. 旧 Activity Weekly 注释已清理;剩余 `case weekly` 均为 ExploreMode、Weekly 网络模型、卡片徽章或第三方服务枚举。
5. 相关单测、String Catalog 校验、最终 build 均通过。

## 验证命令

- `jq empty Starcat/Resources/Localizable.xcstrings`
- `xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' -only-testing:StarcatTests/ExploreDiscoveryViewModelTests -only-testing:StarcatTests/WeeklyBulkRepositoryTests -only-testing:StarcatTests/MultiSelectionStoreTests -only-testing:StarcatTests/AppSettingsTests -only-testing:StarcatTests/ActivityViewModelTests test`
- `xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' build`

## 已知限制

- UI 点击级验证未完成。原因:Orca runtime 未启动,System Events 无法稳定读取 SwiftUI 主窗口 / Settings 窗口。
- checklist 中 UI 项已按“代码路径审查 + 构建运行检查;自动化 UI 受限”标注,没有记录为完整点击验收。

## 结论

本轮未发现需要继续修复的问题。可以进入完成报告。
