# 全局语言与列表控制专项审查报告 - 第 1 轮

时间: 2026-07-03 22:51

## 审查范围

- 专项 checklist 未完成项。
- Weekly 从 Activity 迁移到 Explore 后的代码注释和职责归属。
- PR-8 单测、构建、String Catalog 验证记录。

## 发现问题

1. `WeeklyContentView` / `WeeklySelectionService` / `WeeklyDetailView` / `AppDependencies` 中仍有旧注释写法,把 Weekly 描述为 Activity 分类。
2. PR-3 中“Weekly 语言来源与 Weekly bulk cache 的关系”未回填,文档缺少明确边界。
3. PR-8 中 4 个 UI 手工验证项仍未完成真实交互验证。

## 修复动作

- 已将旧注释统一改为 Explore Weekly。
- 已在专项进度中明确:Sidebar 语言列表以 `WeeklyLanguageStore.displayList` 为 UI 来源,Weekly bulk cache 只服务列表本地筛选与缓存。
- 已保留 UI 手工验证项未勾选,等待后续审查阶段处理。

## 验证记录

- 变更前相关测试已通过:
  `xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' -only-testing:StarcatTests/ExploreDiscoveryViewModelTests -only-testing:StarcatTests/WeeklyBulkRepositoryTests -only-testing:StarcatTests/MultiSelectionStoreTests -only-testing:StarcatTests/AppSettingsTests test`
- 变更前 `jq empty Starcat/Resources/Localizable.xcstrings` 通过。
- 变更前 `xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' build` 通过。

## 后续动作

- 继续处理 UI 手工验证项。
- 第二轮审查需复查 checklist 是否只剩 UI 项,并检查 docs / code 是否还有旧 Activity Weekly 表述。
