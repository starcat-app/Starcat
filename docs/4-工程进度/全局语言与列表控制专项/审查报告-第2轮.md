# 全局语言与列表控制专项审查报告 - 第 2 轮

时间: 2026-07-03 22:54

## 审查范围

- 第一轮修复后的旧 Activity Weekly 表述复查。
- 专项 checklist 剩余项。
- UI 验证可执行性。

## 审查结论

1. 代码中的旧 Activity Weekly 表述已清理。剩余 `ActivityCategory.weekly` 只出现在详细设计中“删除旧入口”的方案描述里,属于历史决策上下文。
2. PR-3 到 PR-8 的代码和自动化验证项已回填。
3. UI 自动化验证受限:Orca runtime 未启动;System Events 能看到 Starcat 进程和顶部菜单,但无法稳定读取 SwiftUI 主窗口 / Settings 窗口。当前没有继续点击,避免误触真实账号数据。

## 代码路径审查结果

- Explore Weekly 入口: `ExploreMode.weekly` / `ExploreView` / `WeeklyContentView(selectedLanguage:)` 路径存在。
- Sidebar 语言列表:Explore Weekly 使用 `weeklyLanguageSidebarContent`,语言来源为 `WeeklyLanguageStore.displayList`。
- Weekly 中栏控制:保留筛选菜单,排序使用 `UnifiedSortMenu`,刷新使用 `SyncIconButton`。
- 重置入口:Settings、菜单栏图标右键菜单、Dock 菜单均发 `starcatResetListPreferencesRequested`,由 `HomeView` 统一 alert 确认后执行。

## 已知限制

- 未完成真实鼠标点击级 UI 验证。已在 checklist 对应项标注“自动化 UI 受限”。
- 后续若要做像素/点击级验证,需要先启动 Orca runtime 或由人工在当前登录态下点验。

## 后续动作

- 第三轮审查聚焦文档/代码/测试/工程进度一致性。
- 若第三轮无新增问题,输出重构完成结果报告。
