# 审查报告 05：测试、构建、文档、工程进度与提交一致性

> 审查时间：2026-07-27
>
> 审查状态：已闭环，无未关闭 P0 / P1 / P2
>
> 审查基线：Starcat `128a0c06`；`starcat-discovery-api` `c207491`；`starcat-localization` `09cc560`

## 1. 审查范围

- 关闭 Xcode 后重新生成工程，运行 Starcat 洞察定向测试、下钻数据库测试、全量测试和 Debug build。
- 运行 `starcat-discovery-api` 的格式、vet、race test、coverage 与构建。
- 核对 49 / 50 号设计、API / README、验收步骤、需求追踪矩阵和专项 Checklist。
- 只读核对 `docs/功能实现总览.md` 与 Changelog 授权边界。
- 审计 Starcat、Discovery API 和本地化仓库的分支、领先提交、message 格式、工作区和未 push 状态。

## 2. 自动化结果

| 项目 | 结果 |
|---|---|
| `xcodegen generate` | 通过，工程文件无未提交变化 |
| 洞察 / Star History 定向矩阵 | `72 tests in 12 suites`，0 失败 |
| `RepoRepositoryTests` | `32 tests in 1 suite`，0 失败 |
| Starcat 全量 | `1800 tests in 209 suites`，0 失败、1 个既有 known issue，`TEST SUCCEEDED` |
| Starcat Debug build | 通过，`BUILD SUCCEEDED` |
| Discovery API | `make check && go build ./...` 通过 |
| 静态检查 | 两仓 `git diff --check`、String Catalog JSON、Insights i18n / 颜色扫描通过 |

全量 `.xcresult` 摘要为 `failedTests = 0`、`expectedFailures = 1`。Testing Library 控制台计数和 xcresult 对动态参数 / XCTest 的汇总口径不同，本专项继续使用项目既有的控制台 `tests in suites` 口径，避免混写两种总数。

## 3. Findings

### R05-F01：设计和验收文档保留过期测试基线

- 等级：P1
- 状态：已修复
- 证据：
  - `49-洞察中心详细设计.md`、专项 Checklist、`验收步骤说明.md` 和需求追踪矩阵仍记录全量 `1802 tests in 209 suites`。
  - Checklist 和验收步骤仍记录洞察定向 `68` 项 / `11` 个 Suite。
  - 本轮从干净工作区重跑后的唯一当前结果是全量 `1800 tests in 209 suites`、洞察定向 `72 tests in 12 suites`、下钻 `32 tests in 1 suite`。
- 根因：第一轮删除废弃 Mock 测试、第三轮增加快速切仓竞态回归后，测试集合发生变化，但实现文档没有在最终矩阵重跑后刷新。
- 修复要求：
  1. 同步更新 49 号设计、Checklist、验收步骤和需求追踪矩阵。
  2. 保留测试日期、0 失败、1 个 known issue 与控制台计数口径。
  3. 不把未执行的人工 UI 或 M0 验收写成通过。
- 修复 commit：`970cceee`（`docs(insights): 刷新洞察测试基线`）
- 验证结果：四处自动化证据已统一为全量 `1800 tests in 209 suites`、洞察定向 `72 tests in 12 suites` 和下钻 32 项；旧数字只保留在本 finding 的历史证据中。

### R05-F02：Checklist 顶部状态仍称远端能力待实施

- 等级：P2
- 状态：已修复
- 证据：Checklist 顶部仍写“GitHub Metrics / Discovery Star 历史待实施”，但 §7～§10、49 / 50 号设计、两仓代码和自动化均表明实现已经完成；真实剩余门槛是 M0、部署、翻译和人工 UI 验收。
- 影响：读者只看文档首屏会错误判断工程进度，与同一文档后续 checkbox 自相矛盾。
- 修复要求：将顶部状态改为“代码与自动化已完成，外部授权和人工验收待完成”，并保留 M0 / 部署未获授权的降级边界。
- 修复 commit：`7be1205c`（`docs(insights): 修正洞察专项实现状态`）
- 验证结果：首屏状态已与 49 / 50 号设计和后续 Checklist 一致，并逐项保留 M0、部署、翻译与人工 UI 外部门槛。

## 4. 无问题项

- 49 / 50 号方案描述的入口、数据层、v16 migration、Star History 两段式来源、私有仓库边界、缓存、错误和部署门槛与代码一致。
- Discovery API README / API / 环境文档明确 `STAR_HISTORY_ENABLED=false` 默认值和 M0 前禁止启用；本轮未部署、未修改 Fly secrets、未运行 BigQuery。
- Starcat `dev` 相对 `origin/dev` 领先 76 个本地提交；Discovery API `dev` 领先 12 个；本地化仓库 `dev` 领先 2 个。三个工作区均 clean。
- 主仓 76 个、Discovery 12 个专项提交均符合 `<type>(<scope>): <中文摘要>`；scope 稳定、摘要含中文，`improve` 类型符合仓库提交规范。
- `git reflog origin/dev` 最近状态均为 `update by push`，本轮没有更新远端引用；没有执行 push、PR、tag、打包或发布。
- 本地化仓库的 `edce82d`、`09cc560` 已提交，工作区 clean；15 个非中英文 Locale 仍保持待翻译 / 审阅。

## 5. 总览与 Changelog 授权核对

`docs/功能实现总览.md` 当前没有“洞察中心 / 仓库洞察”条目，本轮只读未修改。若 dong4j 后续明确授权“同步总览”，建议在 P0“主界面”增加：

```markdown
- [x] **洞察中心与仓库洞察** — 支持收藏全局统计、结构化下钻、仓库活动与 Star 趋势 — `Starcat/Features/Insights/`、`Starcat/Features/Home/ManageDetailContent.swift` — 2026-07-27
> 实现：使用现有三栏与仓库详情框架，本地 SQLite 聚合和 v16 缓存承载统计；公开仓库长期 Star 估算在 M0 与部署授权前保持关闭，私有仓库始终只使用本机快照。
```

获授权写入时还需按总览规范重新计算进度仪表盘，并在变更日志顶部增加一条不超过 80 字的记录；不能直接复制当前数字。四份 Changelog 同样未获授权、未修改。该功能具有用户可感知变化，建议在人工 UI 终验后由 dong4j 决定是否纳入当前待发布版本。

## 6. 下一步

R05-F01 与 R05-F02 已闭环。下一步开始两轮从零复审，不能用本轮结论替代最终复审。
