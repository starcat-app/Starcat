# 洞察中心与仓库星标历史需求追踪矩阵

> 用途：把 49 / 50 号详细设计、专项 Checklist、代码、测试、提交和验收证据关联起来。
>
> 规则：本矩阵只记录可核验事实；未实现、未测试或需要外部授权的事项保持“待完成 / 待授权”，不以计划代替结果。

## 1. 续实施基线

记录时间：2026-07-27

| 仓库 | 分支 | 基线 HEAD | Worktree | Dirty 状态 |
|---|---|---|---|---|
| Starcat | `dev` | `6c6293c8bab8affc3f135b41ad7b2be4a61055a3` | `/Users/dong4j/Developer/1.AI/ai-incubator/Starcat` | clean |
| `starcat-discovery-api` | `dev` | `a52a18cd82bae6910c4c69e4318bc2f25be0c38d` | `/Users/dong4j/Developer/1.AI/ai-incubator/Starcat/supports/starcat-discovery-api` | clean |

基线核对结论：

- Starcat 最新数据库迁移为 `v15-repo-pins`，后续洞察 schema 必须追加真实下一版本 `v16-repository-insights`。
- `GlobalRepoFilterState.neutral` 与 `applyTemporaryGlobalFilters(...)` 已存在，可作为洞察结构化下钻的唯一临时筛选入口。
- `ManageDetailContent` 已提供 `README / 洞察`切换；本地信息、GitHub 活动、贡献者、社区、时间线与 Star 趋势均已接真实数据层。
- `starcat-discovery-api` 已有 SQLite store、Bearer 鉴权、统一 envelope、scheduler 和有界 GitHub token pool。
- 生产 ingest 已通过 `Store.RecordDailySnapshot(...)` 写入 `repo_daily_snapshots`；专项只能补保护测试，不能重复实现另一条快照链路。

## 2. 外部授权门槛

| 门槛 | 当前状态 | 未授权时的处理 |
|---|---|---|
| BigQuery / GCP 凭据、付费查询与 `maximumBytesBilled` | 待 dong4j 另行明确授权 | 只实现可替换 Provider、预算护栏和假数据测试，不执行真实查询，不伪造 M0 GO |
| Fly secrets、测试 / 生产部署 | 未授权 | 只完成代码、配置文档和本地验证 |
| `docs/功能实现总览.md` | 未授权写入 | 只读核对，在审查报告和结果报告中给出拟同步内容 |
| 四份 Changelog | 未授权写入 | 结果报告中给出是否建议纳入待发布版本 |
| push / PR / tag / 打包 / 发布 | 明确禁止 | 全程只保留本地提交 |

## 3. 需求追踪

| ID | 需求与唯一口径 | Checklist | 主要代码位置 | 自动化测试 | 提交 | 验收证据 | 状态 |
|---|---|---|---|---|---|---|---|
| INS-01 | 洞察作为顶级入口并复用 Starcat 三栏 | §1、§4.1 | `Features/Home/HomeView.swift`、`Features/Insights/InsightsNavigationViews.swift` | `InsightsMockDataTests` | `19ce6c25`、`ea819bcb` | 前端 Mock 截图、运行态交互 | 已完成 |
| INS-02 | 我的洞察范围只允许“全部收藏 / 知识库” | §1、§3.1 | `InsightsModels.swift`、`MyInsightsSnapshotProvider.swift`、`MyInsightsViewModel.swift` | 范围 SQL、非收藏入库边界与范围切换失败不串值 | `d45789be`、`4891803a`、`c6537ff1` | Provider / ViewModel 定向测试通过 | 已完成 |
| INS-03 | 一次一致 SQLite 读取生成完整 Snapshot | §3.1 | `MyInsightsSnapshotProvider.swift` | 一致读取、revision、主动失效与 60 秒 TTL | `d45789be` | `MyInsightsSnapshotProviderTests` 4 项通过 | 已完成 |
| INS-04 | 未读、已整理、语言、Health / OpenSSF 使用唯一 SQL 口径 | §3.1 | `MyInsightsSnapshotProvider.swift` | 缺失 note、派生整理、未知语言、Top 8、coverage | `d45789be` | `MyInsightsSnapshotProviderTests` 4 项通过 | 已完成 |
| INS-05 | 需要处理项按范围隐藏知识库专属噪音 | §3.2 | `InsightsModels.swift`、`MyInsightsSnapshotProvider.swift`、`MyInsightsView.swift` | RAG 来源、模型变化、空库、缺失派生数据与非收藏入库边界 | `d45789be`、`4891803a`、`c6537ff1` | 6 项 Provider 测试通过，收藏范围有知识库切换提示 | 已完成 |
| INS-06 | 所有数字从 `.neutral` 结构化下钻，数量与列表一致 | §4.2 | `InsightsDrillDown.swift`、`GlobalRepoFilterState.repoListFilters(...)`、`RepoRepository.makeListQuery(...)`、`applyTemporaryGlobalFilters(...)` | `InsightsDrillDownTests`、`HomeViewModelFilterSortTests.temporaryGlobalFilterSessionLifecycle`、`RepoRepositoryTests.listCountHonorsInsightsDrillDownFilters` | `1c56a6d6`、`2f1edba5`、`30a89d46` | 定向测试通过；路由到 Manage 数据库计数使用唯一筛选转换入口 | 已完成 |
| INS-07 | 洞察刷新只重读本地数据库，稳定呈现 loading / stale / error | §4.1 | `MyInsightsViewModel.swift`、`MyInsightsView.swift` | 首次成功、刷新失败保留旧值、范围失败清旧值 | `4891803a` | 3 项 ViewModel 测试通过，`SyncIconButton` 仅失效本地缓存 | 已完成 |
| INS-08 | 统一追加 v16 两张职责单一的缓存表 | §5.1、§9.1 | `DatabaseMigrationsV1.swift` | `DatabaseMigrationsV1Tests` 覆盖空库、v15 升级、重复迁移、约束与级联 | `fe18ac0f` | `DatabaseMigrationsV1Tests` 定向测试通过，既有仓库与私有笔记保持不变 | 已完成 |
| INS-09 | 仓库洞察缓存支持 TTL、ETag、SWR 和损坏 payload 隔离 | §5.2 | `RepositoryInsightsSnapshotRecord.swift`、`RepositoryInsightsCache.swift` | `RepositoryInsightsCacheTests`、`DatabaseMigrationsV1Tests.repositoryInsightsCachesCascadeWithRepo` | `2caa0222` | 定向测试通过；15 分钟 / 24 小时 TTL、覆盖写、stale 返回、损坏行隔离和级联已验证 | 已完成 |
| INS-10 | README / 洞察在主窗口与独立详情窗口复用 | §6.1 | `ManageDetailContent.swift`、`RepoDetailWindowController.swift` | `ManageDetailContentTests` 覆盖模式切换、仓库切换与 README 生命周期 | `0cf1000a`、`ee857124`、`ef5fd780` | 两个场景复用 `ManageDetailContent`；完整独立窗口人工矩阵待验收 | 已完成（代码）/ 待人工验收 |
| INS-11 | 仓库洞察各区块独立状态并防旧 repo 响应覆盖 | §6.2 | `RepositoryInsightsViewModel.swift`、`RepositoryInsightsView.swift`、`ManageDetailContent.swift` | `RepositoryInsightsViewModelTests` 覆盖真实本地映射、独立失败和 generation 丢弃 | `1185c726`、`ee857124` | 定向测试通过；Release、Health、OpenSSF、License、Community 已接本地真实数据 | 已完成 |
| INS-12 | 类型化 GitHub Metrics Client 与 RAG 共用协议层 | §7.1 | `GitHubRepositoryMetricsClient.swift`、`GitHubRAGRemoteContextProvider.swift` | `GitHubRepositoryMetricsClientTests`、`KnowledgeRAGCoreTests` | `1fcef65e` | 类型化 DTO、统一请求头、202/403/429/404/422、Retry-After、rate reset、请求串行与 RAG 审计回归通过 | 已完成 |
| INS-13 | PR / Issue / Commit / contributors / community / timeline | §7.2 | `RepositoryInsightsRemoteProvider.swift`、`RepositoryInsightsViewModel.swift`、`RepositoryInsightsView.swift` | 活动范围、52 周提交裁剪、contributors / community 映射、时间线合并排序、缓存优先、刷新失败保旧值与旧响应丢弃已覆盖 | `cbbf8df8`、`b7a50ec2`、`cbcc82c9`、`84059a1c` | 全部远端区块定向测试通过，完整运行态待最终验收 | 已完成（代码）/ 待人工验收 |
| STAR-01 | GH Archive 数据质量、成本与覆盖先经过 M0 | §2.2 | 外部 Spike，不进入产品代码 | 六个样本已固定，真实查询待授权 | 待回填 | [`M0-星标历史数据可行性.md`](M0-星标历史数据可行性.md) | 待授权 |
| STAR-02 | Discovery 独立缓存，不污染 catalog | §8.1 | `starcat-discovery-api/internal/model/star_history.go`、`internal/store/star_history.go`、`internal/store/migrations.go` | 独立表、状态约束、同仓去重、过期 building 恢复与 ready / failed round-trip | `3f7341c`、`cc8d307`（`starcat-discovery-api`） | store / ingest 定向测试通过，既有每日快照生产调用已锁定 | 已完成 |
| STAR-03 | Provider 参数化 repo ID、日期和最大扫描预算 | §8.2 | `starcat-discovery-api/internal/starhistory/provider.go`、`bigquery_provider.go`、`bigquery_rest_runner.go` | 命名参数、强制 dry run、预算拒绝、REST DTO 与未完成查询测试 | `40415fe`、`3228cdc`（`starcat-discovery-api`） | Fake / httptest 通过；真实 BigQuery 验证仍待 M0 授权 | 已完成（实现）/ 待授权（M0） |
| STAR-04 | 累计、归一化、单调估算、精确下降与范围降采样 | §8.2 | `starcat-discovery-api/internal/starhistory/normalize.go`、`downsample.go` | 零事件、日聚合、末点校准、同日精确优先、真实下降、日/周/月与 500 点上限 | `43ca9d4`、`b433f37`、`69f6b3d`（`starcat-discovery-api`） | starhistory 定向测试通过 | 已完成 |
| STAR-05 | 首次 miss 异步构建、去重、202、ETag 与稳定错误契约 | §8.3 | `internal/starhistory/service.go`、`internal/handler/star_history.go` | store / service / handler 测试覆盖去重、重启恢复与完整 HTTP contract | `23025f1`、`792cf07`、`ff2dd7e`（`starcat-discovery-api`） | `make check` 通过 | 已完成（代码）/ 未部署 |
| STAR-06 | 本机 metadata 成功落库后幂等记录每日快照 | §9.1 | `RepoStarHistoryRepository.swift` 与 Stars / metadata 成功落库路径 | `RepoStarHistoryRepositoryTests` 覆盖幂等、无额外请求和远端替换边界 | `2a74c56a`、`bdebea47` | DB 定向测试通过 | 已完成 |
| STAR-07 | 客户端 cache-first 合并远端估算、Discovery 和本机点 | §9.2 | `StarHistoryAPI.swift`、`RepoStarHistoryRepository.swift`、`StarHistoryViewModel.swift` | API、Repository、ViewModel 覆盖私有拦截、合并、轮询、stale fallback 与取消 | `ae49f4fd`、`761e689b`、`49c6537c`、`41f50b6e` | Star History 定向测试通过 | 已完成 |
| STAR-08 | Star 趋势独立范围、来源、增长与非单一 hover 读数 | §9.3 | `RepositoryInsightsView.swift`、`StarHistoryViewModel.swift` | 趋势状态、范围、辅助读数和真实数据接入已覆盖 | `25db02da`、`a93e09ab`、`37c2336e`、`44c06bc5` | 自动化通过；完整运行态待人工验收 | 已完成（代码）/ 待人工验收 |
| QA-01 | i18n、颜色、Focus Ring、Reduce Motion、VoiceOver 与长文案 | §10 | `Localizable.xcstrings`、洞察 Views、18 Locale `.xcloc` | JSON、静态规范、辅助读数与自适应布局构建验证 | `51c2e90d`、`ff564c2b`、`edce82d`（本地化仓库） | en / zh-Hans / zh-Hant 完整；其余 15 Locale 新字符串待人工翻译审阅 | 已完成（代码）/ 待翻译与人工验收 |
| QA-02 | 两仓定向 / 全量测试、构建和静态检查 | §11 | 两仓测试目录、Makefile | Starcat 1802 项；Discovery `make check` 与 `go build ./...` | `0af78812`、`a6c84b96` | 自动化矩阵通过 | 已完成 |
| DOC-01 | 49 / 50 号设计、API、README、Checklist 与代码一致 | §12 | 专项文档和后端 README | 链接 / 契约核对 | 本次文档提交待回填 | 审查报告 | 进行中 |
| REVIEW-01 | 五维审查 + 两轮连续无新增 P0 / P1 / P2 | §13 | 专项目录审查报告 | 每轮刷新证据 | 待回填 | 审查报告 01～最终复审 | 待完成 |
| RESULT-01 | 新增结果报告并记录两仓最终状态与未授权动作 | §14～§15 | `结果报告.md` | 最终机器证据 | 待回填 | 结果报告 | 待完成 |

## 4. 更新约定

1. 每个小功能提交后，立即把对应行的代码、测试、commit 和状态更新为真实值。
2. 审查发现不覆盖旧证据；新增 finding、修复 commit 和关闭结论。
3. 外部授权动作只有在真实执行并取得证据后才能标记完成。
4. 最终结果报告以本矩阵、专项 Checklist 和两仓 Git 历史的交集为准。
