# 知识库 RAG 收尾与稳定性结果报告

> 状态: 自动化整改完成；真实环境与大数据人工验收待执行
> 日期: 2026-07-13
> 执行清单: `RAG收尾与稳定性-checklist.md`

## 已交付

- 阶段 1：独立窗口字号实时响应 `InterfaceScale`；会话与知识库浏览器使用最新请求门闩；历史加载完成后定位末尾；失败问题可恢复并重试。
- 阶段 2：流式 Markdown 改为快照节流；会话的 citation 与远程审计改为批量读取；调试轨迹加入内存上限。
- 阶段 3：检索、repo ID、附件提取与远程调用均有明确的数量、预算或并发上界。
- 阶段 4：认证、配置、网络、超时、格式和附件错误被分类，并提供对应的恢复动作。
- 阶段 5：模型可配置 Context Window；请求以统一预算裁剪；Composer 提供 Context Usage 环形入口、分项占比和 Prompt 预览。
- 阶段 6：较早会话按消息覆盖水位压缩为持久化语义摘要；原消息仍保留；压缩失败不丢失原文。

## 自动化证据

- 新增 100/200 条历史会话读取基线：固定执行会话、消息、citation、remote audit 四次关联读取；用例整体运行 0.095 秒。
- 关闭 Xcode 后执行：`rtk xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' test`。
- 结果：1377 通过、0 失败、7 跳过、1 项预期失败，总计 1385。

## 尚未关闭的验收项

以下项目依赖已配置的真实 GitHub/AI/自托管 Provider、真实知识库和人工交互，不能用本次本地自动化代替：

- 使用 1万+ chunk 与超长 PDF 记录 P50/P95、峰值内存和取消响应时间。
- 用真实长会话验证字号切换、历史默认到底部、流式跟随、压缩后重开及 Context Usage 与实际输入的一致性。
- 验证 401、慢网络、超时、远程确认/取消、自托管回退及真实问答评测集。

在上述验收记录补齐前，RAG 整体交付仍保持“验收中”；本报告不把它标记为正式关闭。
