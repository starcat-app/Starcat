# 知识库 RAG 代码审查报告（第 5 轮）

> 审查日期：2026-07-16  
> 审查基线：`main` / `7a02aed1`  
> 范围：第 4 轮整改后的 RAG Core、Storage、UI、迁移、测试日志、正式方案、详细设计、专项进度与 checklist

## 1. 审查结论

第 4 轮 5 项发现均已按报告回填关闭。重新扫描后未发现新的索引一致性、知识库边界、隐私持久化、无界加载、并发取消、重复读模型或文档范围错误；全量测试与双 target 编译证据有效。当前只剩 1 项固定错误格式国际化遗漏，以及必须在真实 App/数据/Provider 环境完成的人工验收。

## 2. 复核证据

- `git status --short` 为空，上一轮各修复均已独立中文 commit，未 push。
- Swift Testing 1459 项 / 174 suites 通过；唯一 known issue 是缺少 `StarcatTests/Fixtures/repo-context/starcat-sample.zip`，属于 RepoContextPacker E2E fixture，不是 RAG 失败。
- XCTest 46 项中 1 项按设计跳过、0 失败；`Starcat` 与 `StarcatDirect` Debug build 均成功。
- `RAGIndexCoverage`、旧全量 replace 结论、固定 3 轮压缩描述、当前支持 PDF/图片等旧实现声明已从活文档移除。
- 剩余 `try!` 均用于进程内固定正则字面量，`fatalError` 仅用于禁用 storyboard coder；有界 FIFO 的 `removeFirst` 只作用于小型 Debug 数组，不是无界正文队列。

## 3. P1：固定错误格式国际化

| 编号 | 发现 | 影响 | 整改要求 |
|---|---|---|---|
| P1-01 | `GitHubRemoteContextError.http` 直接返回 `GitHub HTTP <status>：<message>`，其中冒号是中文全角字符；该错误会写入 `RAGRemoteContextBlock.errorMessage` 并由 Inspector 直接展示。`RAGConversationStore` 的“持久化会话缺失”不变量错误也硬编码英文。 | 英文界面出现中文标点，中文界面出现未本地化诊断；阶段 7 的“固定错误全量 i18n”仍有最后漏口。 | 新增 en/zh-Hans catalog key；GitHub API 返回的 message 保持原文，只本地化 Starcat 生成的格式。补英文/中文环境回归。 |

## 4. 真实环境验收仍是硬门禁

静态与自动化审查不能替代以下证据：本机真实知识库边界、source 增量、中文/混输召回、附件阻断、GitHub/External Search、慢网络、账号切换、真实长会话、可选 Meilisearch/Qdrant 和脱敏问答质量指标。第 5 轮代码问题关闭后，应启动当前 Debug App 逐项验收；可选自托管服务或 Provider 未配置时只能记录“环境未具备”，不得伪造通过。

## 5. 整改回填

- [x] P1-01：GitHub HTTP 与会话存储不变量格式已接入 en/zh-Hans catalog，API message 保持原文；Localization + Core 共 124 项通过。
- [ ] 真实环境验收：待代码问题关闭后执行。
