# 洞察中心性能优化审查报告 02：修复后测试、文档与进度一致性

> 审查日期：2026-07-29
>
> 审查范围：`310b2f76..486ea03d`
>
> 结论：P0 / P1 为 0；发现 2 个 P2，先记录，再修复。

## 1. 首轮问题复核

- 切库前已取消并等待普通洞察 in-flight，随后清热点、Rate Limit 和 endpoint failure 状态。
- Metrics 网络 await 返回后增加取消检查，旧请求不会继续解码为可写缓存结果。
- 页面加载身份已包含 `repoID + databaseScopeRevision`，切库会让旧 generation 失效。
- 手动刷新冷却已改为 `repoID + target`，并在数据库作用域变化时清空。
- 已补主动清热点、清退避、取消 in-flight、跨仓库冷却和切库冷却测试。

## 2. 代码与文档一致性

- 设计文档中的正常冷加载请求数为 5：GraphQL Bundle 1、Commit 1、Contributors 1、Community 1、Security 1，与代码路径一致。
- GraphQL 失败仍回退原 6 次 REST Search，不改变可用性。
- TTL、ETag / 304、负缓存、指数退避、认证隔离和 Star History 非目标均与实现一致。
- 本轮未修改 String Catalog；`jq empty` 和目录格式检查通过。
- `git diff --check`、提交消息范围和 `build-for-testing` 通过。
- 未运行会启动 Starcat test host 的定向 Suite；遵守 dong4j 之前“不启动应用，由本人验证”的边界。新增测试已完成编译，但不能写成“已执行通过”。

## 3. 发现的问题

### P2：缺少完整冷加载请求预算测试

现有测试证明 Activity 与 Recent 共用 1 次 GraphQL，也分别覆盖其它 endpoint，但尚未用同一个真实 Metrics Client 并发加载六类远端数据，直接断言正常路径总请求数为 5。应增加请求预算测试，并确认六类缓存均写入。

### P2：Checklist 尚未按证据回填

Checklist 仍显示 `0 / 63`，与代码、测试和文档提交不一致。应在请求预算测试完成后逐项回填；定向 Suite 和四项人工验收继续保持未勾选，并注明边界。

## 4. 修复要求

1. 增加完整冷加载请求预算与六类缓存落库测试。
2. 回填 Checklist，区分“代码已实现”“测试已编译”“测试已执行”和“人工验收”。
3. 修复后连续进行两轮无新增 P0 / P1 / P2 的最终审查，再写结果报告。
