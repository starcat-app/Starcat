# Agent / RAG 工作台 i18n 审查报告

> 时间: 2026-07-07 19:41
> 范围: Agent 工作台、RAG 工作台、独立 workspace window 标题、Agent 定义与运行态用户可见固定文案、RAG 静态示例内容。

## 1. 文档一致性

- `checklist.md` 已回填全部实施项和验收项。
- `docs/功能实现总览.md` 已补充变更日志和功能条目。
- 本报告记录本轮审查结论。

## 2. 代码一致性

- Agent/RAG 固定 UI 文案已改为 `Text("key")` 或 `String.l10n("key")`。
- Agent 定义、窗口标题、状态、artifact 类型、运行计划、步骤说明、导出面板标题已接入 i18n。
- RAG 示例会话、默认问答、引用来源和示例引用内容已接入 i18n。
- `RAGDemoData` 从 `static let` 改为 `static var`,避免首次访问后缓存旧语言。

## 3. 验证

- `jq empty Starcat/Resources/Localizable.xcstrings`: 通过。
- `git diff --check`: 通过。
- `rg "String\\(localized:" --type swift Starcat/` / `rg "NSLocalizedString" --type swift Starcat/`: 只命中既有注释。
- `rtk xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' -only-testing:StarcatTests/AgentRuntimeTests -only-testing:StarcatTests/AgentWorkspaceViewModelTests test`: 通过。
- `rtk xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' build`: 通过。

## 4. 剩余说明

- `Starcat/Features/Agents/WeeklyReport/GitHubWeeklyReportTools.swift` 中仍有中文周刊生成模板和工具输出片段。这些属于 Agent 产物内容,不是工作台 UI 壳层文案；本专项按既定范围保留,避免 App 语言设置隐式改变用户生成内容。
- 工作区存在其他未提交脏文件,不属于本专项,本轮提交未纳入。
