# Agent / RAG 工作台 i18n 结果报告

> 完成时间: 2026-07-07 19:41
> 分支: `feature/agent`

## 1. 完成内容

- RAG 工作台 UI 壳层、窗口标题、composer、引用 inspector 和静态示例内容接入 en / zh-Hans。
- Agent 工作台 UI 壳层、窗口标题、状态、审计视图、导出面板、内置 Agent 定义和运行态计划/步骤文案接入 en / zh-Hans。
- 新增字符串 key 写入 `Starcat/Resources/Localizable.xcstrings`,并通过 JSON 校验。
- `AgentRuntimeTests` 中依赖中文标题的断言改为依赖 i18n key。
- 专项 checklist、审查报告、主进度索引已回填。

## 2. 交付边界

- 本次只处理工作台固定 UI 文案和用户可见固定模型文案。
- 用户输入、仓库 metadata、README、LLM 生成正文、Weekly Agent 周刊模板不随 UI 语言自动翻译。

## 3. 验证结果

- `jq empty Starcat/Resources/Localizable.xcstrings`: 通过。
- `git diff --check`: 通过。
- `rg "String\\(localized:" --type swift Starcat/` 与 `rg "NSLocalizedString" --type swift Starcat/`: 未引入新违规。
- `rtk xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' -only-testing:StarcatTests/AgentRuntimeTests -only-testing:StarcatTests/AgentWorkspaceViewModelTests test`: 通过。
- `rtk xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' build`: 通过。

## 4. 提交记录

- `d760355b 新增 Agent RAG 工作台 i18n 专项清单`
- `a91bb90f 接入 RAG 工作台界面国际化`
- `6ede97cc 接入 Agent 工作台界面国际化`
- `5dcc0010 接入 Agent 模型运行文案国际化`
- `69aee376 接入 RAG 示例内容国际化`
