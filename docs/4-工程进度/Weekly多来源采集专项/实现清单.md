# Weekly 多来源采集专项实现清单

> 状态：功能实现与基础验证完成，进入多轮审查
> 单一方案：`docs/2-产品/需求讨论/正式方案/Weekly多来源扩展与AI情报采集正式方案.md`
> 涉及仓库：Starcat `dev`、`supports/starcat-weekly-api` `dev`
> 提交约束：每完成一个可独立验收的小功能立即提交；提交信息使用中文；禁止 push。

## 一、weekly-api 通用来源底座

- [x] 增加可追加的 schema migration 框架和 schema version 记录，不修改或删除线上既有表。
- [x] 创建并初始化固定 `source_catalog`，首期包含 `weekly / zread / discovery / hellogithub / ai_intelligence`。
- [x] 创建 `repo_source_events / ingest_batches / ingest_items / weekly_pins` 与必要索引、约束。
- [x] 把既有三源历史事实幂等迁移到 `repo_source_events`，并从通用事件重建聚合缓存字段。
- [x] 将列表、详情、语言 facet 和来源过滤切换到通用来源事件查询。
- [x] 将 bulk 升级为 schema v2，返回动态来源目录、`source_entries` 和置顶字段，并保留一个兼容窗口。

## 二、异步入队、Worker 与管理接口

- [x] 实现统一批量入队服务：事务只写 batch/items，commit 后非阻塞唤醒 Worker。
- [x] 实现 Worker 启动扫描、主动唤醒、15 分钟兜底扫描和持续 drain。
- [x] 实现 30 分钟租约、过期租约恢复、GitHub 网络请求事务外执行。
- [x] 实现永久失败剔除、瞬时失败 15/30 分钟退避、最多 3 次尝试和批次终态汇总。
- [x] 实现 `GET /internal/sources`、来源同步和批次状态管理接口，并统一使用 admin key。
- [x] 实现 `POST /internal/imports` 批量录入、幂等重放、批内去重与状态查询。
- [x] 实现仓库搜索和有序多项目置顶原子替换接口，置顶变化主动失效 bulk cache。

## 三、既有采集源与 HelloGitHub

- [x] 将阮一峰周刊采集改为统一入队，不再同步调用 GitHub enrich。
- [x] 将 ZRead 采集改为统一入队，不再写专属来源事实。
- [x] 将 Show HN 采集改为统一入队；保留其分类流水线所需能力但不再双写来源事实。
- [x] 实现 HelloGitHub featured API 增量采集、结构校验、分页和幂等键。
- [x] 实现 HelloGitHub periodical volume 解析、最新期号探测和历史回填。
- [x] 实现持久化回填 cursor、逐期恢复、失败可见和增量/月刊调度。

## 四、AI 情报采集 Skill

- [x] 用 `skill-creator` 初始化 `.claude/skills/starcat-weekly-import`。
- [x] 编写中文 `SKILL.md`，限定解析、搜索、核验、确认、固定来源和整批提交工作流。
- [x] 编写 API reference 与 `submit_import.py`，支持提交、幂等键和批次状态查询。
- [x] 运行脚本离线测试与 `quick_validate.py`，确认不记录密钥且拒绝非法输入。

## 五、Starcat Weekly 客户端

- [x] 把固定来源 enum/filter 重构为 raw-value 来源模型和动态来源 descriptor。
- [x] bulk v1/v2 均可解码；未知来源可缓存、筛选和展示，不导致整批失败。
- [x] 追加下一可用 GRDB migration，保存来源目录、通用来源条目和置顶字段。
- [x] WeeklyBulkRepository 的来源/语言/覆盖/状态过滤使用新模型。
- [x] 本地与远端排序统一为：筛选 → 置顶 → pin position → 用户排序 → repo id。
- [x] Weekly filter popover 动态展示来源与 count，详情改用通用来源事件。
- [x] 增加 HelloGitHub、AI 情报的本地图标 resolver、置顶标记和中英文 i18n。
- [x] 补齐 DTO、迁移、缓存、筛选、排序、详情和未知来源单元测试。

## 六、`pages/_local-admin`

- [x] 扩展 Weekly 通用 API Console endpoint。
- [x] 增加来源状态、队列统计和最近成功/失败展示。
- [x] 增加 HelloGitHub 增量/历史回填表单、二次确认和批次轮询。
- [x] 增加仓库搜索、加入、上移、下移、移除、清空和原子保存置顶列表。
- [x] 验证未配置 admin key、刷新恢复进度和保存后预览顺序。

## 七、文档、测试与工程进度

- [x] 更新 weekly-api README / README-ZH、环境变量和 API 契约。
- [x] 更新 Weekly 详细设计、最终方案中的实现状态和相关历史文档指针。
- [x] 更新 `docs/功能实现总览.md` checkbox、实现说明、仪表盘和变更日志。
- [x] weekly-api 通过 `go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./...`。
- [x] Starcat 完整 Swift 单测通过，并已运行 `xcodegen generate`。
- [x] local-admin 完成静态检查与真实浏览器/mock API 交互验收。
- [x] 完成 i18n、数据库 migration、API 鉴权、缓存失效和隐私边界专项核对。

## 八、多轮审查与收口

- [x] 第一轮完整性审查：先新增审查报告，再修复代码、测试、文档和 checklist 问题并提交。
- [ ] 第二轮一致性审查：先新增审查报告，再修复跨仓库契约、工程进度和遗漏并提交。
- [ ] 第三轮最终审查：先新增审查报告；确认无遗留问题，或修复后重新审查。
- [ ] 全部功能与审查项勾选后新增结果报告。
- [ ] 最终确认两个仓库工作区干净、提交均为中文且没有 push。
