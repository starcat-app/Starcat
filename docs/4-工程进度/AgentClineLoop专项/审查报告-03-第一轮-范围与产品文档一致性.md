# 审查报告 03:第一轮范围与产品文档一致性

> 审查时间: 2026-07-11
> 审查范围:总 checklist、Cline 分析、Agent 产品技术方案、不做范围、当前分支改动
> 审查结论:实现范围正确,发现 3 项文档漂移,修复后进入第二轮

## 1. 已核对证据

- `checklist.md` 明确排除 shell、文件编辑、浏览器自动化、subagent、自动写用户数据和第二套 Provider 设置。
- 当前代码只开放 Starcat 领域工具,没有命令执行、文件编辑或任意网络浏览器工具。
- `LoopAgentRuntime` 已落实模型选工具、tool-result 回灌、Approval、消息事实源和预算边界。
- `ExternalSearchAgentTool` 复用 `AppSettings`、`ExternalSearchContextProvider`、现有 API Key、缓存和隐私设置。
- Weekly 与 Repo Insight 是当前仅启用的正式 Agent,其余定义保持预览禁用。

## 2. 发现项

### P1-1 Agent 底层平台技术方案仍描述旧基线

`docs/2-产品/需求讨论/agent/16-Agent底层平台技术方案.md` 仍标记“等拍板”,并写覆盖主窗口、step/artifact 持久化以及 `AIClientProtocol` 不支持 tool calling。当前实现已改为独立 workspace window、message/approval/artifact 事实表和 OpenAI-compatible tool-calling adapter。

修复:在文档顶部增加当前落地状态和实现基线,明确后续正文是 2026-06 原始方案,冲突时以当前基线与专项 checklist 为准。

### P1-2 Weekly 技术方案仍把旧固定工具与多产物设想当作当前实现

`docs/2-产品/需求讨论/agent/17-GitHubWeeklyReportAgent技术实现方案.md` 顶部仍写 read-only tools + 最终 Markdown 生成,正文列出 Trending/Weekly 固定工具、小红书/HTML/视频等非本次范围,且没有描述模型驱动 loop、External Search 和结构化提交工具。

修复:增加当前正式工具 allowlist、真实上下文、单 Markdown artifact、Repo Insight 复用及未实现范围说明;保留原始产品设想作为历史提案。

### P1-3 Cline 落地分析仍保留旧 Runtime fallback 建议

`Cline-Agent实现分析与Starcat落地方案.md` 第 5.4 节建议保留 `LinearAgentRuntime` fallback/facade,与本项目“未上线,不保留旧 Runtime 兼容路径”的硬约束及当前删除线性实现的事实不一致。

修复:增加落地决策说明,明确已直接替换旧 Runtime,并记录工具命名、权限分类和 artifact 提交的最终取舍。

## 3. 无问题项

- 没有把 Cline 的 Coding Agent 高权限工具带入 Starcat。
- 没有新增第二套 AI 或 External Search 设置。
- 没有把未启用预览 Agent 宣称为已交付。
- Approval 作为底层能力存在,但当前正式只读 Agent 不制造演示审批。

## 4. 修复顺序

1. 修正 Cline 分析中的最终落地决策。
2. 修正 Agent 底层平台文档当前状态。
3. 修正 Weekly 技术方案当前实现边界。
