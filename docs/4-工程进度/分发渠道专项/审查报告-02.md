# 分发渠道专项审查报告 02

> 审查时间：2026-07-04
> 审查范围：App Store / Direct target、Sparkle、打包脚本、分发文档与 checklist
> 结论：未发现新的阻塞问题。

## 1. 审查结论

- checklist：`docs/4-工程进度/分发渠道专项/checklist.md` 未发现未勾选项。
- App Store / Direct 分离：App Store 路径保持 StoreKit-only，Direct 路径包含 Sparkle 和 License 设置入口。
- 发版检查清单：已把 Sparkle / Direct 授权从“后续接入”更新为“首期已接入，正式发版前需配置真实环境”。
- 打包脚本：脚本语法检查通过。

## 2. 验证记录

- `bash -n scripts/package-appstore.sh scripts/package-direct.sh`：通过。
- `rtk xcodebuild -quiet -scheme Starcat ... test`：通过相关测试并完成 App Store target 构建。
- `rtk xcodebuild -quiet -scheme StarcatDirect -destination 'platform=macOS,arch=arm64' -derivedDataPath build/Review-Direct build`：通过。

## 3. 备注

- Direct 构建输出仍包含既有 `StatusBarController.popUpMenu` deprecation warning。
- Direct 正式发版前还需要配置真实 Sparkle EdDSA public/private key、Developer ID 签名、公证、appcast 部署和下载 URL。
