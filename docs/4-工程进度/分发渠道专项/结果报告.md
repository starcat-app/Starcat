# 分发渠道专项结果报告

> 完成时间：2026-07-04
> 结果：首期实现完成，专项 checklist 已全部勾选。

## 1. 已完成范围

- 新增 `StarcatDirect` target / scheme，与 App Store `Starcat` target 分离。
- App Store target 保持 StoreKit-only，不依赖 Sparkle。
- Direct target 集成 Sparkle 2，并配置 `SUFeedURL`、`SUPublicEDKey` 和 sandbox mach lookup entitlement。
- 新增 App Store / Direct 打包脚本和教程。
- Direct 打包脚本支持本地 DMG、SHA256、notarization、appcast 生成入口。

## 2. 审查结论

- 第一轮：发现并修复发版检查清单中“Sparkle / Direct 授权后续接入”的旧状态。
- 第二轮：未发现阻塞问题，脚本、相关 Swift 测试和 Direct 构建通过。
- 第三轮：产物级抽检确认 Sparkle 只出现在 Direct 构建中。

## 3. 验证记录

- `bash -n scripts/package-appstore.sh scripts/package-direct.sh`：通过。
- `rtk xcodebuild -quiet -scheme Starcat ... test`：相关测试通过。
- `rtk xcodebuild -quiet -scheme StarcatDirect -destination 'platform=macOS,arch=arm64' -derivedDataPath build/Review-Direct build`：通过。
- 产物抽检：App Store 构建未命中 `SUFeedURL` / `SUPublicEDKey`，未发现 `Sparkle.framework`。
- 产物抽检：Direct 构建命中 `SUFeedURL` / `SUPublicEDKey`，包含 `Sparkle.framework`。

## 4. 剩余上线配置

- 配置真实 Sparkle EdDSA key，并将公钥写入 `Configs/Secrets.xcconfig`。
- 使用 Developer ID 证书签名并完成 notarytool 公证。
- 生成真实 appcast，部署到 `https://starcat.ink/appcast.xml`。
- 将 DMG 下载文件部署到官网稳定 URL，并用 Sparkle 菜单入口做真实更新验证。
