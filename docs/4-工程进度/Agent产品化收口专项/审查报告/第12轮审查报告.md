# Agent 产品化收口专项第 12 轮审查报告

- 审查时间：2026-08-15 14:22 CST
- 审查范围：长工具链完成后的 Agent Run Surface 卡死、Provider 流式事件链、滚动跟随、相关测试与专项文档
- 审查结论：发现 1 个高严重度布局风暴和 3 个中严重度实现 / 文档问题，均已修复；定向与全量回归通过

## 1. 检查项目

- 文档审查：核对权威设计、专项 checklist、完成结果报告与当前流式实现。
- 代码审查：检查 `LoopAgentRuntime` 原始 delta 转发、`AgentWorkspaceViewModel` MainActor 消费、Run Surface 滚动和 SwiftUI 布局更新链。
- 测试审查：复核 Runtime、Workspace、尾部跟随状态机和生产视图渲染覆盖。
- 工程进度审查：确认本轮只修复卡死与同步专项文档，不改写 `docs/功能实现总览.md`，不扩大到分发门禁。

## 2. 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R12-01 | `AgentMessageTimelineView` 在正文、状态、审批或 Artifact 变化时同步执行 `scrollTo`；长工具链完成突发刷新会反复触发 `ScrollView + LazyVStack` 测量，Hang Report 的主线程持续停留在 `GraphHost.flushTransactions → AttributeGraph`。 | 高 | 删除复合 revision 和高频同步 `scrollTo`，复用 `ScrollTailController`、底部 sentinel、`ScrollPhase` 与 `sizeChanges` anchor。 | 已修复 |
| R12-02 | Provider 每个原始正文 / reasoning delta 都先进入 `AgentWorkspaceViewModel` 的 MainActor；展示层节流只能减少 Observable 发布，不能减少主线程需要消费的原始事件。 | 中 | 在 `LoopAgentRuntime` 增加 100 ms 入站合并，完成时 flush，保持完整文本与消息事实不变。 | 已修复 |
| R12-03 | 既有文档仍描述“直接定位”尾部，并把流式保护只归因于展示层节流，与修复后的两级合并和滚动状态机不一致。 | 中 | 同步权威设计、专项 checklist 与完成结果报告。 | 已修复 |
| R12-04 | 首版入站合并只在 Provider 正常完成时 flush；Provider 抛错或任务取消会漏掉不足一个时间窗的尾部展示增量。 | 中 | 在错误转交上层前 flush pending delta，并新增失败流集成回归。 | 已修复 |

## 3. 修复验证

- `LoopAgentRuntimeTests`：新增确定性批处理单测和 400 个 Provider delta 的 Runtime 集成回归，正文完整且 UI 事件数量保持有界。
- `AgentWorkspaceViewModelTests`：既有长流式输出、展示节流和生产 `NSHostingView` 渲染测试通过。
- `ScrollTailControllerTests`：用户接管、离开底部、恢复跟随和重复状态写入保护通过。
- 全量 `StarcatTests`：2,291 项，0 failed、8 skipped、1 expected failure；结果 `Test-Starcat-2026.08.15_14-30-41-+0800.xcresult`。
- 本轮未修改数据库 schema、Agent 消息协议、RAG 产品逻辑、国际化目录或 `docs/功能实现总览.md`。

## 4. 遗留问题

- 真实 Provider 的长 Weekly Run 需要在用户现有模型配置和真实窗口中复验；自动化已覆盖同等 400-delta 突发事件链，但不能替代实际窗口手感验收。
- App Store / Direct 真实分发产物验收仍等待发版授权，不属于本轮卡死修复。
