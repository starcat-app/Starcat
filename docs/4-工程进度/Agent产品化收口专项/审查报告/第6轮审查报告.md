# Agent 产品化收口第 6 轮审查报告

- 审查时间：2026-08-14 09:49 CST
- 审查范围：Mac 解锁后的 Direct Debug 真实界面、真实 Provider、Weekly 工作流收敛性、历史 Run 恢复状态
- 审查基线：`e09675d`

## 1. 检查项目

### 文档审查

- 权威设计要求 GitHub Weekly Report 先基于冻结热点上下文获取事实，再且仅调用一次 `artifact_build_weekly_report` 形成最终产出物。
- 专项 checklist 要求真实 Provider Run、工具链、Artifact 和 History 在真实数据环境闭环，不能用 Stub 或编译成功代替。

### 代码审查

- `GitHubWeeklyReportAgentDefinition` 已通过 Prompt Rule 要求模型提交最终 Artifact，但 Runtime 仍将全部读取工具持续暴露到迭代上限。
- `AgentToolDefinition.completesRun` 已能标识完成型工具，可作为通用收敛边界，不需要按 Agent ID 写特例。
- `LoopAgentRuntime` 当前没有在剩余迭代不足时缩小工具集合，也没有给模型明确的最终提交回合。

### 测试审查

- 既有测试覆盖工具执行、审批、必需 Artifact 缺失和迭代上限，但未覆盖“模型持续读取直到耗尽预算”的真实 Provider 行为。

### 工程进度审查

- 最新 NoSandbox Direct Debug 使用当前 `dev` 代码构建并启动。
- 历史库中两个进程中断的 `running` Run 已在真实 UI 中恢复为失败；数据库中 `planning/running` 数量为 0，证明中断恢复修复真实生效。
- 真实 Provider Run `02332234-8DA3-44F2-8E41-2C8165E49C7D` 共记录 43 条消息，但没有 Artifact，最终以“Agent 已达到 12 轮迭代上限。”失败。

## 2. 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R6-01 | Weekly 模型反复调用 `context_resolve_repos`、`repo_cluster_topics`、`knowledge_search`，没有进入最终 Artifact 提交，耗尽 12 轮上限。 | 高 | 在 Runtime 增加基于 `completesRun` 的通用最终提交回合：最后一次模型机会仅暴露完成型工具并使用 `toolChoice=required`；自动化通过后重跑真实 Weekly。 | 已关闭：真实 Run 生成 17,002 字符 Artifact |
| R6-02 | 第 5 轮将真实 Provider / Artifact 门禁保留为待验收是正确的；本轮证明它确实能发现 Stub 测试未覆盖的问题。 | 无 | 保留门禁，修复后必须使用同一真实环境复验。 | 已关闭 |
| R6-03 | 真实完成 Run 和历史 Run 在快照刷新时把 `user_prompt` 回填进 Composer，导致已发送消息重新出现在输入框；现有历史测试还把错误行为写成预期。 | 高 | 分离“当前 Run 展示问题”和“可编辑 Composer 草稿”；快照只恢复前者，Composer 继续保持空白或用户未发送草稿，并修正 ViewModel 测试。 | 已关闭：发送后与历史切换均保持空白 |
| R6-04 | Repo Alternatives 连续执行多次 `external_search` 时，Runtime 每次都覆盖 `externalContextMarkdown`，Artifact 只能校验最后一批证据；真实 Run 已取得多批 GitHub 来源却连续两次被判定“候选不在证据中”，最终又因过度重试触发 Provider HTTP 400。 | 高 | Runtime 按执行顺序累积所有成功 External Search markdown，恢复历史 Run 时使用同一合并规则；新增多次搜索共同支撑一个 Artifact 的回归测试，再重跑真实 Provider。 | 已关闭：真实 Run 使用多批证据生成 Artifact |
| R6-05 | 修复 R6-04 后，Repo Alternatives 把累计的所有搜索标题、摘要和正文原样写入 Markdown 来源区，真实 Artifact 膨胀到 64,021 字符；虽然审计完整，但最终交付物噪声过大、渲染和导出成本失控。 | 中 | 继续用完整累计上下文做候选准入校验，但最终 Markdown 只列已验证候选的根 GitHub URL；原始 Tool Result 继续留在消息事实和过程详情，不复制进交付物。 | 自动化已修复，待真实复验 |

## 3. 本轮结论

真实 UI 已证明进程中断恢复逻辑有效，真实 Weekly Provider Run 暴露的 P0 收敛缺口已完成通用 Runtime 修复。`AgentLoopModelClientTests` 与 `LoopAgentRuntimeTests` 共 32 项通过、0 failed；同一真实 Provider 随后已生成 1 个 17,002 字符 Markdown Artifact。Composer 快照回填缺陷 R6-03 已完成自动化与真实 UI 复验。成功联网的 Repo Alternatives 又暴露了多批 External Search 证据被覆盖的 R6-04，代码与回归测试已修复，等待重建后的真实 Provider 复验。
