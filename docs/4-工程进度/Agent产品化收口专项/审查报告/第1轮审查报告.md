# Agent 产品化收口第 1 轮审查报告

- 审查时间：2026-08-14 02:33 CST
- 审查范围：Agent P0～P4 需求与权威设计、失败重试、统一能力层、正式入口门禁、Run Surface、专项 checklist、定向测试
- 审查基线：`8f064c25`

## 1. 检查项目

### 文档审查

- 对照 `57-Agent工作台与统一能力层详细设计.md` 的 P0～P4、P4-A 与 Definition of Done。
- 对照专项 checklist 检查已实现能力、非目标、测试证据和待收口项。
- 检查 `docs/功能实现总览.md` 的 Agent 历史描述、checkbox、仪表盘和变更日志。

### 代码审查

- 检查失败 Run 同 ID 重试、写工具不重放与终态原子恢复。
- 检查 Agent / MCP 共享 Tag、Metadata、Repository Read 能力装配。
- 检查 Run Surface 的 call/result 合并、Activity Group、折叠、Markdown、reasoning 与 Inspector 边界。
- 检查 Pro、模型、用量、隐私与双渠道入口边界。

### 测试审查

- 复跑 24 个 Agent / Capability / MCP 定向测试套件。
- 复跑 `AgentTimelineProjectionTests`，检查过程 / 结果分层新增覆盖。
- 检查尚未执行的全量测试、Debug build 和真实外部服务验收门禁。

### 工程进度审查

- 检查每个独立功能是否已有中文本地 commit 且未 push。
- 检查专项 checklist 与权威设计是否同步。
- 检查功能实现总览是否仍有历史描述漂移。

## 2. 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R1-01 | Run Surface 修订键未包含 Run status、approval status 与 error；失败终态新增错误行时自动跟随逻辑不会触发，且当前滚动函数仅允许 `isRunning`，终态即使仍在底部也不会定位新错误。 | 中 | 把状态、审批和错误摘要纳入确定性修订键；只以“用户是否仍在底部”决定跟随，终态不再被 `isRunning` 拦截。 | 已修复 |
| R1-02 | Activity Group 点击只切换展开状态，旧时间线中“同时选中 Knowledge Audit 并在 Inspector 展示”的行为丢失。 | 中 | 展开 Activity 时选中组内首个带 Knowledge Audit 的工具调用；无审计活动不改变 Inspector。 | 已修复 |
| R1-03 | `docs/功能实现总览.md` 仍把 Agent 描述为 deterministic runtime、仅 en/zh-Hans、写入确认待后续，并未登记本轮 P0～P4 收口。 | 中 | 在代码修复后同步 Agent 条目、`> 实现：`、P2/总计仪表盘与变更日志。 | 已修复 |
| R1-04 | 全量 StarcatTests、最终 Debug build、真实 Provider / External Search / GitHub / 私仓与 App Store / Direct 人工验收尚未形成最终证据。 | 高（验收门禁） | 后续轮次执行全量自动化；无法从当前环境观察的真实外部证据明确保持为人工门禁，不以静态检查冒充通过。 | 进行中 |

## 3. 本轮结论

当前实现架构与权威方案一致，未发现数据库迁移、权限绕过、写操作重放或 Agent/RAG 重新耦合问题；定向测试通过。修复 R1-01～R1-03 后进入第 2 轮审查，R1-04 在最终验收阶段关闭或如实登记为外部人工门禁。
