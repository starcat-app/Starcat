# Agent 产品化收口第 2 轮审查报告

- 审查时间：2026-08-14 02:38 CST
- 审查范围：第 1 轮修复、权威设计与总览一致性、Agent 测试覆盖、StarcatTests 全量结果、Debug / Release 构建
- 审查基线：`b7ba314`

## 1. 检查项目

### 文档审查

- 复核权威设计当前结论、实现基线、P4-A、Release Gate 与 Definition of Done。
- 复核功能实现总览的 Agent 条目、P2 / 总计仪表盘、变更日志与 `> 实现：`。
- 复核专项 checklist 和第 1 轮报告修复状态。

### 代码审查

- 复核终态跟随、用户上滚保护、Run 切换、Activity / Inspector 联动。
- 静态检查普通 Agent UI 是否仍展示 raw reasoning。
- 检查 Markdown Artifact 与 log / Inspector 的单一主阅读边界。
- 检查 `DebugFlags.agentToolbarEntry` 是否完全删除，Release 入口是否仍经过统一门禁。

### 测试审查

- 全量结果：`totalTestCount=2280`，`passedTests=2271`，`skippedTests=8`，`expectedFailures=1`，`failedTests=0`。
- Debug build 通过；上一阶段 Release build 已通过。
- 检查 Agent 投影测试对完成、失败、审批、取消、日志 Artifact 与 Activity 边界的覆盖。

### 工程进度审查

- 检查 7 个本地中文 commit，确认未 push。
- 对照专项结束条件核对尚未关闭的真实外部人工门禁与多轮审查。

## 2. 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R2-01 | 投影测试已覆盖完成态折叠、call/result 合并和 Markdown/log 分流，但没有直接断言失败/审批态默认展开、Approval 作为 Activity Group 强边界。 | 中 | 增加失败、等待确认、取消状态默认策略与 Approval 边界测试。 | 已修复 |
| R2-02 | 权威设计结论第 7 条仍写“当前收口重点转为 Workflow、上下文、eligible 子集与国际化”，与 P0～P4 已完成的当前状态冲突。 | 低 | 改为已完成事实，并将当前阶段明确为测试、人工门禁与终审。 | 已修复 |
| R2-03 | 全量测试的 `DiagnosticsTests.swift` 产生 4 条“Publishing changes from background threads” runtime warning。该 warning 在非 Agent 诊断测试中，当前 0 failed。 | 低（既存、非本专项） | 在报告和最终结果保留；不借 Agent 专项修改 Diagnostics 相邻模块。 | 不在本轮修复 |
| R2-04 | 当前机器没有已配置对话模型，真实 UI 已验证入口、Pro 付费墙和模型配置门禁，但无法执行真实 Provider、External Search、GitHub 与私仓端到端 Run；App Store / Direct 也未执行打包。 | 高（外部人工门禁） | 保持人工验收项开放并写明所需前置条件；不伪造凭据，不执行未授权打包/发布。 | 等待人工环境 |

## 3. 本轮结论

全量自动化与双配置编译没有发现 Agent 回归。R2-01、R2-02 可在仓库内立即关闭；R2-03 是明确的既存非 Agent warning，R2-04 是需要真实凭据和分发产物的人工门禁，均不得伪装为本地自动化通过。
