# AI 个人笔记审查报告 21：耗时列顺序与等待确认语义

> 审查时间: 2026-07-22 16:09 CST
> 审查基线: `76c797e`
> 审查范围: 步骤行布局、计时状态机、单元测试、构建与专项文档一致性
> 结论: 代码与自动化通过；发现 4 项文档漂移，待本轮修复

## 1. 代码与交互核对

| 需求 | 实现证据 | 结论 |
|---|---|---|
| 耗时放在状态之前 | `stepRow` 行尾顺序为 `stepDuration` → 状态 `Text` | 通过 |
| 状态列保持右对齐 | 状态作为行尾最后一列，继续使用 44pt 最小宽度 | 通过 |
| 等待确认不显示耗时 | UI 使用透明固定列宽，不渲染数值或 `—` | 通过 |
| 等待确认不参与计时 | `shouldTrackDuration` 排除 `.awaitingConfirmation`，进入该阶段主动清理时间数据 | 通过 |

## 2. 状态机与回归证据

- 七步状态顺序、流式草稿、取消、保存失败和 repo 会话所有权没有改动；
- 可控时钟单测已改为断言“等待确认”始终没有起点或耗时；
- 联合选择集 41 / 41 通过，0 失败、0 跳过、0 预期失败；
- `StarcatDirect` Debug 构建通过；
- `Localizable.xcstrings` JSON、Notes 静态规范与 `git diff --check` 通过。

## 3. 审查发现

### P1-21-01：详细设计仍把等待确认视为可计时步骤

`5.7 步骤耗时` 仍写“等确认耗时包含用户等待时间”和保存失败后重新起计，与当前“用户停留不是系统耗时”的产品语义相反。

### P1-21-02：详细设计的列顺序与 UI 相反

UI 章节仍写“状态文字右侧预留耗时列”，应修正为行尾按“耗时 → 状态”排列，并明确等待确认只保留对齐宽度。

### P2-21-03：正式方案与验收步骤缺少新例外

正式方案仍表述为每行固定显示耗时，验收步骤也没有要求核对耗时在状态之前、等待确认无耗时。

### P2-21-04：Checklist 与最终结果报告仍是上一轮收口基线

Checklist 仍写“每个已执行步骤记录耗时”并停留在 20 轮审查，最终结果报告也没有记录本次布局顺序与等待确认例外。

## 4. 修复计划

1. 同步正式方案和详细设计的耗时列顺序与等待确认语义；
2. 补充人工验收步骤，覆盖“耗时 → 状态”与等待确认无耗时；
3. 更新 Checklist 和最终结果报告，记录 41 / 41 最新回归与 `76c797e`；
4. 修复后再新增一轮最终一致性复审。

## 5. 保护边界

- 受保护的 `docs/功能实现总览.md` 仅只读核对，未修改；
- 工作树中 `CHANGELOG.md` 与 `CHANGELOG-ZH.md` 的既有修改没有纳入本专项；
- 未 push，未执行打包、发布或上传脚本。

## 6. 本轮结论

本次布局与计时语义修复已通过代码、单元测试和构建核验，未发现功能回归。当前剩余 4 项文档漂移，按“先记录审查、再修复、修复后终审”流程继续收口。
