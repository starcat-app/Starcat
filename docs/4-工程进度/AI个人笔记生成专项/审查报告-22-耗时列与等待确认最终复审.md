# AI 个人笔记审查报告 22：耗时列与等待确认最终复审

> 审查时间: 2026-07-22 16:14 CST
> 审查基线: `5b34c0a`
> 审查范围: 需求、详细设计、代码、单元测试、构建、验收、结果文档、Checklist 与提交边界
> 结论: 通过，报告 21 的 4 项文档漂移已关闭，0 个未关闭问题

## 1. 报告 21 发现项关闭证据

| 发现项 | 修复证据 | 状态 |
|---|---|---|
| P1-21-01 等待确认仍被设计为可计时步骤 | 详细设计已明确 `shouldTrackDuration` 排除 `.awaitingConfirmation`，保存失败回到该阶段也不起计 | 已关闭 |
| P1-21-02 UI 列顺序文档与实现相反 | 详细设计已改为“耗时 → 状态”，状态是最后一列并右对齐 | 已关闭 |
| P2-21-03 正式方案与验收步骤缺少例外 | 两份文档均已覆盖列顺序、等待确认无耗时与对齐要求 | 已关闭 |
| P2-21-04 Checklist 与结果报告停留在旧基线 | Checklist 新增第 11 节，三份结果报告已记录当前行为、41 / 41、`76c797e` 与报告 21 | 已关闭 |

## 2. 代码与 UI 终审

- `stepRow` 的行尾顺序为耗时 View、状态 Text，与用户确认的视觉顺序一致；
- 状态列作为最后一列，使用 44pt 最小宽度右对齐；
- 等待确认行使用 54pt 透明占位，不显示数值或 `—`，也不进入辅助功能树；
- ViewModel 在读取耗时和转移状态时都排除 `.awaitingConfirmation`，没有隐藏的用户等待计时；
- 可计时步骤的起计、冻结、取消、放弃重置和 repo 会话保留语义没有改变。

## 3. 测试、构建与静态证据

- `RepoDetailScaffoldTests`、`RepoNoteAIGenerationViewModelTests`、`RepoNoteAIServiceTests` 和 `RepoNotesSectionViewModelTests` 联合选择集 41 / 41 通过；
- 0 失败、0 跳过、0 预期失败；
- `StarcatDirect` Debug 构建通过；
- `Localizable.xcstrings` JSON 解析通过，Notes 范围未命中 `.tertiary`、`String(localized:)` 或 `NSLocalizedString`；
- `git diff --check` 通过；
- `76c797e` 之后只有报告 21 和专项文档变更，没有使代码回归证据失效。

## 4. 文档与进度终审

- 正式方案、详细设计、验收步骤、结果报告、交互增强结果报告和最终结果报告均已统一当前语义；
- 历史审查报告保留当时证据，现行耗时语义明确以报告 21 / 22 为准；
- Checklist 的实现和自动化项已全部勾选；本报告提交后，可回填唯一剩余的修复后终审项。

## 5. 提交与保护边界

- 代码修复、审查报告和文档同步已分为独立中文小步 commit；
- 受保护的 `docs/功能实现总览.md` 未修改；
- 工作树中 `CHANGELOG.md` 与 `CHANGELOG-ZH.md` 的既有修改保持不变；
- 未 push，未执行打包、发布或上传脚本；
- 本轮未自行截图，人工 UI 验收仍以 dong4j 真实窗口检查为准。

## 6. 最终结论

报告 21 发现的 4 项文档漂移已全部关闭。本轮未发现布局回归、计时旁路、单元测试失败、构建失败、文档遗漏、Checklist 失真或提交越界。当前 0 个未关闭问题，可回填 Checklist 并完成本轮收口。
