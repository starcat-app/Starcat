# AI 个人笔记审查报告 16：模式按钮最终收口

> 审查时间: 2026-07-22 13:55 CST
> 审查基线: `21b5c54`
> 审查范围: 编辑 / 预览右对齐与间距、代码回归、设计与进度一致性
> 结论: 代码与自动化通过，发现 3 项文档漂移，待本轮修复

## 1. 最终实现核对

`RepoNotesSection` 的扩展态模式控件已经收口为：

- 控件由笔记卡片自身的 `topTrailing` overlay 定位，右侧保留 10pt；
- “编辑”和“预览”使用两个独立系统按钮，`HStack(spacing: 6)` 提供明确间距；
- 当前模式使用系统 prominent 状态，另一模式使用系统 bordered 状态；
- 控件使用固有宽度，不再依赖 150pt segmented Picker 外框；
- `inlineEditorMode` 初始值、编辑器收起和 repo 切换仍统一重置为 `.preview`。

dong4j 已在 Direct 真实窗口确认右对齐和按钮间距符合预期。本报告只把这一项标记为人工通过，不扩张到真实 Provider、VoiceOver 或完整验收清单。

## 2. 代码与回归审查

| 检查项 | 证据 | 结论 |
|---|---|---|
| 右对齐基准 | 卡片 `.overlay(alignment: .topTrailing)` | 通过 |
| 按钮间距 | `HStack(spacing: 6)` | 通过 |
| 默认预览 | 初始化、收起、repo 切换均为 `.preview` | 通过 |
| 保存语义 | 编辑 / 预览继续共享 `editingContent` | 通过 |
| UI 规范 | 系统 bordered 样式，未新增 plain Button、硬编码颜色或嵌套 Button | 通过 |
| 其它功能边界 | AI 会话、草稿、Hero viewport、README 与数据库均未修改 | 通过 |

Direct Debug 构建通过。联合选择集覆盖 `RepoDetailScaffoldTests`、`RepoNoteAIGenerationViewModelTests`、`RepoNoteAIServiceTests` 与 `RepoNotesSectionViewModelTests`：39 / 39 通过，0 失败、0 跳过、0 预期失败。

## 3. 审查发现

### P1-16-01：详细设计仍停留在旧模式控件描述

详细设计只写“右对齐 `[编辑 | 预览]`”，没有记录卡片 overlay、两个独立系统按钮、6pt 间距与固有宽度约束，无法解释最终实现为什么不再使用 segmented Picker。

### P1-16-02：Checklist 仍把失效的宽度方案写成完成证据

Checklist 仍写“模式栏显式占满可用宽度并按 trailing 对齐”。该语句对应历史中已被真实截图推翻的实现，当前正确方案是让模式控件直接锚定卡片 overlay，并移除人工 150pt 外框。

### P1-16-03：结果报告与提交记录缺少最终交互修复

现有结果报告尚未记录模式控件从 segmented Picker 收口为两个独立系统按钮、6pt 间距，也缺少 `a3a7768` 至 `21b5c54` 的最终修复链路。历史审查报告 10 / 11 可以保留为当时证据，但结果报告必须明确其结论已被报告 16 更新。

## 4. 修复计划

1. 更新详细设计的原位扩展编辑器布局契约；
2. 修正 Checklist 的右对齐完成证据并增加 6pt 间距条目；
3. 更新交互增强结果报告和总结果报告的最终行为、自动化证据、审查索引与提交记录；
4. 修复后新增一轮终审报告，确认代码、测试、文档和 Checklist 无未关闭问题；
5. 不修改受保护的 `docs/功能实现总览.md`，不带入现有 Changelog 修改，不 push。

## 5. 本轮结论

最终代码行为与 dong4j 已确认的可见结果一致，联合回归无失败；当前阻止专项正式收口的只有 3 项文档漂移。按“报告先提交、再修复”的专项流程，本报告提交后进入文档修复。
