# AI 个人笔记审查报告 23：保存状态标题行布局

> 审查时间: 2026-07-22 16:38 CST
> 审查基线: `97d33f2`
> 审查范围: 用户反馈、标题行布局实现、保存状态语义、自动化验证、设计文档与 Checklist
> 结论: 代码修复与自动化验证通过，发现 3 项文档 / 验收记录漂移待关闭

## 1. 需求与根因

用户要求把“已保存 / 未保存”放在“笔记”标题行的最近编辑时间戳之后。修复前 `SaveIndicator` 已位于标题行，但排列在 `Spacer()` 之后，因此被推到标题行右侧，视觉上没有紧跟时间戳。

本次只交换 `SaveIndicator` 与 `Spacer()` 的顺序，形成“笔记 → 最近编辑时间 → 保存状态 → 弹性空白 → AI 操作”的布局；未修改保存状态逻辑、文字、颜色、图标、按钮样式或 AI 操作入口。

## 2. 代码审查

- `RepoNotesSection.notesDisclosure` 在时间戳之后立即渲染 `SaveIndicator`，随后才使用 `Spacer()`；
- `SaveIndicator` 仍统一覆盖未保存、保存中、已保存和保存失败状态，没有复制状态来源；
- 保存状态是被动视图，不会破坏标题行整行展开 / 折叠命中区；
- 右侧 AI 入口仍是同级 overlay，没有 Button 嵌套；
- 本次 diff 仅 1 行移动，没有无关样式或结构改动。

## 3. 自动化证据

- `RepoDetailScaffoldTests`、`RepoNoteAIGenerationViewModelTests`、`RepoNoteAIServiceTests`、`RepoNotesSectionViewModelTests` 联合选择集 41 / 41 通过；
- 0 失败、0 跳过、0 预期失败；
- `StarcatDirect` Debug 构建通过；
- `git diff --check` 通过；
- 未自行截图，最终视觉间距仍以 dong4j 的真实窗口验收为准。

## 4. 发现项

| 编号 | 级别 | 发现 | 修复要求 | 状态 |
|---|---|---|---|---|
| P2-23-01 | 文档 | 正式方案仍写“标题行右侧只保留 AI 操作入口”，未说明保存状态紧跟时间戳 | 补充标题行左侧信息顺序，并明确右侧独占区域仅保留 AI 操作 | 待修复 |
| P2-23-02 | 文档 | 详细设计的标题行结构未记录 `SaveIndicator` 与 `Spacer()` 的先后约束 | 更新结构图与布局约束，防止再次被推到右侧 | 待修复 |
| P2-23-03 | 进度 | Checklist、验收步骤和结果报告未记录本次位置修正与 41 项回归 | 新增收尾项并同步自动化 / 人工验收边界 | 待修复 |

## 5. 提交与保护边界

- 代码修复已独立提交：`97d33f2 fix(starcat): 将笔记保存状态移到时间戳后`；
- 受保护的 `docs/功能实现总览.md` 未修改；
- `CHANGELOG.md` 与 `CHANGELOG-ZH.md` 的既有修改保持不变；
- 未 push，未执行打包、发布或上传脚本。

## 6. 本轮结论

代码实现已满足指定顺序，自动化验证通过。需先关闭 3 项文档 / 进度漂移，再进行下一轮最终一致性复审。
