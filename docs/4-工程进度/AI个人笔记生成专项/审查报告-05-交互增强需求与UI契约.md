# AI 个人笔记交互增强审查报告 05：需求与 UI 契约

> 审查时间: 2026-07-22 07:10 CST
> 审查范围: 本次交互增强需求、详细设计、`RepoNotesSection`、AI 生成视图与 repo 会话状态所有权
> 结论: 通过，未发现新的阻断或一般问题

## 1. 审查基线

- `b6aab93d`：个人笔记原位扩展与 Markdown 预览。
- `096175f7`：AI 标题去重、绿色完成态、可折叠草稿与确认后收口。
- `6ec2e87b`：按 repo 保留 AI 生成会话并补单测。
- `ae3d29b`：草稿折叠适配 Reduce Motion。
- `48007dc5`：详细设计、Checklist 与人工验收契约同步。

主工作树同时存在快捷键等其它功能的未提交改动；本轮只在 `48007dc5` 的隔离 worktree 中读取和验证本专项文件，没有带入、覆盖或暂存并行改动。

## 2. 逐项结论

| 需求 / 契约 | 代码证据 | 结论 |
|---|---|---|
| 放大不再创建新窗口 | `RepoNotesSection.expandButton` 只切换 `isEditorExpanded`；`showEditorSheet` 没有写入入口 | 通过 |
| 扩展态保留 Markdown 渲染 | 2026-07-22 交互收口后使用 480pt 内联区域，右对齐 Edit / Preview 并默认预览，二者共用 `editingContent` | 通过 |
| 暂时隐藏而非删除旧弹窗 | `NoteEditorSheet`、`showEditorSheet` 和 `.sheet` 装配仍保留，并有不可达约束注释 | 通过 |
| 标题行不重复过程 | `RepoNoteAIGenerationHeaderControl` 仅在 idle / completed 显示生成或优化入口 | 通过 |
| 完成步骤为绿色圆形勾 | 完成态使用 `checkmark.circle.fill` + `.green`，同时保留“已完成”文字 | 通过 |
| 草稿可折叠 | `RepoNoteAIDraftDisclosure` 标题整行 Button，可展开 Markdown 内容 | 通过 |
| 确认后隐藏草稿与步骤 | 2026-07-22 交互收口后 completed 分支不再创建过程面板或草稿 Disclosure | 通过 |
| 切换 repo 不取消生成 | `onDisappear` 不取消 AI；共享 Store 按 repo 持有运行会话 | 通过 |
| 切回时自动展开 | `shouldExpandNotesOnReturn` 驱动 `isNotesExpanded` | 通过 |
| 主动取消仍然有效 | 2026-07-22 回归修正后仅当前仓库“取消生成”按钮调用 ViewModel cancellation；折叠不再取消 | 通过 |

## 3. UI 规范审查

- 折叠标题仍保持整行点击，AI 操作为同级 overlay，没有嵌套 Button。
- 新增 plain Button 均配置 `.focusEffectDisabled()`。
- 草稿折叠动画与内联编辑器扩展动画均遵循 Reduce Motion。
- 绿色只用于用户明确要求的完成成功态，并有图标和状态文字双重表达，不依赖颜色传义。
- 未新增 Sheet 关闭按钮、刷新按钮、Stepper 或第三方视觉依赖。

## 4. 发现项与处理

本轮未发现需要新增修复提交的问题。实现阶段的 Reduce Motion 漏项已在进入本轮审查前由 `ae3d29b` 修复，本轮复核确认已关闭。

## 5. 本轮结论

本次增强的需求、详细设计和 UI 代码语义一致，可以进入第二轮单元测试、构建、国际化与工作区隔离审查。人工视觉手感、真实 Provider 流式过程和 VoiceOver 仍按 `验收步骤说明.md` 保持待人工验收，不在本报告中伪造完成。
