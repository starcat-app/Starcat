# Awesome 发现第 3 轮审查报告

- 审查时间：2026-08-24 02:56（Asia/Shanghai）
- 审查范围：三个仓库的完整差异、来源稳定排序、聚合证据顺序、全量测试与文档收口
- 审查方式：逐仓库 diff 检查、排序比较器走读、边界测试补齐、全量门禁复验

## 检查项目

- 文档：精选来源排序契约、专项 Checklist、人工 UI 验收边界。
- 代码：客户端来源读取顺序、跨来源证据聚合顺序、API 与运营后台最终差异。
- 测试：同 `sort_order` 精选来源、同来源序/条目序证据的确定性。
- 工程进度：三轮报告、最终结果报告与 Issue #109 状态。

## 发现的问题

| 编号 | 严重程度 | 问题 | 影响 | 修复计划 | 修复状态 |
|---|---|---|---|---|---|
| R3-01 | P2 | API 与正式方案约定精选来源按 `sort_order, id` 排序，客户端在 `sort_order` 相同时却按展示名称排序。 | 来源改名可能改变左栏顺序，与 API 契约不一致。 | 精选来源同序时改用 `source.id` 作稳定 tie-breaker，自定义来源仍保留添加时间和名称顺序。 | 已修复 |
| R3-02 | P2 | 聚合证据比较器只比较 `sort_order` 和 `entry_order`，完全相同时没有最终 tie-breaker。 | 特定输入下证据顺序依赖 SQLite 返回顺序，不利于测试与 UI 稳定。 | 补充来源 ID 最终比较，并增加同序边界测试。 | 已修复 |

## 修复计划

1. 修正 `AwesomeRepository` 的两处比较器。
2. 增加精选来源与证据同序测试。
3. 重跑 Starcat 全量测试、Discovery API 全量测试/`go vet`、运营后台测试与语法检查。
4. 复核三仓库 `diff --check`，生成最终结果报告并更新 Issue。

## 修复结果

- 精选来源已统一按 `sort_order ASC, id ASC` 排序，显示名改动不再影响顺序。
- 聚合证据已增加来源 ID 最终 tie-breaker，不再依赖数据库隐式顺序。
- 新增两个同序边界测试，`AwesomeRepositoryTests` 定向测试通过，`exit_code: 0`。
- 首次定向命令未显式传入 Team，被开发证书门禁在构建阶段拦截；按项目既有 `DEVELOPMENT_TEAM=8WCUMGCWMB` 和 `Apple Development` 参数重试后通过，非代码失败。
- 完整全量门禁结果待最终复验后回填。
