# Awesome 发现专项第 14 轮审查报告

- 审查时间：2026-08-24 19:30 CST
- 审查范围：生产发布目标、Volume 数据安全、聚合网关路由、Fly Release 健康、Awesome 来源回填和客户端验收前置

## 检查项目

- 部署目标：确认 Starcat 正式版使用 `https://starcat-api.fly.dev` 与 `X-SC-Svc: discovery`，而非只访问独立 Discovery App。
- 数据安全：部署前确认生产 Volume 和 Machine，并创建 Volume 快照。
- 发布过程：从 `supports/` 构建上下文打包当前 `starcat-discovery-api` 源码，执行聚合 API rolling deploy。
- 生产验证：检查 Fly Release、Machine、聚合 health、六服务 ping、来源目录和 entries 完整字段。
- 回填验证：手动刷新用户截图涉及的三个来源，统计必需 GitHub 元数据缺失数。

## 发现的问题

1. 中：部署前生产 entries 已有 Stars，但 forks、watchers、创建时间等新增字段未完整进入旧快照，单纯启动新镜像不能替代来源同步。
2. 低：仅部署独立 `starcat-discovery-api` 无法覆盖正式 Starcat 客户端，因为当前生产请求实际经过聚合 `starcat-api`。

## 问题严重程度

- 最高严重程度：中；若不执行来源回填，客户端虽能解码新契约，但详情仍会显示部分空值。

## 修复计划

- 部署聚合 `starcat-api`，启动时执行安全 schema 升级和一次性元数据版本标记。
- 立即手动刷新 `awesome-mac`、`awesome-design-patterns` 和 `awesome-python`；其余来源交给每 3 小时定时任务持续回填。
- 对三个来源统计 Stars、forks、watchers、subscribers、issues、默认分支和时间字段完整率。

## 修复状态

- 问题 1：已修复。三个来源分别成功写入 285、29、475 条，所有必需字段缺失数均为 0；license、topics、homepage 按 GitHub 实际可用性返回。
- 问题 2：已修复。生产聚合 API 已升级至 Fly Release v10，Machine 为 started 且健康检查通过。
- 六个聚合服务 `/api/v1/ping` 均返回 `200` 和 `ok=true`，未发现旁路服务回归。
- Issue #109 继续保持 Open/`Acceptance`，等待 dong4j 人工 UI 验收。
