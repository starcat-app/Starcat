# Awesome 发现第 2 轮审查报告

- 审查时间：2026-08-24 02:55（Asia/Shanghai）
- 审查范围：第 1 轮修复后代码、正式方案数据契约、API 输出时间语义、客户端 v22 schema、排序规则
- 审查方式：数据库字段逐项对照、API envelope 字段追踪、Store SQL 排序核对、相关测试走读

## 检查项目

- 文档：§3.3、§5.2、§9.1 与代码中的表名、字段名、排序和时间语义。
- 代码：`PublishedEntries`、`ListPublishedAwesomeEntries`、客户端 migration/records/repository。
- 测试：同步成功时间、公共 entries 快照、客户端 migration 字段和稳定排序覆盖。
- 工程进度：第 1 轮报告已闭环，专项 Checklist 未冒充人工 UI 验收通过。

## 发现的问题

| 编号 | 严重程度 | 问题 | 影响 | 修复计划 | 修复状态 |
|---|---|---|---|---|---|
| R2-01 | P2 | 正式方案仍写 `awesome_cache_meta` 和条目 `fetched_at`，实际实现为 `awesome_state`，目录 ETag 放在 state、entries ETag 放在 source，条目时间为 `cached_at`。 | 后续 AI 可能按过期草案重复建表或误改字段。 | 把 §9.1 更新为已落地 schema 和职责拆分。 | 已修复 |
| R2-02 | P2 | entries 响应的 `source.updated_at` / `meta.generated_at` 使用来源内容修订时间，没有优先使用最近成功快照时间。 | API 的快照时间语义不准确，运营编辑与 README 快照容易混淆。 | `PublishedEntries` 优先使用 `last_synced_at`，无成功同步时间时才回退 `updated_at`；补测试和文档说明。 | 已修复 |
| R2-03 | P2 | §3.3 写精选来源按 `sort_order + 名称`，§5.1 与 Store 实际为 `sort_order + id`。 | 同一文档存在两个排序契约。 | 统一为 `sort_order ASC, id ASC`，ID 在改名后仍稳定。 | 已修复 |

## 本轮其他结论

- 第 1 轮新增的 preview/save 边界与订阅 draft 状态一致，定向测试已覆盖。
- v22 是本功能尚未发布的 migration 草稿，可在本功能分支内补齐字段，不涉及回写已发布 v1。
- 公共 entries 的 ETag 对完整 envelope 计算哈希，条目变化会失效；本轮只修正时间字段语义，不改缓存算法。

## 修复后验证计划

1. 更新正式方案 schema、排序和 entries 时间说明。
2. 增加 API 服务测试，断言公共快照时间等于成功同步时间。
3. 运行 API 全量测试与 `go vet`，运行 Starcat migration/Awesome 定向测试。
4. 更新本报告状态并提交中文 commit，不 push。

## 修复结果

- 正式方案 §9.1 已与 v22 实际四表结构、`cached_at` 和 ETag 存放位置一致。
- 公共 entries 快照优先用 `last_synced_at` 作为 `source.updated_at` / `meta.generated_at`，并新增同步时间断言。
- 精选来源排序契约统一为 `sort_order ASC, id ASC`。
- Discovery API `80` tests 与 `go vet` 通过；Starcat API/Repository/migration 定向测试通过，均为 `exit_code: 0`。
- 本轮遗留问题：无。
