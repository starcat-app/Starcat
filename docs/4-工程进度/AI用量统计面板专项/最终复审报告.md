# AI 用量统计面板最终复审报告

> 复审时间: 2026-07-19
> 基线: `dev@59067156`
> 分支: `codex/ai-usage-dashboard`
> 结论: 通过；未发现专项范围内未关闭问题

## 1. 功能完整性

- Chat、流式 Chat、流式兼容重试和 Embedding 均在统一客户端边界记录实际 HTTP 调用。
- RAG、知识索引、语义搜索、Repo 摘要 / 标签 / 对话、README 翻译、Agent、MCP 均有稳定功能归因。
- Today / 7 天 / 30 天 / 全部时间，功能 / Provider / 模型筛选，总览、趋势、功能 / 模型下钻和最近调用明细完整。
- 状态 popover 与 Actions 菜单均可打开单例原生窗口；空态、加载态、错误态、窗口恢复、明暗主题、en / zh-Hans 和 VoiceOver 语义已覆盖。

## 2. 数据、隐私与迁移

- `v14-ai-usage-events` 是追加迁移，`v1-initial` 未修改；新旧数据库路径均由测试覆盖。
- 未知 usage 保持 `NULL`，不会伪装为 0；缓存输入和推理输出不重复计入总 token。
- 仅保存时间、数值、稳定维度和错误枚举，不保存 prompt、response、API Key、Base URL、HTTP body 或完整错误文本。
- 用量事件不进入 CloudKit 或匿名遥测，账号切换后 Repository 获取当前用户 writer。

## 3. 代码与性能

- Recorder 写入失败不改变 AI 主请求结果；失败和取消状态不会泄漏完整错误内容。
- Dashboard 在单次只读事务内生成一致快照；popover 使用单 SQL 汇总，不加载趋势、分布或明细。
- ViewModel 使用请求代际避免旧结果覆盖新筛选，并在成功、失败、取消时正确收尾 loading。
- 变更保持专项边界，没有无关格式化或重构；各小功能、修复和审查报告均使用独立中文 commit。

## 4. 测试与静态检查

- `AIUsageCaptureTests`、`AIUsageRepositoryTests`、`AIUsageDashboardViewModelTests`：通过。
- 全量测试：执行 1,641 个测试；仅基线已存在的 `HomeViewModelFilterSortTests` 2 个用例失败（3 个 expectation），另有 1 个 known issue。
- 基线对照：在临时 detached `dev@59067156` worktree 单独运行失败 Suite，失败用例和断言完全一致，确认不是本专项回归；临时 worktree 已移除。
- 排除该基线失败 Suite 后，其余全量测试通过。
- xcstrings JSON、i18n callsite、UI 颜色 / Focus Ring、`git diff --check`：通过。

## 5. 文档与工程进度

- 详细设计、验收步骤、四轮审查报告、最终复审报告与当前实现一致。
- 专项 Checklist 除“最终报告 / clean 收口”外均已完成；结果报告提交后即可最终回填。
- `docs/功能实现总览.md` 已只读核对。因未获得该文件要求的单独写入授权，未修改主进度索引；第四轮报告已给出可直接审核的拟同步条目，这不是实现缺口。

## 6. 最终判断

专项范围内没有遗留 P0 / P1 / P2 问题，可以进入人工 Provider 与视觉验收。正式合并前只需由 dong4j 决定是否另行授权同步 `docs/功能实现总览.md`。
