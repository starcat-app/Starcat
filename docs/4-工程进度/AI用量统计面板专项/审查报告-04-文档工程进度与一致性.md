# 第四轮审查报告：文档、工程进度与一致性

> 审查时间: 2026-07-19
> 审查范围: 功能分支全部变更、设计文档、验收步骤、专项 Checklist、提交历史、`docs/功能实现总览.md`（只读）、全量测试与静态检查
> 结论: 代码与专项范围一致，发现 2 项文档回填问题；主进度总览需等待单独授权

## 已通过

1. `dev@59067156...HEAD` 的生产改动均可追溯到用量存储、采集、归因、聚合、入口或面板，没有夹带无关重构；每个功能 / 修复 / 审查闭环均有独立中文提交。
2. 设计文档中的调用口径、token 语义、功能归因、v14 schema、隐私边界、筛选维度和 UI 入口均与当前代码一致。
3. Checklist 的数据、采集、展示和前三轮审查条目均有对应代码、测试、文档或 commit 证据。
4. xcstrings 是合法 JSON；新增 Swift 调用没有 `String(localized:)` / `NSLocalizedString`；相关 UI 没有 `.tertiary`，plain button 均关闭额外 Focus Ring；`git diff --check` 通过。
5. 全量测试执行了 1,641 个测试，仅 `HomeViewModelFilterSortTests` 的 2 个既有用例失败（3 个 expectation），另有 1 个既有 known issue；在独立 `dev@59067156` detached worktree 同条件重跑该 Suite 得到完全相同失败，确认不是本分支回归。临时基线 worktree 已移除。
6. AI 用量专项 `AIUsageCaptureTests`、`AIUsageRepositoryTests`、`AIUsageDashboardViewModelTests` 全部通过。

## 发现问题

### P2-1 验收文档的自动验证命令未包含 ViewModel 测试

实现已新增 `AIUsageDashboardViewModelTests`，但 `验收步骤说明.md` 仍只运行采集和 Repository 两个 Suite，预期说明也未覆盖取消 loading 与旧请求代际保护。

修复要求：把 ViewModel Suite 加入可复制命令，并补充对应预期。

### P2-2 设计文档未回填轻量状态摘要查询

第三轮已将状态 popover 从完整 Dashboard 聚合改为单 SQL `summary`，但设计文档仍只描述“一次查询返回完整快照”，容易让后续维护者误把轻量入口改回重查询。

修复要求：在查询与 UI 章节明确完整窗口和状态 popover 的两条读取路径及共用口径。

## 主进度总览核对

`docs/功能实现总览.md` 当前没有 AI 用量统计面板条目。根据仓库铁律，当前消息虽授权完成专项和 Checklist，但没有单独出现“可以写总览 / 同步总览 / 记到总览 / 勾上”等授权语句，因此本轮只给出拟同步内容，不修改该文件：

```markdown
- [x] **AI 用量统计面板** — 本地记录 Chat / Embedding 调用与 token，支持时间、功能、Provider、模型筛选及趋势明细 — `Starcat/Features/AI/Usage` / `docs/4-工程进度/AI用量统计面板专项` — 2026-07-19
> 实现：在 OpenAIClient 统一采集 Provider usage 并以 v14 请求事件本地落库；状态 popover 与 Actions 打开原生独立面板，未知 token 保持不可用，不记录 prompt、response、密钥或完整错误。
```

拟放入 `P2 - 远期 / 后续迭代 > AI 高级功能`。若 dong4j 后续明确授权同步总览，还需同步顶部进度仪表盘与 §10 变更日志；本专项结束时保持该主文档未改。

## 本轮后续

先修复 P2-1、P2-2 并提交，再执行“全量排除已确认基线失败 Suite”的补充测试与最终静态检查；之后回填本报告和 Checklist。
