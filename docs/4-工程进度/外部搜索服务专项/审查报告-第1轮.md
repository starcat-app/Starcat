# 外部搜索服务专项审查报告-第1轮

> 日期: 2026-07-03
> 范围: 文档、代码、单元测试、工程进度一致性
> 结论: 发现 1 个文案一致性问题,已在本轮修复。

## 1. 审查项

- 专项 checklist 是否仍有未完成项。
- `docs/功能实现总览.md` 是否已把 External Search Provider 化重构回填为完成。
- 详细设计索引是否登记 `37-外部搜索服务设计.md`。
- `CONTEXT.md` 与 ADR 的术语、边界是否与实现一致。
- 新增 UI 文案是否走 String Catalog。
- 代码是否仍残留 AnySearch 专属上下文文案。
- `rtk jq empty Starcat/Resources/Localizable.xcstrings` 与 `rtk git diff --check` 是否通过。

## 2. 发现与修复

- 问题: `AIConfiguration.swift` 的 `{externalContext}` 注释、`Localizable.xcstrings` 的 AI prompt 占位符说明和旧 `settings.anySearch.*` stale 文案仍写着 AnySearch。
- 风险: 设置页/AI prompt 帮助文案会把已经 Provider 化的 External Search 误描述成 AnySearch 专属能力。
- 修复: 改为 External Search / Provider-aware 文案,保留旧 stale key 但不再显示 AnySearch 专属语义。

## 3. 验证

- `rtk jq empty Starcat/Resources/Localizable.xcstrings`: 通过。
- `rtk git diff --check`: 通过。
- `rtk xcodebuild -scheme Starcat -destination 'platform=macOS,arch=arm64' build`: 通过。

## 4. 后续动作

- 进入第 2 轮审查,重点复核修复后是否还有 AnySearch 专属 UI / AI 文案残留,以及测试与工程进度是否一致。
