# 支付对接正式方案

> 创建：2026-07-04
> 状态：方案已确认，分阶段实施
> 用途：Starcat App Store / Direct 双支付路径、可替换支付网关抽象和统一 Pro 权益方案
> 需求入口：`docs/2-产品/需求讨论/支付对接.md`
> 详细设计：`docs/3-设计/详细设计/40-支付对接详细设计.md`
> 工程进度：`docs/4-工程进度/支付对接专项/checklist.md`

---

## 1. 方案结论

Starcat 支付体系分为两条渠道：

```text
App Store build
  -> StoreKitEntitlementProvider
  -> Apple IAP / subscription

Direct build
  -> DirectEntitlementProvider
  -> Starcat License API
  -> DirectPaymentProvider / DirectLicenseProvider
      -> CreemProvider（首期候选）
      -> LemonSqueezyProvider（备选）
      -> PaddleProvider（备选）
      -> StripeProvider（备选）
```

客户端业务层只依赖：

```text
EntitlementGate
  -> ProEntitlement
```

不得在 AI、Release、Search、Settings 等业务模块里直接判断 StoreKit、Creem 或 license provider。

---

## 2. 渠道边界

### 2.1 App Store build

App Store build 必须保持 StoreKit-only：

1. 商品购买、恢复购买、订阅管理都走 StoreKit / Apple。
2. 不显示外部支付 provider。
3. 不显示 license key 输入。
4. 不显示官网购买链接。
5. 不允许外部 license 解锁 App Store build。

### 2.2 Direct build

Direct build 必须保持 Direct-only：

1. 不显示 StoreKit 商品购买入口。
2. 展示 license 激活、解绑、官网订阅管理入口。
3. 可打开 Direct provider checkout / customer portal。
4. 通过 Starcat License API 获取统一权益快照。

---

## 3. Provider 抽象

### 3.1 DirectPaymentProvider

负责购买和订阅管理：

```text
createCheckoutSession(plan, customer)
createCustomerPortal(customer)
mapWebhookEvent(payload)
```

### 3.2 DirectLicenseProvider

负责 license 设备实例：

```text
activateLicense(key, instanceName)
validateLicense(key, instanceId)
deactivateLicense(key, instanceId)
```

### 3.3 DirectEntitlementProvider

负责把 provider-specific 状态转换为 Starcat 统一权益：

```text
Provider response
  -> DirectEntitlementSnapshot
  -> ProEntitlement
```

---

## 4. Creem 首期策略

Creem 作为 Direct v1 候选 provider，原因：

1. 支持 Merchant of Record，降低全球税务处理压力。
2. 支持 subscriptions。
3. 支持 license keys。
4. 支持 activate / validate / deactivate。
5. 支持 webhook 和 customer portal。
6. 支持 test mode。

但 Starcat 不绑定 Creem：

1. 客户端命名不出现 `CreemLicenseManager` 作为业务核心类型。
2. 后端 provider adapter 可以叫 `CreemDirectPaymentProvider`。
3. API response 必须转成 provider-neutral DTO。
4. 文案尽量写“官网订阅 / 授权码”，不直接把 provider 作为产品概念。

---

## 5. 服务端边界

客户端不能直连 Creem / Lemon Squeezy / Paddle / Stripe 的私密 API。

Direct v1 后端最小接口：

```text
POST /v1/direct/checkout
POST /v1/direct/licenses/activate
POST /v1/direct/licenses/validate
POST /v1/direct/licenses/deactivate
POST /v1/direct/customer-portal
POST /v1/webhooks/direct/{provider}
```

后端职责：

1. 保存 provider customer id、subscription id、license instance id 的映射。
2. 校验 webhook 签名。
3. 处理退款、过期、取消、争议等事件。
4. 生成 Starcat entitlement snapshot。
5. 给客户端提供 provider-neutral 状态。

---

## 6. 本地存储

Direct 客户端只保存必要信息：

| 字段 | 存储 | 说明 |
|------|------|------|
| license key | Keychain | 敏感 |
| license instance id | Keychain | 敏感 |
| entitlement snapshot | UserDefaults / JSON | 非敏感 UI 快照 |
| last validated at | UserDefaults / JSON | 离线宽限 |
| provider display name | UserDefaults / JSON | UI 展示用，不参与授权 |

新增任何启动期 Keychain 访问都必须继续遵守 `TestEnvironment.isRunning` 门控。

---

## 7. 实施分期

### Phase 1：支付抽象

- [ ] 落地 `DistributionChannel`。
- [ ] 抽出 `EntitlementProvider` 协议。
- [ ] 将 StoreKit 实现包进 `StoreKitEntitlementProvider`。
- [ ] 业务层只依赖 `EntitlementGate`。

### Phase 2：Direct License API 契约

- [ ] 定义 Direct license DTO。
- [ ] 定义 checkout / activate / validate / deactivate / portal endpoint。
- [ ] 定义 webhook 事件映射。

### Phase 3：Creem provider

- [ ] Creem test mode 产品与 license 配置。
- [ ] 后端 `CreemDirectPaymentProvider`。
- [ ] 后端 `CreemDirectLicenseProvider`。
- [ ] 客户端 License 设置页。

### Phase 4：多 provider 扩展

- [ ] provider registry。
- [ ] provider-specific webhook adapter。
- [ ] provider 切换的运维文档。

---

## 8. 验收标准

### App Store

- [ ] App Store build 无 Direct payment / license 文案和入口。
- [ ] StoreKit 购买 / 恢复 / 管理订阅可用。
- [ ] App Store build 无 Creem / Lemon Squeezy / Paddle / Stripe API URL。

### Direct

- [ ] Direct build 无 StoreKit 商品购买入口。
- [ ] license 激活后 Pro 生效。
- [ ] validate 失败时按宽限期处理。
- [ ] license 过期 / 退款 / 禁用后 Pro 失效。
- [ ] customer portal 可打开官网订阅管理。

### 架构

- [ ] AI / Release / Search 等业务模块不直接 import StoreKit 或 provider adapter。
- [ ] provider API key 只存在服务端。
- [ ] webhook 先验签后处理。

---

## 9. 关联与归档

以下旧文档的正文已归并到本文和详细设计：

| 旧文档 | 新归属 |
|--------|--------|
| `Direct分发与LemonSqueezy授权方案.md` | Direct 授权抽象与 provider-neutral 策略 |
| `StoreKit订阅上架方案.md` | App Store 支付路径 |
| `StoreKit订阅实施决策记录.md` | StoreKit 历史实施记录，保留参考 |

