# 第 3 轮审查报告

## 审查信息

- 审查时间：2026-08-23 23:40（Asia/Shanghai）
- 审查范围：全新 Collection SQLite、最新 migration、Swift live fixture、真实训练指标、安全与滥用控制、最终完成定义
- 审查基线：Starcat `c258006`、Collection `8fc59ed`、Trainer `cd4d963`

## 检查项目

### 文档审查

- 核对 63 号详细设计的“完整离线评估”和“IP / participant_key 短期限流”是否有真实实现证据。
- 核对专项 checklist 与最新 migration、清理和 E2E 结果一致。
- 核对非目标没有被本轮工程化补强扩大。

### 代码审查

- 在全新数据库进程上验证 `001_initial.sql` 自动执行并可直接接收 Swift 分块上传。
- 复审公网写入的 key、请求大小、仓库数量和短期限流边界。
- 检查 live fixture 的训练/验证时间切分是否能产生非空评估主体。

### 测试审查

- 全新 Collection 数据库成功接收 2 名匿名主体，Admin 导出为 2 行。
- Trainer 完整管道、Bundle verify 和 repo query 成功。
- 本次 manifest 显示 `evaluated_subjects=0`，说明训练成功但 live fixture 未实际覆盖验证窗口。

### 工程进度审查

- migration 与过期上传清理已完成；三个仓库没有未提交代码。
- 仍需修复 live fixture 和短期限流后再做最终全量回归。
- `docs/功能实现总览.md` 继续保持只读，专项 checklist 已独立回填。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R3-01 | 中 | live fixture 把 repo 3 的 `starred_at` 设为 2025-03，早于 `train_end=2025-06-01`，导致真实 E2E 虽能训练和查询，但离线评估主体为 0。 | 将第一名主体的 repo 3 固定到 2025-07 验证窗口，重新从全新数据库执行 E2E，并要求 `evaluated_subjects > 0`。 | 已修复 |
| R3-02 | 中 | Collection 已有 Bearer key、body/chunk 上限，但 63 号设计声明的 IP 与 `participant_key` 短期限流尚未实现；公开 App key 被提取后缺少第二层滥用抑制。 | 增加内存固定窗口限流：公网写接口按客户端地址限制，create 再按匿名主体 HMAC key 限制；只短暂保存在内存，不写日志、数据库或训练产物，补 429 与窗口恢复测试。 | 已修复 |
| R3-03 | 低 | 使用说明声称 E2E 完整执行离线评估，但没有写明评估必须包含至少一个主体，容易把空评估当成通过。 | 在 E2E 说明和最终报告记录非空评估验收值。 | 已修复 |

## 修复结论

本轮问题全部修复。全新数据库 E2E 最终得到 `evaluated_subjects=1`、`recall@3=1.0`、`mrr@3=1.0`、`ndcg@3=1.0`，Bundle verify 与 query 通过；公网地址和匿名主体两层限流的普通测试、窗口恢复测试与 race test 通过。继续执行第 4 轮无遗留审查。
