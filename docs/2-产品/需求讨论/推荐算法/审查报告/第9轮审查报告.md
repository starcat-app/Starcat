# 第 9 轮审查报告

## 审查信息

- 审查时间：2026-08-30 14:05（Asia/Shanghai）
- 审查范围：History Snapshot 安装峰值空间、Registry 保留策略、Delta 事务、代码调用影响和生产持久卷
- 审查基线：History `7a45234`、聚合生产镜像 `deployment-01M18M3EWB66PJ7FHGWMMDRRQK`

## 检查项目

### 文档审查

- 核对 README、62/66 号设计是否完整描述 Builder attestation、流式 checksum 和大文件生命周期。
- 核对 Snapshot 保留、runtime 可写副本和 Fly Volume 容量边界。

### 代码审查

- 刷新 History、Trainer、聚合 API 的代码知识图谱；相关修改文件无记录的解析缺口。
- 复核 `extractAndVerifyZip`、`verifySnapshot`、`prepareRuntime`、`cleanupArtifacts` 和 `ApplyDelta`。
- 复核 Delta 更新统计、active watermark 和幂等键均处于同一事务。

### 测试审查

- 新增 `TestExtractAndVerifyZipReleasesArchiveAfterExtraction`，确认 ZIP 删除后解压数据库仍可继续使用。
- History `make test` 再次通过：Go test/vet 与 Builder 6 项 pytest 全部通过。
- 聚合镜像使用所有服务的当前 `dev` 工作区重新构建并部署成功。

### 工程进度审查

- Fly Volume 为 29.5 GiB，部署前已用 10.9 GiB、可用 17.4 GiB；History Registry 当前约 10.7 GiB。
- 新代码不再让已解压 ZIP 跨越 checksum/schema/runtime 安装阶段，降低后续多 Snapshot 切换的峰值空间。
- 部署后 7 个聚合服务健康，History active watermark 和全部统计保持不变。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R9-01 | 中 | Snapshot ZIP 解压完成后仍保留到整个安装函数退出，月度大快照会额外占用约一个压缩包大小的峰值空间 | 完成所有 ZIP entry 解压后立即关闭 reader 并删除 `bundle.zip`，后续只使用已验证的解压文件；补直接回归测试 | 已修复（History `790e68f`、`7a45234`） |

## 修复结论

- Snapshot 安装同时消除了重复文件扫描和无必要 ZIP 驻留，当前 30GB Volume 的后续月度切换空间更可控。
- 修复已进入聚合生产镜像；部署后查询返回 200，active watermark 仍为 `2026-08-26`，未发生数据回退。
- 本轮未发现 Delta 事务、幂等语义、统计增量或 Registry 原子切换的遗留问题。
