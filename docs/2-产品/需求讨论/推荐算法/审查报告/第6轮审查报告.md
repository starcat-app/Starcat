# 第 6 轮审查报告

## 审查信息

- 审查时间：2026-08-24 03:10（Asia/Shanghai）
- 审查范围：真实训练重跑、ServingBundle 完整性、Recommend API v2、进程重启恢复、Starcat 客户端真实请求、BigQuery 查询配置
- 审查基线：Starcat `a0f6ce3`、Collection `afb8321`、Trainer `08131a0`、Recommend API `5f5c98a`

## 检查项目

### 文档审查

- v1 SimRepo 与 v2 自研路由边界和 61/63 号详细设计一致，没有恢复已取消的 `starcat-recsys-api`。
- BigQuery 示例范围是 `2026-08-01`～`2026-08-07`；已实际验证的是单日 `2024-01-01`，二者必须在最终报告中分开说明。
- 当前 GCP CLI 有有效登录，但默认 project 为 unset；Trainer 配置必须显式填写 `billing_project`，不能把历史验证误写成当前可直接执行的默认配置。

### 代码审查

- Trainer 修复后在单主体数据上将 SVD 标为 `skipped`，Popular、co-star、ablation、评估、Bundle 安装和远端发布全部完成。
- Recommend API v2 单仓和多 seed 查询返回 `source=starcat_trained`、`fallback=false` 和真实 `model_version`。
- Recommend API 重启后能从同一 Registry 恢复 active version，说明 active 指针和不可变模型目录不是进程内临时状态。
- Starcat 只有 URLProtocol 契约测试，尚缺直接使用产品 `RecommendAPI(.trainedV2)` 访问本机已激活模型的自动化证据。

### 测试审查

- 真实 Bundle：1867 个公开非 archived 仓库、2000 条 co-star Top-20 边、100 个可查询 source repo。
- Bundle verify/query、v2 GET、v2 POST 和重启恢复查询均通过。
- Starcat 全量测试在新增 live test 前通过：总计 2581，失败 0。

### 工程进度审查

- 四个仓库仍各自保持独立 Git 边界，未 push、未部署。
- 主仓库真实数据只用于本机 E2E；原始匿名主体没有进入 ServingBundle 或 v2 响应。
- `docs/功能实现总览.md` 继续保持只读。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R6-01 | 中 | 缺少 Starcat 产品客户端对本机 v2 ServingBundle 的真实网络测试 | 增加默认离线、显式环境变量开启且可强制要求执行的 live integration test | 已修复 |
| R6-02 | 低 | Collection live fixture 注释仍称单主体会导致完整训练无法启动 | 修订为“两主体用于实际覆盖 SVD，单主体现在会明确 skipped” | 已修复 |
| R6-03 | 中 | BigQuery 默认 project 当前未配置，若只复制 SQL 会报 `Cannot start a job without a project id.` | 最终报告明确配置前置条件，并同时记录示例范围与已验证范围 | 待最终报告收口 |

## 修复结论

- Starcat 提交：`1124e94 test(recommendation): 增加自研推荐接口真实验收`。
- 使用 `STARCAT_RECOMMEND_LIVE_REQUIRED=1` 运行 1 项 live test，通过并校验 `real-star-e2e-20260824-v2`。
- R6-03 将在最终测试报告中关闭；UI 视觉验收仍等待 macOS 解锁，不在本轮提前判定通过。
