# 第 8 轮审查报告

## 审查信息

- 审查时间：2026-08-30 13:55（Asia/Shanghai）
- 审查范围：`starcat-history-api`、WatchEvent 每日追加、聚合 `starcat-api`、Starcat History 客户端、Discovery 迁移边界及 62/66 号设计
- 审查基线：Starcat `803094cc`、History `3ff4a2e`、Trainer `5b2ce10`、聚合 API `6616cc4`、Discovery `43d6f8b`

## 检查项目

### 文档审查

- 核对 62 号设计的 Snapshot/Delta 校验、客户端 `/events`、Discovery 迁移阶段和完成定义。
- 核对 66 号设计的本地唯一 Raw、History 日增量与整个数据平台阶段边界。
- 核对开发前问题清单 5.22、推荐算法 README 与真实生产水位和统计。

### 代码审查

- 审查 Snapshot ZIP 白名单、流式 checksum、manifest v1/v2、Builder attestation、必需表和 active state 校验。
- 审查 Delta 的完整 `quick_check`、相邻水位、事务应用、幂等键和统计增量。
- 审查 WatchEvent append-existing 的连续日期、预算、checkpoint 和旧分区免重扫行为。
- 审查 Starcat 默认 History 聚合路由及 Discovery 默认关闭条件。

### 测试审查

- History：Go test/vet 与 Builder 6 项 pytest 全部通过。
- Trainer：Ruff、strict mypy、122 项 pytest 全部通过，覆盖率 87%。
- 聚合 API：`go test ./...`、`go vet ./...` 通过。
- Discovery：`make check` 通过，默认关闭 History 的配置测试通过。
- Starcat：History API、Repository 与聚合路由 3 个 Suite、21 项测试通过。

### 工程进度审查

- 真实 Raw 已连续追加到 `2026-08-26`，共 3,891 个分区。
- 生产 active Snapshot 为 `watch-history-20260825-v1`，active watermark 已由真实 Delta 推进到 `2026-08-26`。
- Discovery 生产 Secrets 未配置 `STAR_HISTORY_ENABLED`、GCP Project 或 BigQuery History 预算，不会创建新的 History BigQuery job。
- `docs/功能实现总览.md` 未获单独授权，本轮保持只读。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R8-01 | 高 | Snapshot 上传原先会在解压后再次计算大文件 checksum，并在服务端重复执行完整 `quick_check` | checksum 合并进流式解压；Builder manifest v2 写入 `quick_check` attestation 与数据库大小，服务端校验 attestation/schema/statistics | 已修复（History `7cc94e7`） |
| R8-02 | 高 | `/internal/stats` 原先扫描全量压缩序列表，会占用唯一 SQLite 查询连接并拖慢业务查询 | Snapshot 固化 `history_statistics`，Delta 事务内增量更新，统计接口改为常量时间读取 | 已修复（History `352ce09`） |
| R8-03 | 中 | 聚合 Publisher 未携带 History 分流头，且未兼容聚合响应信封 | 增加 `HISTORY_GATEWAY_SERVICE` 与 `X-SC-Svc`，统一解包 `{schema_version,data}` | 已修复（History `eff0b62`、`3ff4a2e`） |
| R8-04 | 中 | 62/66 号设计和推荐算法 README 仍停留在“生产待部署/History 仍由 Discovery 提供” | 按真实 Snapshot、Delta、水位、统计和稳定窗口边界同步文档 | 已修复（Starcat `803094cc`） |
| R8-05 | 低 | 聚合 Fly 配置注释仍声称服务端执行完整 Snapshot `quick_check` | 改为流式 checksum + Builder attestation/schema/runtime 安装说明 | 已修复（聚合 API `6616cc4`） |

## 修复结论

- 大文件校验从“多次全盘扫描”收敛为“Builder 一次完整校验 + 云端单次流式摘要和结构校验”；Delta 保持严格完整校验。
- 生产查询不再被统计接口的全表扫描阻塞，真实 200/304 查询和统计均已恢复。
- 文档、代码、测试和生产水位已经一致；Discovery 的代码删除仍严格受一个稳定发布窗口约束，不提前移除回退能力。
