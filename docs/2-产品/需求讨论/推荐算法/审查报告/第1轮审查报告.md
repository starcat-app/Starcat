# 第 1 轮审查报告

## 审查信息

- 审查时间：2026-08-23 23:25（Asia/Shanghai）
- 审查范围：Starcat 公开 Star 数据贡献、`starcat-collection-api`、Collection Source Connector、三仓本机 E2E 与配套文档
- 审查基线：Starcat `67e31ee`、Collection `bdce1ce`、Trainer `cd4d963`

## 检查项目

### 文档审查

- 对照需求入口、60/61/63 详细设计、Collection/Trainer README 与使用说明检查职责边界。
- 核对第一阶段没有 History、服务端删除、上传状态 UI、Gateway 集成和第三方 API。
- 核对字段白名单、默认关闭、完整同步边沿和最长 24 小时静默重试。

### 代码审查

- 检查公开仓过滤、canonical hash、账户级 Outbox、同步旁路、分块协议和 Admin 导出。
- 检查账户切换、取消、新快照覆盖旧任务和上传失败的并发边界。
- 检查 Collection HMAC、鉴权、幂等 commit 与 Trainer 数据源上限/脱敏。

### 测试审查

- Swift 分层定向测试已通过；显式 live test 已真实上传两名匿名主体。
- Go 服务测试已通过；Python Connector 测试已通过。
- Starcat → Collection → Trainer → ServingBundle 的本机真实链路已通过，Bundle 校验与查询成功。
- 全量回归留到修复本轮问题后执行。

### 工程进度审查

- 三个仓库均已有独立中文本地提交，未 push、未部署。
- `docs/功能实现总览.md` 按项目铁律保持只读，未获得单独写入授权。
- 专项需求文档尚需把“进入实施”和设置入口描述更新为实际完成状态。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R1-01 | 高 | 旧快照上传失败时，Coordinator 会重新读取当前 due task；若完整同步已用新快照覆盖 Outbox，旧请求的错误可能把新快照直接标记为 `retry_wait`，导致最新数据被错误延后。 | 将失败处理绑定到实际发起请求的 task ID；Repository 原子返回 UPDATE 是否命中，并新增“上传中被新快照替换”并发测试。 | 已修复 |
| R1-02 | 中 | 63 号详细设计仍写“方案已确认，进入实施”，设置入口写“隐私与数据”，与实际“通用 → 隐私与推荐”不一致。 | 更新状态、入口和验证完成记录，不改动功能实现总览。 | 已修复 |
| R1-03 | 中 | 现有测试覆盖普通失败重试，但没有覆盖 R1-01 的快照替换并发时序。 | 使用可控 uploader 固定旧请求失败、新请求成功的时序并断言最新任务正常上传。 | 已修复 |

## 修复结论

本轮发现的 1 个高严重程度并发问题和 2 个文档/测试一致性问题均已修复。`DataContributionCoordinatorTests` 与 `DataContributionTests` 定向回归通过，可以进入第 2 轮审查。
