# 第 2 轮审查报告

## 审查信息

- 审查时间：2026-08-23 23:33（Asia/Shanghai）
- 审查范围：第 1 轮修复后的三仓代码、完整测试集、数据库生命周期、详细设计完成定义与专项进度
- 审查基线：Starcat `d2ff933`、Collection `bdce1ce`、Trainer `cd4d963`

## 检查项目

### 文档审查

- 逐项核对 63 号详细设计的服务目录、Outbox 状态机、24 小时清理、验证矩阵和完成定义。
- 核对 Starcat 使用说明与 Trainer 示例命令已被真实 E2E 采用。
- 核对 History、在线推荐、删除和第三方 API 仍明确属于非目标。

### 代码审查

- 复审第 1 轮 task ID 条件更新，确认旧请求不会删除或延后最新快照。
- 检查 Collection SQLite 初始化、外键、分块生命周期和 active snapshot 原子切换。
- 检查 Trainer Connector 的 checksum、上限、schema 与错误脱敏。

### 测试审查

- Starcat 全量结果：`2579` 项，失败 `0`；通过 `2568`、跳过 `10`、预期失败 `1`。
- Collection：`go test ./...`、`go test -race ./...`、`go vet ./...`、`go build ./...` 全部通过。
- Trainer：格式、Ruff、mypy、`53` 项 pytest、`89%` 覆盖率、sdist/wheel 构建全部通过。
- xcresult 中有 4 条既有 `DiagnosticsTests` 后台发布 runtime warning，不由本专项文件触发，不影响测试通过结论。

### 工程进度审查

- 三仓工作区在测试后均干净；训练产物位于 gitignore 的本机 workspace。
- 第一阶段功能和 E2E 已完成，但 Collection 数据库演进与临时上传回收尚未满足详细设计。
- 专项尚缺一份可逐项回填的实施 checklist；通用 AI 开工检查不是本任务完成清单。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R2-01 | 高 | Collection schema 直接内嵌在 `store/sqlite.go` 并靠 `CREATE TABLE IF NOT EXISTS` 初始化，没有 migration 版本记录，后续新增数据源或字段无法安全、可审计地追加升级。 | 增加内嵌、顺序执行、事务化的 SQL migration runner 和 `schema_migrations`，把 v1 schema 移入 `001_initial.sql`，补幂等测试。 | 已修复 |
| R2-02 | 中 | 详细设计声明未 commit 上传 24 小时后可清理，但服务没有清理入口，长期失败/中断会积累 metadata 和 chunk。 | 增加仅删除 `uploading` 状态过期记录的级联清理，并在新上传入口按服务时间执行；补 committed/fresh 保留测试。 | 已修复 |
| R2-03 | 低 | 专项没有独立实施 checklist，无法直接证明完成定义逐项回填；Outbox 取消文案还把状态过度限定为 `retry_wait`。 | 在 63 号详细设计新增已验证实施 checklist，并把取消语义改为保留原 `pending/retry_wait` 状态。 | 已修复 |

## 修复结论

本轮问题全部修复。Collection 普通测试、race test、vet 与 build 再次通过；migration 幂等、过期 uploading 删除、fresh/committed 保留和 chunk 级联删除均有自动化证明，可以进入第 3 轮审查。
