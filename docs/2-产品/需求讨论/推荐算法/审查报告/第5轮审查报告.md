# 第 5 轮审查报告

## 审查信息

- 审查时间：2026-08-24 02:51（Asia/Shanghai）
- 审查范围：主账户真实 Star 快照、Collection 导出、Trainer 单主体训练编排、Recommend API 发布前置链路
- 审查基线：Starcat `345710a`、Collection `afb8321`、Trainer `e369c4f`、Recommend API `5f5c98a`

## 检查项目

### 文档审查

- 63 号详细设计要求 Trainer 能直接消费 Collection 当前 active snapshot，并保持单机冷启动可用。
- Trainer 文档原先只描述 Popular、SVD、co-star 均执行，没有说明候选模型样本条件不足时的降级行为。
- 专项 checklist 的“真实数据完成训练和发布”不能仅凭 fixture E2E 勾选，必须用主账户真实快照验证。

### 代码审查

- Starcat 只在 `force: true` 完整同步成功边沿生成快照；普通工具栏同步走增量，不上报，符合旁路契约。
- Collection 成功接收 1 个匿名主体、1954 个公开 Star，按 1000 条上限拆为 2 个 chunk；客户端 Outbox 成功后清空。
- Trainer 首次真实运行在 `SVDTrainer` 抛出“至少需要 2 个主体和 2 个仓库”后中止，导致可训练的 Popular/co-star 也无法发布。

### 测试审查

- 真实上传、commit、active 切换和 NDJSON 导出通过。
- 失败运行已证明问题只发生在 Trainer 候选模型编排，不是 Starcat 上传或 Collection 数据损坏。
- 原测试缺少“只有一个合格主体时整条管道仍交付所选模型”的覆盖。

### 工程进度审查

- 四仓分支与职责边界正确；本轮发现的问题必须在 Trainer 独立仓库修复并单独提交。
- `docs/功能实现总览.md` 未取得单独授权，继续保持只读。

## 发现的问题

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R5-01 | 高 | 单主体冷启动时 SVD 的数学前置条件会中止整条训练管道 | 引入明确的 `InsufficientTrainingDataError`；执行器仅对该异常记录 `skipped`，其它异常仍失败；继续训练、评估和发布可用模型 | 已修复 |
| R5-02 | 中 | 文档和测试未说明/覆盖单主体候选模型降级 | 更新 Trainer README、架构、使用说明、实施清单并增加单主体端到端回归测试 | 已修复 |

## 修复结论

- Trainer 提交：`08131a0 fix(training): 支持单主体冷启动降级`。
- `make check` 通过：Ruff format、Ruff lint、strict mypy、58 项 pytest，覆盖率 88%。
- 同一批真实数据将在下一轮重新训练；只有重新生成、发布并在线查询成功后，才能关闭真实全链路事项。
