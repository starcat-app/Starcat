# Starcat v2 功能清单

> 创建：2026-06-17
> 用途：v1 上架后迭代的**功能 backlog**（不阻塞 v1 发版）
> 维护：新功能先入本清单 `- [ ]`，开工前再同步到 `docs/功能实现总览.md` 对应章节
> 关联：`docs/6-发版与上架/v1-上架检查清单.md`（v1 收尾）、`docs/1-立项/功能清单.md`（原始优先级矩阵）

---

## 0. 范围说明

### 0.1 与 v1 的边界

| 维度 | v1 | v2（本文档） |
|------|-----|----------------|
| 目标 | 可上架、核心闭环可用 | 信息密度、迁移工具、付费墙等增强 |
| 进度索引 | `功能实现总览.md` §3–§8 | 本文件为**规划池**，落地时再写回总览 |
| 详情页外链 | Toolbar `ExternalLinksMenu` 已覆盖跳转 | Hero **内联展示**数字与摘要（见 §1） |

### 0.2 状态符号

| 符号 | 含义 |
|------|------|
| `- [ ]` | 待开始 |
| `- [x]` | 已完成（落地后写 `> 实现：...` 并同步总览） |

---

## 1. 详情页 Hero 信息增强（dong4j 2026-06-17 延期至 v2）

### 1.1 背景

2026-06-03 在 `功能实现总览.md` §3.3 登记了 4 条 P1「详情页信息增强」TODO。2026-06-17 走查结论：

- **Toolbar**（`ExternalLinksMenu`）已在 Manage / Trending / Weekly / Activity 四场景提供 Issues / PR / Releases / Homepage **跳转**。
- 本组功能的目标是：在 **hero 区内联展示**信息（数字、tag、官网），让用户**不打开菜单**也能扫一眼判断仓库状态——与 toolbar **互补**，不是重复造链接。
- **v1 不做**；布局方案已讨论定型，v2 按 §1.3(详见 `docs/2-产品/需求讨论/正式方案/Pro付费墙验证清单.md`) 实施。

### 1.2 功能条目

- [ ] **Website 内联显示** — 在 hero 展示 `Repo.homepage`（可点击域名，非完整 URL 堆砌）
  > **v1 现状**：字段已落库；仅 toolbar 菜单项 `externalLinks.homepage` 可打开。
  > **v2 目标**：`descriptionSection` 与 `statsSection` 之间新增 `metaLinksRow`：`globe` + 截断域名，`pressableHover` + `NSWorkspace.open`。
  > **约束**：复用 `RepoExternalLinks` / homepage scheme 白名单；无 homepage 时整行隐藏。
  > **涉及**：`RepoMetadataHeaderView.swift`（或抽 `RepoDetailMetaLinksRow`）

- [ ] **Open Issues 数（含 PR）** — stats 行展示 `open_issues_count`
  > **v1 现状**：`GitHubRepoDTO` / `Repo` / DB 已有 `openIssuesCount`（R-01）；搜索弹窗已展示；**hero stats 未展示**。
  > **v2 目标**：`statsSection` 中放在 **Forks 之后**，chip 显示数字；点击跳 Issues 页（`RepoExternalLinks.issues`）。
  > **约束**：GitHub 语义为 **Issue + PR 合计**；label 或 tooltip 必须写清「含 PR」，避免误解。
  > **涉及**：`RepoMetadataHeaderView.swift` + i18n key

- [ ] **Open PR 数（独立）** — 单独展示打开的 PR 数量（可选，低优先级）
  > **v1 现状**：无；toolbar 仅有「去 Pull Requests 列表页」。
  > **v2 目标**（二选一，开工前拍板）：
  > - **推荐 A**：不单独占格，Issues chip 长按/菜单展开「Issues n · PR m」
  > - **B**：Issues 旁再加 `PR m` chip，需 `GitHubAPIClient` 懒加载 + SWR 缓存（1h TTL）
  > **约束**：**禁止**塞进 Stars 同步流程；仅详情页 `.task(id: repo.id)` 按需拉取；评估 Search API 30/min 与主限流。
  > **涉及**：`GitHubAPIClient` + 可选 `repo_extra_stats` 缓存层 + hero UI

- [ ] **最新 Release 内联展示（未订阅可见）** — tag + 发布时间 + 可选摘要
  > **v1 现状**：`RepoReleaseStatItem` 仅在**已订阅**时第二行显示最新 `tagName`；未订阅为灰色 “Releases” 文案；toolbar 可跳 Releases 列表页。
  > **v2 目标**（二选一，开工前拍板）：
  > - **推荐 A（最小）**：扩展 `RepoReleaseStatItem`——未订阅也懒加载 `releases/latest`，第二行显示 `vX.Y.Z · 相对时间`（可点跳 GitHub）
  > - **B（完整）**：在 `metaLinksRow` 下方加 `releaseSummaryRow`：`tag · 时间 — body 首行截断`
  > **约束**：与 §4.1 Release 订阅共用 `ReleaseRepository` / 缓存，避免重复打 API；404 显示「无 Release」占位，不报错。
  > **涉及**：`RepoReleaseSection.swift` / `ReleaseRepository` + `RepoMetadataHeaderView.swift`

### 1.3 Hero 布局方案（v2 实施时遵循）

已定型的垂直结构（`RepoMetadataHeaderView`）：

```
header          avatar · full_name · badges · trailingActions
badgeRow        archived / fork / license
inlineTopicsRow
description     （可选）
metaLinksRow    ← Website；可选 Release 摘要第二行
statsSection    ⭐ Forks · ◯ Issues · Watchers · 日期 · 🔔 Release
```

设计原则：

1. **数字指标** → 只进 `statsSection`（与 Stars/Forks 同语言）。
2. **外链/身份**（Website）→ 不进 stats，放 description 与 stats 之间的 `metaLinksRow`。
3. **Release 长摘要** → 不进 stats 单行；优先扩 `RepoReleaseStatItem`，摘要过长时用 `metaLinksRow` 第二行。
4. **独立 PR 数** → 默认合并进 Issues 展示，避免 stats 行在窄窗口挤爆。

与 toolbar 分工：

| 位置 | 职责 |
|------|------|
| Toolbar `ExternalLinksMenu` | 熟练用户快捷跳转 GitHub 子页 |
| Hero 内联 | 扫一眼决策：活跃度、版本、官网 |

### 1.4 v2 最小增量建议（实施顺序）

若分期做，建议顺序：

1. Open Issues 数（数据已有，改动小）
2. Website `metaLinksRow`（纯 UI）
3. 最新 Release 扩展 `RepoReleaseStatItem`（复用 Release 层）
4. 独立 Open PR 数（视需求再定 A/B）

---

## 2. 其它 v2 候选（占位，待 dong4j 追加）

> 上架走查中已识别、明确不进 v1 的项可追加到本节。开工前从总览迁入并写清优先级。

- [ ] **JSON 导入** — OhMyStar / Astral 兼容（原 P0 延后，见总览 §3.6）
- [ ] **JSON 导出** — 全量 / 选中导出（原 P0 延后）
- [ ] **Discovery（Show HN）客户端** — Activity 第 8 分类（总览 §5，后端已有设计）
- [ ] **StoreKit 2 + Pro 付费墙** — 先拍板 `docs/2-产品/需求讨论/正式方案/PRO 订阅功能划分.md` §8 D-1～D-8
- [ ] **CloudKit 多端同步** — 总览 Week 5 整块
- [x] **Release 资产直接下载** — `ReleaseAssetDownloader` + `ReleaseAssetDownloadCoordinator` + `ReleaseAssetRowView`（2026-06-17）

---

## 3. HelloGitHub 数据源接入（Trending 扩展）

### 3.1 背景

HelloGitHub 是一个中文社区驱动的 GitHub 项目推荐平台，每月精选有趣、易上手的开源项目。其 API 返回结构化数据，可直接作为 Trending 分类的**补充发现源**。

### 3.2 API 端点

```
GET https://abroad.hellogithub.com/v1/?sort_by=featured&page={1-5}&rank_by=newest&tid=all
```

- 无需认证
- 当前约 5 页数据
- `sort_by`: `featured` / `newest` / `hot`
- `rank_by`: `newest` / `monthly` / `yearly`
- `tid`: `all` 或具体分类 ID

### 3.3 单条数据示例

```json
{
    "item_id": "9a250cf850034e029b9e222291804464",
    "full_name": "margelo/react-native-graph",
    "title": "高性能 React Native 折线图组件",
    "title_en": "High-Performance React Native Line Chart Component",
    "author": "margelo",
    "author_avatar": "https://img.hellogithub.com/github_avatar/91980534.jpeg",
    "name": "react-native-graph",
    "summary": "这是一款基于 Skia 渲染引擎的 React Native 高性能折线图组件...",
    "summary_en": "This is a high-performance React Native line chart component...",
    "is_hot": false,
    "is_claimed": false,
    "primary_lang": "TypeScript",
    "lang_color": "#2b7489",
    "clicks_total": 197,
    "comment_total": 0,
    "updated_at": "2026-05-28T08:07:51"
}
```

### 3.4 字段映射（→ Trending 卡片）

| HelloGitHub 字段 | Trending 卡片映射 | 说明 |
|-----------------|------------------|------|
| `full_name` | repo 标识，用于去重 / 匹配已有 starred | `"owner/name"` |
| `title` + `title_en` | 中文标题优先，英文兜底 | 替代 repo description 作为卡片副标题 |
| `summary` + `summary_en` | 详情摘要 | 比 description 更"人话" |
| `author` | 作者名 | 与 owner 不一定相同 |
| `author_avatar` | 头像 | 可替换 GitHub avatar |
| `primary_lang` + `lang_color` | 语言色 | 与现有 LanguageBadge 对齐 |
| `is_hot` | 🔥 标记 | 热门项目 badge |
| `clicks_total` | 热度参考 | 可替代 stars 排序 |
| `updated_at` | 时间排序 | ISO8601 |

### 3.5 功能条目

- [ ] **爬取 HelloGitHub API** — 分页拉取全部数据（当前 ~5 页），写入本地缓存表
  > **涉及**：新增 `HellogithubAPIClient`（URLSession + JSONDecoder）
  > **缓存**：复用 Trending 现有 `trending_repos` 表或新建 `hellogithub_items` 表（开工前拍板）
  > **去重**：以 `item_id` 为主键

- [ ] **Trending 列表混合展示** — HelloGitHub 项目与 GitHub Trending 项目在同一列表中，用来源 badge 区分
  > **来源标识**：卡片上加 `hellogithub` 来源 badge（参考 Weekly 的 `sourceTypes` 模式）
  > **排序**：默认按 `updated_at` 降序，或与 GitHub Trending repos 混合按时间排序

- [ ] **详情页适配** — 点击 HelloGitHub 项目进入详情时，优先展示 `title` + `summary`，README 走 GitHub API 懒加载

### 3.6 待讨论（开工前拍板）

| # | 问题 | 选项 |
|---|------|------|
| D1 | 存入现有 `trending_repos` 表还是独立表？ | A: 独立 `hellogithub_items`（隔离、好清理） B: 复用 `trending_repos`（统一列表、少改 UI） |
| D2 | 与 GitHub Trending 的排序关系？ | A: 混合按时间排序 B: 分区（HelloGitHub 在上，GitHub Trending 在下）C: 独立 Tab |
| D3 | 刷新频率？ | 跟 Trending 24h TTL 还是独立 TTL？HelloGitHub 月更，24h 刷新一次够用 |

---

## 4. 导出 Starred 到 GitHub Awesome 仓库

### 4.1 背景

分享页面已支持生成用户分享卡片（PNG 导出 / X 分享）。本功能是另一种"分享"：**将用户已 Star 的项目列表同步到自己的 GitHub 仓库，自动生成一份 Awesome 风格的 README**。

### 4.2 参考实现

GitHub Action [simonecorsi/mawesome](https://github.com/simonecorsi/mawesome) 提供了核心思路：

- 读取用户 GitHub Stars 列表
- 按语言或分类自动生成 `README.md`
- 输出为 Awesome 风格的项目清单
- 提交到指定仓库（通常为 `awesome-stars` 或同名仓库）

Starcat 在此基础上增强：利用本地已缓存的 Stars 数据 + 用户标签分类 + AI 摘要，生成比原始 mawesome 更丰富的内容。

### 4.3 功能条目

- [ ] **Awesome 导出配置** — 用户选择导出范围（全部 / 按标签筛选 / 按语言筛选）、排序方式（Star 数 / 最近更新 / 自定义排序）、输出仓库
  > **涉及**：新增 `AwesomeExportConfigView`（设置页），配置落本地 SQLite
  > **约束**：首次导出前必须已通过 GitHub OAuth 授权 `public_repo` 写入权限；若 scope 不足引导重新授权

- [ ] **README 生成引擎** — 按模板生成 `README.md`：包含分类目录、项目列表（名称 + 描述 + 语言 + Stars）、AI 摘要摘要、最后更新时间戳
  > **涉及**：新增 `AwesomeReadmeGenerator`（模板引擎，Swift 内实现）
  > **模板变量**：`{{category}}` `{{repo_name}}` `{{description}}` `{{stars}}` `{{language}}` `{{ai_summary}}` `{{last_updated}}`
  > **约束**：模板可内置默认 + 用户自定义；每类上限 500 条避免 README 过大

- [ ] **GitHub 推送** — 将生成的 README 提交到用户指定仓库（自动创建或覆盖 `README.md`），可选 PR 模式或直接 push
  > **涉及**：复用 `GitHubAPIClient` 的 Contents API（`PUT /repos/{owner}/{repo}/contents/{path}`）
  > **约束**：仓库不存在时自动创建（`POST /user/repos`）；首次使用引导确认目标仓库名（默认 `awesome-stars`）
  > **冲突策略**：默认覆盖 `README.md`，可选"追加到已有文件末尾"

- [ ] **定时同步（可选）** — 支持按周期（每周 / 每月）自动拉取最新 Stars 并更新 Awesome 仓库
  > **涉及**：`NSBackgroundActivityScheduler` + 本地变更检测
  > **约束**：必须在 Starcat 运行期间触发；不跑后台 silent push

### 4.4 与已有分享功能的边界

| 功能 | 输出 | 用途 |
|------|------|------|
| 分享卡片（已有） | PNG 图片 | 一次性展示，社交分享 |
| Awesome 导出（本功能） | GitHub README | 持续维护的公开项目清单，SEO / 社区影响力 |

---

## 5. Trending 推荐栏目（基于 GitHub Issues）

### 5.1 背景

阮一峰的《科技爱好者周刊》通过 GitHub Issues 管理推荐内容，读者通过 Issue 评论互动。Starcat 参考此模式，在 Trending 模块中新增一个「推荐」栏目：**dong4j 维护一个独立的 GitHub 仓库，以 Issues 形式发布项目推荐，Starcat 客户端拉取该仓库 Issues 列表并以内嵌详情页展示**。

### 5.2 数据流

```
dong4j 创建 Issue（推荐某个仓库）
    ↓
GitHub Issues API（GET /repos/{owner}/{repo}/issues）
    ↓
Starcat 客户端拉取 → 缓存 → 渲染推荐列表 + 详情
```

与 Weekly 模块的关键区别：

| 维度 | Weekly（已有） | 推荐栏目（本功能） |
|------|---------------|-------------------|
| 数据源 | 四个后端 API（聚合源文章） | 单一 GitHub 仓库 Issues |
| 详情展示 | WebView 渲染源文章 | **原生详情页**，展示 dong4j 写的推荐语 + 仓库元数据 |
| 内容归属 | 第三方作者 | dong4j 原创推荐 |

### 5.3 功能条目

- [ ] **推荐仓库配置** — dong4j 创建专用 GitHub 仓库（如 `starcat-recommendations`），以 Issues 承载推荐内容
  > **Issue 模板**（使用 GitHub Issue Template）：
  > ```markdown
  > ---
  > name: 项目推荐
  > about: 推荐一个 GitHub 项目
  > title: '[推荐] {项目名}'
  > labels: ['recommendation', '{类别标签}']
  > ---
  > 
  > **仓库地址**: https://github.com/{owner}/{repo}
  > **推荐语**: （为什么推荐这个项目？解决了什么问题？适合谁用？）
  > **标签**: （逗号分隔，如：效率工具、开发框架、AI）
  > ```

- [ ] **Trending 推荐列表** — 在 Trending 模块新增「推荐」分类，拉取 Issue 列表并在 Trending 列表中以卡片展示
  > **涉及**：`GitHubAPIClient.getRepoIssues(owner:repo:)` + 新增 `RecommendationRepository` 缓存层
  > **排序**：默认按 Issue 创建时间降序
  > **TTL**：24h 缓存，与 Trending 共用刷新周期
  > **列表卡片**：显示 Issue title（即项目名） + dong4j 写的首行推荐语 + 类别标签

- [ ] **推荐详情页** — 点击推荐卡片进入详情页，**不跳转 GitHub WebView**，而是在 Starcat 内原生渲染：
  > - 顶部：被推荐仓库的元数据（name / description / stars / language / license）——复用现有 `RepoMetadataHeaderView`
  > - 中部：**dong4j 的推荐语**（从 Issue body 解析，去除模板标记，保留纯文本 + Markdown）
  > - 底部：被推荐仓库的 AI 摘要（若本地已有缓存）或 README 摘要
  > **涉及**：新增 `RecommendationDetailView`，复用 `RepoDetailView` 的 header 组件
  > **约束**：Issue body 解析需处理模板格式；表情 / 图片保留 Markdown 渲染

- [ ] **Issue 互动（可选，后期）** — 支持在 Starcat 内查看 Issue 评论区、点赞等
  > **约束**：v2 首版不做评论；详情页底部放"在 GitHub 查看"链接即可

### 5.4 待讨论（开工前拍板）

| # | 问题 | 选项 |
|---|------|------|
| D1 | 推荐仓库名？ | A: `starcat-recommendations` B: `starcat-weekly`（与定位对齐） |
| D2 | Issue 正文是纯推荐语，还是包含结构化字段？ | A: 纯 Markdown 推荐语（灵活） B: YAML front matter + 推荐语（好解析） |
| D3 | 推荐栏目在 Trending 中的位置？ | A: 独立 Segment（`Popular` \| `Recommend` \| `HelloGitHub`） B: 与 GitHub Trending 混合排序（带 badge 区分） |
| D4 | 详情页中推荐仓库的 README 是否需要懒加载？ | A: 默认不加载，点按钮手动加载 B: 自动加载第一屏 |

---

## 6. 变更日志

| 日期 | 变更 |
|------|------|
| 2026-06-17 | 初版：§1 详情页 Hero 四项从 v1 延期写入；含布局方案与 toolbar 分工 |
| 2026-06-17 | 新增 §3：HelloGitHub 数据源接入（Trending 扩展），含 API 字段映射 + 3 个待拍板决策 |
| 2026-06-19 | 新增 §4：导出 Starred 到 GitHub Awesome 仓库（基于 mawesome 思路增强） |
| 2026-06-19 | 新增 §5：Trending 推荐栏目（基于 GitHub Issues 的原创推荐，原生详情页渲染） |

---

*维护者：dong4j。v2 功能落地时须同步更新 `docs/功能实现总览.md` 对应 checkbox + `> 实现：...` 行。*

# 
