# 国际化规范 (i18n)

> **强制**
> 来源：早期从根协作规范沉淀到本目录；现行维护源为根 `AGENTS.md`。`AGENTS.md` 只保留引用链接，不重复正文。
> 单一信任源:`docs/5-规范/i18n-军规.md`(原 `docs/5-规范/i18n-军规.md`,2026-06-28 重整时改用 kebab-case 命名)。

---

## 最小必知(违反任一项即不合规)

1. **新代码禁止** `String(localized:)` 与 `NSLocalizedString`。返回 `String` 的本地化场景一律走 `String.l10n("key")`。
2. SwiftUI `Text` / `Label` / `Button` 等接 `LocalizedStringKey` 的 view **直接写** `Text("key")`,不要套 `String.l10n`。
3. **每个** `.sheet { }` / `.popover { }` 闭包内根视图、**每个**自建 `NSWindow` / `NSPanel` 的 hostingView 根,**必须**挂 `.appLocaleEnvironment()`。
4. 任何 Foundation formatter(`RelativeDateTimeFormatter` / `Date.RelativeFormatStyle` / `DateFormatter` / `NumberFormatter`)默认走系统 locale,**必须**视图内 `@Environment(\.locale)` + `formatter.locale = locale`(或链式 `.locale(locale)`)显式注入。
5. 字符串目录:`Starcat/Resources/Localizable.xcstrings`,新增 key **必须**同时填 en + zh-Hans 双语,命名 `{section}.{subsection}.{component}`,禁用 `_`。
6. 手工编辑 `Localizable.xcstrings` **必须**保持 Xcode 排版：JSON 键后冒号左右各一个空格，即 `"key" : value`；只做局部修改，禁止使用 formatter 或序列化脚本重写整个文件。
7. **改 Catalog 只约束写回方式**：禁止 StrReplace / ApplyPatch（会截断）和任何 JSON 重排写回。允许 Xcode 手加，或对相邻唯一 key 做不解析 JSON 的按行插入；`git diff --stat` 只能新增、`wc -l` 不得变少。失败则 `git checkout --` 该文件。详见 [`i18n-军规.md`](i18n-军规.md) 铁律 #6。

## 提交前自检

```bash
rg "String\(localized:" --type swift Starcat/   # 必须只出现在注释里
rg "NSLocalizedString"  --type swift Starcat/   # 必须只出现在注释里
rg -n '^[[:space:]]*"[^"]+":' Starcat/Resources/Localizable.xcstrings      # 冒号左侧缺空格；必须无输出
rg -n '^[[:space:]]*"[^"]+" :[^ ]' Starcat/Resources/Localizable.xcstrings # 冒号右侧缺空格；必须无输出
jq empty Starcat/Resources/Localizable.xcstrings                             # 只校验语法，不覆写文件
git diff --stat -- Starcat/Resources/Localizable.xcstrings                   # 检查是否误伤整个目录
```

## 详细规则

> 决策矩阵 / 5 大 root cause / 反例对照 / 实战参考文件 → 见 [`docs/5-规范/i18n-军规.md`](i18n-军规.md)。
