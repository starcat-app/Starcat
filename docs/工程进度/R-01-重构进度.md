# R-01 三场景共用架构 重构进度看板

> 创建：2026-06-09
> 状态：进行中（feature/main 分支）
> 设计依据：`docs/详细设计/18-三场景共用架构.md` v1.2
> 主进度索引：`docs/工程进度/功能实现总览.md` §6.6 R-01

---

## 0. 本次重构边界（dong4j 2026-06-09 拍板）

| 维度 | 决策 |
|---|---|
| 范围 | **仅前端 R-01**（`docs/详细设计/18-三场景共用架构.md` Step 2-8）；后端 3 个 Go API 由其他人在 supports/ 各独立仓库内完成 |
| 后端不可用时本地策略 | **严格按 v1.2 schema 写**（不加临时兼容层）；trending / weekly 在后端切到 `/api/v1/*` + envelope 之前真机跑不通；**单测必须全绿** |
| 单测覆盖 | **关键路径测试**——StarringSubsystem 写权限路径 / RepoResolver chain 命中顺序 / DTO schema_version 解码 / RepoCardViewData 转换正确性 / StarActionService star+unstar；预计新增 ~20 项 |
| 提交粒度 | **按可独立编译 + 跑测的最小单元**（~30-40 commit），每个 commit 通过 `xcodegen generate && xcodebuild test` |
| Manage unstar confirm | **严格按设计删**（`showUnstarConfirm` / `isUnstarring` / `unstarError` 全删，统一无 confirm） |
| 分支 / Push | `feature/main`，**全部本地提交，不 push** |

---

## 1. 子任务清单（按 Step → 子任务 → commit 粒度）

> **每个 commit 完成后**：① 把对应子任务的 `[ ]` 改成 `[x]` + 行末加 commit hash + 完成时间 ② 在「3. 实现日志」追加一行说明。
>
> **每个 Step 全部完成后**：在「功能实现总览.md」对应 R-01.N 子条目打勾 + 写 `> 实现：...` 行。

### Step 2: 前端数据基础设施（无外部依赖，可立即开工）

- [x] **2.1** 新建 `Core/Network/StarcatRepoCardDTO.swift` — **2026-06-09 18:08 完成**
  - 含 `StarcatRepoCardResponse { schema_version, data }` envelope
  - 含 `StarcatRepoCardDTO` 主体 + `TrendingExtension` / `WeeklyExtension` 扩展段
  - 顶部加 §6.1.4 硬边界规则注释（核心字段仅 GitHub Repo metadata 原生语义；Article 类字段另起 endpoint）
  - 含 `toEphemeralRepo()` 转换为 `Repo` 的扩展（暂时只构造 in-memory，不落 DB）
  - 单测：`StarcatRepoCardDTOTests` 9 项（schema 1 解码 / weekly 扩展段 / schema 2 兼容警告 / 缺扩展段 / gh_repo_id 缺失抛错 / schema_version 缺失抛错 / 字段映射 / fallback htmlUrl / 空 topics）
  > 实现：DTO 字段层级严格按 §6.1 设计；`isSupported = schemaVersion <= supportedSchemaVersion(=1)` 双保险；`toEphemeralRepo()` 永远写 `isStarred = false`，由 StarredRegistry 在 view data 转换层覆盖真实状态。新增 `Starcat/Core/Network/StarcatRepoCardDTO.swift` (417 行) + `StarcatTests/StarcatRepoCardDTOTests.swift` (9 测试)。340 / 340 全绿（331 基线 + 9 新增）。

- [x] **2.2** 新建 `Core/Sync/StarringSubsystem.swift`（**单文件**：Registry + Service + Bootstrapper） — **2026-06-09 18:24 完成**
  - `StarredRegistry`：`@MainActor @Observable final class`，`private(set) var ids: Set<Int64>`，写方法 `_add/_remove/_replace` 全部 `fileprivate`
  - `StarActionService`：`@MainActor final class`，依赖 `apiClient / repoRepository / registry / userIDProvider: () -> Int64? / homeRefresher: HomeRefreshing?`；提供 `star(owner:repo:) -> Repo` / `unstar(repo:)`
  - `StarredRegistryBootstrapper`：`reload()` / `clearOnSignOut()`
  - 文件头大段注释：解释「为什么三类型放一个文件 = fileprivate 锁死写入路径唯一」+ Snapshot 扩展点占位（starred > 5000 触发）
  - 协议 `HomeRefreshing` 同文件定义（弱引用注入到 service）
  - 同步给 `RepoRepositoryProtocol` 加两个新方法：`fetchStarredRepoIDs() -> [Int64]` / `upsertSingleStarred(repoDTO:starredAt:userID:syncedAt:) -> Repo`
  - i18n 新增 `starAction.error.notAuthenticated`（zh-Hans + en）
  - 单测：`StarringSubsystemTests` 8 项（registry contains O(1) / star 流程 / unstar 保留私有数据 / bootstrapper.reload / clearOnSignOut / star API 失败 registry 不变 / 未登录抛 .notAuthenticated（star 与 unstar 各一））
  > 实现：v1.2 §4.3 严格按「单文件 subsystem + fileprivate 锁死」落地。`StarActionService` 用 `userIDProvider: () -> Int64?` 闭包注入避免直接持有 AuthSession（便于单测）。`upsertSingleStarred` 复用现有 `repoFromDTO` 静态工厂 + StarredRepo 行写入。348/348 全绿（基线 + 9 + 8）。

- [x] **2.3** 新建 `Shared/Models/RepoCardViewData.swift` — **2026-06-09 18:37 完成**
  - `RepoCardViewData: Identifiable, Hashable, Sendable`，`var id: Int64 { ghRepoId }`
  - `CardBadge` enum: `.trendingChange(Int)` / `.weeklyIssue(Int)` / `.activityKind(ActivityCategory, Date)`
  - `extension Repo { func asCardData(badge:) -> RepoCardViewData }`
  - `extension StarcatRepoCardDTO { func asCardData(registry:badge:) -> RepoCardViewData }`（@MainActor 因为 registry 是 @MainActor）
  - 单测：`RepoCardViewDataTests` 6 项（Repo 转 / Repo+badge / DTO+registry 命中 / DTO+registry 未命中 / DTO+trendingChange badge / id 稳定性 rename 不变）
  > 实现：v1.1 决策严格落地——id 直接是 `Int64 (ghRepoId)`，不引入 CardID 枚举。`Repo.asCardData` 直接读 `self.isStarred`；DTO 版本通过 `registry.contains` 查询。354/354 全绿。

- [x] **2.4** 新建 `Shared/Models/RepoDetailViewData.swift` — **2026-06-09 18:37 完成**（与 2.3 合并提交）
  - `RepoDetailHero`（含 ghRepoId Int64? / avatar URL? / fullName / desc / stars / forks / watchers / subscribers / topics / license / 三时间字段已解析为 Date? / archived / fork / private / isStarred / htmlUrl / homepage）
  - `RepoDetailAction` enum: `.share(handler:)` / `.ai(handler:)` / `.weeklyIssue(number:url:)` / `.custom(id:label:systemImage:handler:)`
  - `RepoDetailViewData { hero, trailingActions, translation, backendHint }`
  - 占位 `ReadmeTranslationContext` struct（Step 4.3 实现 Scaffold 时填充字段）
  - 无单测（纯类型定义，§3.2.2 数据结构）
  > 实现：严格遵守 v1.1「**没有 DetailSections 配置**」约束，避免 god view 腐烂路径——「该不该显示 tags 段」由各 ContentView 自己判断 `resolution.isLocalHit`。

### Step 3: 前端服务层（依赖 Step 2 完成）

- [x] **3.1** 新建 `Core/Sync/RepoSource.swift` — **2026-06-09 18:52 完成**
  - 协议 `RepoSource: Sendable { var name: String; func tryResolve(...) async throws -> Repo? }`

- [x] **3.2** 新建 5 个 RepoSource 实现（`Core/Sync/RepoSources/` 子目录） — **2026-06-09 18:52 完成**
  - `LocalRepoSource.swift`（查 `RepoRepository.findByOwnerName`）
  - `BackendHintRepoSource.swift`（hint DTO → ephemeral Repo，含 owner/repo case-insensitive 匹配防御 = 容忍大小写差异）
  - `BackendAggregateRepoSource.swift`（占位实现：永远返回 nil，待后端单 repo 聚合 endpoint 上线后回填，标 P2）
  - `GitHubFallbackRepoSource.swift`（`GitHubAPIClient.repo(owner:repo:)` → ephemeral Repo）
  - `MinimalRepoSource.swift`（`Repo.makeMinimal(owner:name:hint:)` 永远命中）
  - 同步给 `Repo` 加 `static func makeMinimal(owner:name:hint:)` 工厂

- [x] **3.3** 新建 `Core/Sync/RepoResolver.swift` — **2026-06-09 18:52 完成**
  - `RepoResolver { let chain: [any RepoSource] }`，@MainActor
  - `RepoResolution { sourceName, repo, isLocalHit, isMinimal }`
  - `resolve(owner:name:hint:) async -> RepoResolution`，遍历 chain，命中或单源 throws 都用 `AppLog.sync` 打日志
  - 兜底：理论不可达的 chain 全 fail 路径用 `MinimalRepoSource (forced)` 直接构造，避免 force unwrap 崩溃

- [x] **3.4** `App/AppDependencies.swift` 装配 + 钩子注入 — **2026-06-09 18:52 完成**
  - 装配 `starredRegistry / starActionService / starredRegistryBootstrapper / repoResolver`
  - SyncManager 加 `onSyncCompleted: () async -> Void` 字段，AppDependencies 注入「同步完成 → bootstrapper.reload」
  - AuthSession 加 `onSignOut: () -> Void` 字段，AppDependencies 注入「登出 / 401 失效 → bootstrapper.clearOnSignOut」
  - 启动期异步 Task 调一次 `bootstrapper.reload()`（test host 跳过）
  - 单测：`RepoResolverChainTests` 6 项（LocalRepoSource 命中停止询问 / Hint case-insensitive 命中 / Hint 真不一致跳过 / GitHub source throws 跳到 MinimalRepoSource 兜底 / 仅 MinimalRepoSource chain 永远命中）

  > 实现：5 source + Resolver 严格按 §5.2 chain 顺序装配。`onSyncCompleted` / `onSignOut` 是 var 而非构造器参数（避免双向构造依赖）。HomeView 在 Step 6 接入时通过 `starActionService.attachHomeRefresher(homeViewModel)` 挂接 refresher。360/360 全绿。

### Step 4: 前端 UI 通用层（依赖 Step 2 完成；Step 3 可并行）

- [x] **4.1** 新建 `Shared/Components/RepoRowSurface.swift`（统一卡片容器，解决 D-17） — **2026-06-09 19:05 完成**
  - 替代 `RepoRowSurface` (RepoRowView.swift) + `TrendingRepoRowSurface` (TrendingRepoRowView.swift) + `ActivityRowSurface` (ActivityView.swift) + `WeeklyProjectRow` 内嵌容器
  - 接受 `isSelected: Bool` + `accentColor: Color?` + `@ViewBuilder content`
  - 选中态：3pt 语言色 accent bar + 0.18 accent 底 + 0.42 accent 边框
  - hover：0.08 accent 底 + 0.18 accent 边框

- [x] **4.2** 新建 `Shared/Components/UnifiedRepoRow.swift`（统一卡片视图，仅 card 密度） — **2026-06-09 19:05 完成**
  - 入参 `RepoCardViewData`
  - 头像 40pt / 已 star ✓ 标记（systemGreen 11pt，紧贴 fullName 左侧 4pt）
  - 描述 2 行截断
  - chip 行：Lang → Stars → Forks → 场景独有 chip（依 `badge` 派发渲染）
  - 不渲染 star/unstar 按钮（设计 §3.1.6）

- [x] **4.3** 新建 `Shared/Components/RepoDetailScaffold.swift`（详情页通用骨架） — **2026-06-09 19:05 完成**
  - 入参 `repo: Repo` + `viewData: RepoDetailViewData` + `onStarTapped` + `@ViewBuilder body`
  - Hero header 复用 `RepoMetadataHeaderView`（含 stats chips 内的 ⭐/☆ chip 触发 star/unstar）
  - trailing actions 派发：`.share` → `RepoShareButton(repo:)` / `.ai` → `RepoAIOpenButton(repo:)` / `.weeklyIssue` → 紫色 capsule Link / `.custom` → 通用 icon button
  - 折叠面板（`CollapsibleRepoMetadataPanel` 复用）
  - 翻译 / 刷新浮动按钮：保留在 ReadmeStateView 内嵌 cacheFooter（Step 5 接入时由 ContentView 自带，Scaffold 不重复渲染，避免 layout 冲突）
  - body slot：ContentView 自由渲染 ScrollView + sections + Readme，符合「Scaffold 不带 sections 配置避免 god view」决策
  - **enum 调整**：`.share(handler:)` / `.ai(handler:)` 改成无参 `.share` / `.ai`（业务行为对所有 repo 一致，由现有 `RepoShareButton` / `RepoAIOpenButton` 自治；handler 注入路径已走不通——会双重渲染按钮）
  > 实现：保持设计 v1.2 §3.2.2 入参契约（trailingActions / translation / backendHint）但实现层面采取务实策略——hero 直接复用 `RepoMetadataHeaderView`（含 R-01 §3.2.4 视觉规则）；share / ai 直接复用现有 button 组件；翻译和刷新由 ReadmeStateView footer 内嵌（Step 5 ContentView 渲染 ReadmeStateView 时自带）。新增 `Starcat/Shared/Components/RepoRowSurface.swift` + `Starcat/Shared/Components/UnifiedRepoRow.swift` + `Starcat/Shared/Components/RepoDetailScaffold.swift`；`Starcat/Features/Home/RepoRowView.swift` 内 `RepoRowSurface` rename → `LegacyRepoRowSurface` 解决重名。`RepoDetailViewData.swift` `.share` / `.ai` 改无参。360/360 全绿。

### Step 5: 各场景 ContentView 插槽（依赖 Step 2/3/4 完成）

- [ ] **5.1** 新建 `Features/Home/ManageDetailContent.swift`
  - sections：`RepoTagsSection` → `RepoNotesSection` → `RepoReleaseSection` → `ReadmeSection`

- [ ] **5.2** 新建 `Features/Trending/TrendingDetailContent.swift`
  - 入参 `dto: StarcatRepoCardDTO` + `@State resolution: RepoResolver.Resolution?`
  - 已 star（`resolution.isLocalHit`）才显示 Tags/Notes/Release 三段
  - `TrendingContributorsSection`（独有）
  - ReadmeSection
  - 三段 spring 0.25s 展开动画

- [ ] **5.3** 新建 `Features/Activity/WeeklyDetailContent.swift`
  - 入参 `dto: StarcatRepoCardDTO` + `@State resolution`
  - 已 star 才显示三段；不显示 trending 独有；ReadmeSection

- [ ] **5.4** 新建 `Features/Activity/ActivityRepoDetailContent.swift`
  - 入参 `repo: Repo`（已 star，从本地）
  - 三段 + ReadmeSection（与 ManageDetailContent 视觉一致，未来加 timeline 段时此处独立扩展）

### Step 6: 场景层替换（依赖 Step 5 完成）

- [ ] **6.1** **Manage** 场景接入
  - `RepoListView` row 渲染改 `UnifiedRepoRow`（删除原 `RepoRowView` 引用）
  - `RepoDetailView` 拆成 `RepoDetailScaffold` + `ManageDetailContent`
  - `HomeViewModel.performStar/performUnstar` 改调 `StarActionService`
  - 删除 `showUnstarConfirm` / `isUnstarring` / `unstarError` 三个 @State + 对应 alert（设计 §3.2.3）

- [ ] **6.2** **Trending** 场景接入
  - `TrendingView` row 改 `UnifiedRepoRow`
  - 详情改 `RepoDetailScaffold` + `TrendingDetailContent`
  - 删除 `TrendingViewModel.subscribedRepoIDs` / `subscribe(fullName:)` / `incrementStarsCount(fullName:)`
  - `TrendingViewModel` 改吃 `StarcatRepoCardDTO`（旧 `TrendingResponseDTO` → 新 envelope）

- [ ] **6.3** **Weekly** 场景接入
  - `WeeklyContentView` 内嵌 `WeeklyProjectRow` 改 `UnifiedRepoRow`
  - `WeeklyDetailView` 改 `RepoDetailScaffold` + `WeeklyDetailContent`
  - 「第 N 期」按钮改通过 `RepoDetailAction.weeklyIssue` 注入 `trailingActions`
  - `WeeklyAPI` 改吃新 envelope schema

- [ ] **6.4** **Activity-repo-backed** 场景接入
  - `ActivityView` row 改 `UnifiedRepoRow`（含 kind icon badge）
  - `ActivityDetailView` repo-backed 分支改 `RepoDetailScaffold` + `ActivityRepoDetailContent`
  - 非 repo-backed 分支（announcement/release/following）保持现状

### Step 7: 删除遗留（依赖 Step 6 完成）

- [ ] **7.1** 删除 compact 密度
  - `Core/Settings/AppSettings.swift` 删 `listDensity` 字段 + 持久化 key
  - `Features/Settings/SettingsView.swift` 删 ListDensity Picker UI
  - `RepoListDensity` 枚举删除（连 `.card` 单 case 也不留）
  - `RepoRowSkeletonView` 删 compact 分支
  - `Localizable.xcstrings` 删相关键
  - 已勾选 §3.1.1 强制要求

- [ ] **7.2** 删除遗留文件 + 5 处 star/unstar 旧实现
  - 删除文件：`Features/Home/RepoRowView.swift` / `Features/Trending/TrendingRepoRowView.swift`
  - 删 `RepoDetailView.swift` 中 Trending 分支（`trendingMetadataHeader` / `starTrending` / `performUnstar(repo:)`）
  - 删 `ActivityDetailView.swift` 中 `performUnstar(repo:)`
  - 删 `WeeklyDetailView.swift` 中 `performUnstar(repo:)` / `performStar(repo:)`

- [ ] **7.3** 替换 Activity / Weekly 自写刷新动画为 `SyncIconButton`
  - 通过 `RepoDetailScaffold.onRefresh` 入参传入

### Step 8: AI 窗口规则改造

- [ ] **8.1** AI 窗口打开瞬间冻结 star 状态
  - `RepoAIWindowContentView` 在 init / onAppear 时**捕获一次** `registry.contains(ghRepoId:)`
  - 本轮窗口打开期间**不响应** `registry.ids` 的变化
  - 未 star 时**不渲染**标签段，**不发**标签生成请求
  - 关闭窗口再重新打开时按新状态重新决定（设计 §3.2.7）

---

## 2. 依赖图

```
Step 2.1 DTO ──┐
Step 2.2 Subsystem ─┐
Step 2.3 CardViewData ─┼─→ Step 4.x UI ─→ Step 5.x ContentView ─→ Step 6.x 场景接入 ─→ Step 7.x 删除遗留
Step 2.4 DetailViewData ─┘
                    │
Step 3.1 Source 协议 ──→ Step 3.2 5 source ──→ Step 3.3 Resolver ──→ Step 3.4 装配 ─→ Step 6.x 接入

Step 8.1 AI 窗口（独立，不依赖其他 Step；建议 Step 6 完成后做）
```

并行机会：Step 2.x 之间无依赖可并行；Step 3 与 Step 4 可并行（依赖只到 Step 2）；Step 5 必须等 Step 2/3/4。

---

## 3. 实现日志（逆序最新在前）

> 格式：`<日期 时间> [Step N.M] <commit hash> <一句话>`
> 完成后追加 `> 实现：...`

- 2026-06-09 19:05 [Step 4.1+4.2+4.3] RepoRowSurface + UnifiedRepoRow + RepoDetailScaffold 三件套通用 UI 层（360 全绿）
  > 实现：Surface 接 isSelected + accentColor + content slot 解决 D-17；UnifiedRepoRow 吃 RepoCardViewData 派发 badge；Scaffold 复用现有 RepoMetadataHeaderView/RepoShareButton/RepoAIOpenButton/CollapsibleRepoMetadataPanel 避免重写。enum `.share/.ai` 改无参（行为通用化）。RepoRowView.swift 旧 `RepoRowSurface` rename → `LegacyRepoRowSurface` 暂存到 Step 7.2 删除。
- 2026-06-09 18:52 [Step 3.1+3.2+3.3+3.4] RepoSource chain + RepoResolver + AppDependencies 装配（360 全绿）
  > 实现：5 source 严格按设计顺序，Repo.makeMinimal 工厂兜底；SyncManager / AuthSession 加 hook 闭包让 bootstrapper 自动同步 registry。
- 2026-06-09 18:37 [Step 2.3+2.4] RepoCardViewData + RepoDetailViewData/Hero/Action 适配层（354 全绿）
  > 实现：id = Int64 (ghRepoId) 严格落地避免 rename 漂移；Detail 适配层不带 sections 配置避免 god view 腐烂。
- 2026-06-09 18:24 [Step 2.2] StarringSubsystem 单文件 + fileprivate 锁死 + 8 项关键路径测试（348 全绿）
  > 实现：StarredRegistry + StarActionService + Bootstrapper 三类型同文件强制写权限唯一。RepoRepositoryProtocol 加 fetchStarredRepoIDs / upsertSingleStarred 两个 R-01 用方法。HomeRefreshing 协议同文件定义。i18n 加 starAction.error.notAuthenticated。
- 2026-06-09 18:08 [Step 2.1] StarcatRepoCardDTO + envelope + 9 项关键路径测试（340 全绿）
  > 实现：v1.2 §6.1 schema 严格落地 — envelope 顶层 `schema_version` + DTO 主体 + 扩展段硬边界注释 + ephemeral Repo 转换。
- 2026-06-09 15:00 [Step 0] 建立 R-01 进度看板 + 在功能实现总览拆 R-01.1~R-01.8 子条目

---

## 4. 已知风险 / 临时绕过

| ID | 描述 | 状态 |
|---|---|---|
| RISK-1 | 后端 `/api/v1/repos`、`/api/v1/projects` 未上线，trending/weekly 真机暂时跑不通；单测用 fixture 覆盖 | 已知接受（dong4j Q2-A） |
| RISK-2 | `BackendAggregateRepoSource` R-01 内永远返回 nil（占位），等后端单 repo 聚合 endpoint 上线后回填实现 | P2 待跟进 |
| RISK-3 | StarredRegistry 启动期全量 SELECT 在 starred > 5000 时可能 > 30ms 阻塞首屏；本次留 Snapshot 扩展点注释 | P2 阈值后做（§4.3.2 已规划方案） |

---

## 5. 完成标志

- [ ] 8 个 Step 全部完成
- [ ] `xcodebuild test` 全绿（331 + 关键路径新增 ~20 项 = ~351 项）
- [ ] 「功能实现总览.md」§6.6 R-01 主条目打勾 + 写 `> 实现：...`
- [ ] 「功能实现总览.md」§6.6 R-01.1 ~ R-01.8 子条目全部打勾 + 各自 `> 实现：...`
- [ ] D-17（RepoRowSurface 抽公共）作为子集自然结清

---

*最后更新：2026-06-09 19:05（Step 4 全部完成）*
