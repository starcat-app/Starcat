# 开发前问题清单

> 本文档基于对 `docs/` 目录下全部文档的系统审查，列出开发启动前需要明确和解决的问题。
>
> 审查范围：需求分析、功能清单、概要设计、调研报告、详细设计（6 篇）、CloudKit 设计、AI 代理 API 设计、GitHub OAuth 设计、UI 原型 Skill，共 15 份文档。

---

## 一、🔴 阻断性问题（已解决）

### 1.1 平台版本假设过于激进

**问题**：所有文档将最低系统要求定为 **macOS 26 (Tahoe)**，并深度依赖 Liquid Glass API。

**解决方案**：采用 **macOS 15 Sequoia** 作为最低版本。

```
┌─────────────────────────────────────────────────────────────┐
│                    兼容性策略                                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  最低支持：macOS 15 Sequoia                            │
│  目标支持：macOS 26 Tahoe（如果有 Liquid Glass）         │
│                                                              │
│  实现方式：                                                │
│  1. 用 @available(macOS 26, *) 检测                    │
│  2. Sequoia：用 .material(.ultraThinMaterial) 代替      │
│  3. Tahoe：用 .glassEffect() 优先                        │
│  4. 抽象出 GlassStyle 协议，统一调用                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**已更新**：`04-技术选型.md`、`概要设计.md`

---

### 1.2 数据库循环外键依赖

**问题**：`repos.id → starred_repos.repo_id` 形成循环外键，SQLite 无法创建。

**解决方案**：移除 `repos.id → starred_repos.repo_id` 这个不合理的外键。

```sql
-- repos 表：移除循环外键
CREATE TABLE repos (
    id TEXT PRIMARY KEY,                    -- GitHub repo ID (唯一标识)
    ...
    cached_at TEXT                          -- 移除：FOREIGN KEY (id) REFERENCES starred_repos
);

-- starred_repos 表保持
CREATE TABLE starred_repos (
    repo_id TEXT PRIMARY KEY,             -- 关联 repos.id
    ...
    FOREIGN KEY (repo_id) REFERENCES repos(id) ON DELETE CASCADE
);
```

**已更新**：`01-数据库设计.md`

---

### 1.3 OAuth 权限范围与功能矛盾

**问题**：`public_repo` scope 是否支持删除 Star 操作？

**解决方案**：经查证 GitHub 文档，`public_repo` scope **已包含**对公开仓库的 star/unstar 操作权限。

```swift
// GitHub OAuth Scope
let scopes = ["read:user", "public_repo"]
```

> **说明**：GitHub 官方文档明确指出 `public_repo` scope "Also required for starring public repositories"，即已包含 star/unstar 权限。

**已更新**：`GitHub OAuth 设计.md`

---

## 二、🟡 架构级不一致（已解决）

### 2.1 README 渲染方案

**问题**：文档中有的说 WebView，有的说原生 Down/_markdown，无法统一。

**解决方案**：统一使用 **WebView 渲染**，保证 100% GFM 兼容。

```
┌─────────────────────────────────────────────────────────────┐
│                    README 渲染策略                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  WebView 渲染 GitHub 风格 Markdown：                       │
│  ├── 复杂表格 ✅                                         │
│  ├── 代码块高亮 ✅                                       │
│  ├── Task list ✅                                         │
│  ├── Emoji shortcut ✅                                    │
│  └── HTML 混排 ✅                                        │
│                                                              │
│  方案： WKWebView + GitHub CSS 样式                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**已更新**：`04-技术选型.md`、`概要设计.md`

---

### 2.2 语义搜索架构

**问题**：SQLite 原生不提供项目可直接依赖的向量相似度能力，同时 Starcat 已明确采用本地优先、BYOK、不自建 AI 代理的产品路线。

**最终方案（2026-06-13 按当前实现修订）**：embedding 由用户配置的 OpenAI-compatible Provider 生成，repo 向量缓存在本地 SQLite；搜索时只生成 query vector，并在 Swift 内存中计算 cosine similarity 和 Top-K。第一阶段不引入 pgvector、sqlite-vss 或 Starcat 自建语义搜索服务。

```
┌─────────────────────────────────────────────────────────────┐
│                    语义搜索架构                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Starcat 本地：                                             │
│  ├── 存 repo 向量与 IndexedSnapshot（SQLite）               │
│  ├── 生成 query vector 后在 Swift 内存计算 cosine            │
│  └── 排序并展示 Top-K                                       │
│                                                              │
│  用户选择的 Embedding Provider：                            │
│  └── 仅负责生成 repo / query embedding                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**已更新**：相关文档保持一致

---

### 2.3 Swift 版本与代码风格

**问题**：`04-技术选型.md` 要求 Swift 6.0，但代码示例用的是 ObservableObject。

**解决方案**：使用 **Swift 6 语言模式** + **@Observable**。

| 项目 | 版本 | 说明 |
|------|------|------|
| 编译器 | Swift 6.0 | Xcode 16 内置 |
| 语言模式 | Swift 6 | 启用 Swift 6 数据竞争检查 |
| 状态管理 | @Observable | 继续使用 Observation，避免回退 ObservableObject |

> **说明**：2026-06-29 已切到 `SWIFT_VERSION = 6.0` 与 `SWIFT_STRICT_CONCURRENCY = complete`，并完成首轮 Swift 6 编译修复。

**已更新**：`04-技术选型.md`、`06-核心模块设计.md`、`03-项目结构设计.md`

---

### 2.4 增量同步没有真实方案

**问题**：GitHub Stars API 不支持增量查询，文档中的"增量同步"实际是全量拉取。

**解决方案**：定期全量对比 + **手动刷新**功能。

```
┌─────────────────────────────────────────────────────────────┐
│                    同步策略                                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  首次同步：全量拉取，存 last_sync_at                    │
│                                                              │
│  增量同步：                                                │
│  ├── 按 starred_at DESC 排序，只拉前 N 条（N = 你 star 数）│
│  ├── 对比本地数据，更新新增的                           │
│  └── 检测删除（本地有 remote 没有）→ 软删除           │
│                                                              │
│  手动刷新：                                                │
│  └── 用户主动触发完整同步                              │
│                                                              │
│  定期全量同步（如每周一次）：                             │
│  └── 凌晨低峰期做，检测"取消 star 但本地还显示"        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**已更新**：`05-GitHub API设计.md`

---

### 2.5 AI 功能优先级矛盾

**问题**：CLAUDE.md 说 MVP 含 AI，但功能清单说 AI 是 P1。

**解决方案**：MVP **不含 AI**（Release 订阅移到 P1），第一版纯本地管理工具。

| 版本 | 内容 |
|------|------|
| v1.0（4-5 个月后） | 纯本地管理工具 |
| v1.1（1-2 个月后） | AI 摘要/标签（BYOK） |
| v1.2 | 语义搜索、自建服务 |

**已更新**：`概要设计.md`、`功能清单.md`

---

## 三、🟠 关键设计缺失（已补充）

### 3.1 Release 订阅通知

**问题**：功能清单中 Release 订阅追踪是 P2，但用户要求必须做。

**解决方案**：Release 订阅移到 **P1**，使用轮询方案监测。

```swift
/// Release 发布通知监测管理器
/// 使用 NSBackgroundActivityScheduler 在 macOS 后台定期执行（建议每 4-6 小时一次）
@MainActor
@Observable
class ReleaseMonitor {
    var newReleases: [ReleaseNotification] = []

    /// 检测所有订阅的仓库是否有新 Release
    func checkForNewReleases() async -> [ReleaseNotification] {
        let subscriptions = database.getActiveSubscriptions()
        var notifications: [ReleaseNotification] = []

        for subscription in subscriptions {
            if let notification = await checkRepo(subscription) {
                notifications.append(notification)
            }
        }

        return notifications
    }
}
```

**已更新**：`06-核心模块设计.md`、`功能清单.md`、`CloudKit数据同步设计.md`

---

## 四、🟢 产品决策（已确认）

### 4.1 自建服务 vs BYOK

**决定**：BYOK 需要 **Pro 订阅**。

| 模式 | 要求 | 说明 |
|------|------|------|
| 免费版 | 无 | 基础管理（同步、标签、搜索、笔记） |
| Pro | $29.99/年 或 $79.99 买断 | AI 功能 + Release 通知 + 云端同步 |
| BYOK | Pro 订阅 | 用户自行提供 API Key，节省 AI 费用 |

**已更新**：`功能清单.md`、`概要设计.md`

---

### 4.2 多平台

**决定**：watchOS **待定**（价值存疑）。

| 平台 | 状态 | 说明 |
|------|------|------|
| macOS | ✅ 优先 | 主力生产力端 |
| iOS/iPadOS | 后续 | 响应式布局适配 |
| watchOS | ❓ 待定 | 轻量 companion 价值存疑 |

**已更新**：`概要设计.md`、`功能清单.md`

---

### 4.3 定价

**决定**：

| 方案 | 价格 | 功能 |
|------|------|------|
| 免费版 | $0 | 基础管理（同步、标签、搜索、笔记） |
| Pro 订阅 | $29.99/年 或 $79.99 买断 | AI 功能 + Release 订阅通知 + 云端同步 |
| BYOK 模式 | 需要 Pro 订阅 | 用户自行提供 API Key，节省 AI 调用费用 |

**已更新**：`功能清单.md`、`概要设计.md`

---

### 4.4 双分发渠道

**决定（2026-06-27）**：Starcat 支持 App Store 与 Direct 两种分发渠道，但支付 / 授权机制分离。

| 渠道 | 分发 | 支付 / 授权 | 约束 |
|------|------|-------------|------|
| App Store | Mac App Store | StoreKit 2 / Apple IAP | App Store build 不展示 Lemon Squeezy、授权码或外部购买入口 |
| Direct | 官网 DMG | Lemon Squeezy / License Key | Direct build 单独展示授权码激活与官网订阅管理入口 |

**实现原则**：双渠道、单权益。业务层继续只依赖 `EntitlementGate` / `isPro`，购买与授权 Provider 按构建渠道分叉。

**已更新**：`docs/2-产品/需求讨论/正式方案/Direct分发与LemonSqueezy授权方案.md`、`docs/2-产品/需求讨论/正式方案/StoreKit订阅上架方案.md`、`docs/6-发版与上架/v1-上架检查清单.md`、`docs/功能实现总览.md`

### 4.5 RAG 无证据引导与 GitHub 联网审计

**决定（2026-07-14）**：知识库问答不能把“没有本地分片”直接等同于整轮失败，也不能在没有可信证据时让 Generator 自由回答。

| 场景 | 决议 |
|---|---|
| 明确问候 / 致谢 / 告别 | 本地直接返回知识库使用引导与推荐问题，不调用 Planner、Retriever 或 Generator |
| Planner 判断为非知识库闲聊 | 返回 `guided_discovery`，不执行检索与生成 |
| 本地无命中，但附件或 GitHub 临时上下文有真实内容 | 允许 attachment-only / remote-only 生成 |
| 所有证据都为空 | 明确说明未找到相关内容，展示最多 3 个可点击推荐问题，不调用 Generator |
| 推荐问题 | 持久化到历史消息；点击后恢复原问题的显式 repo 范围，再作为新问题发送 |
| GitHub 联网 | Planner 声明意图，执行层兜底高置信实时资源；默认按 `repo × resource` 逐项确认，Composer 主动联网时视为本轮已授权 |

GitHub Issues / PR 使用 `/search/issues`，执行层固定注入目标 `repo:` 与 `is:issue` / `is:pr`，并过滤 Planner query 中可扩大范围的 qualifier。审计仅保存资源、目标仓库、查询摘要、网络或缓存、HTTP 状态、结果数、耗时、脱敏 URL 与错误，不保存 token 或远程正文。

**已更新**：`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/知识库RAG专项进度.md`、`docs/功能实现总览.md`

### 4.6 RAG 长流式内容的 UI 性能边界

**决定（2026-07-14）**：Provider 的原始 token/delta 不得逐条写入 SwiftUI 可观察状态。完整内容必须在独立 buffer 中累积，正文严格不超过 8Hz、Think 严格不超过 5Hz 发布快照，并在完成、失败、取消和流提前结束时 flush，性能保护不得截断回答或推理内容。

| 问题 | 决议 |
|---|---|
| 长 Think 高频刷新 | UI 严格不超过 5Hz；运行中只渲染最近 8,000 字，完整内容仍保留并在结束时收口 |
| 长回答实时尾部 | 继续使用稳定 Markdown 前缀与普通 `Text` 尾部，不对持续变化的尾部启用 `.textSelection(.enabled)` |
| Think 执行步骤 | 始终复用同一详情行身份，不以完整正文作为 `ForEach` identity |
| 滚动跟随 | 只有 `isFollowing` 作为外部可观察状态；phase、底部可见性与手势标志使用 `@ObservationIgnored` |
| 复制能力 | 流式中不创建 AppKit `SelectionOverlay`；完成态保留整条回答复制/导出入口 |

运行态采样已确认原故障为 SwiftUI/AttributeGraph 持续布局：主线程热点集中在 `SelectionOverlay`，内存持续增长并伴随 “Modifying state during view update”。因此该约束属于正确性边界，不只是体验优化。

**已更新**：`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/知识库RAG专项进度.md`、`docs/功能实现总览.md`

### 4.7 主窗口 AI 对话的 Think 展示边界

**决定（2026-07-14）**：主窗口 AI 对话只展示 Provider 明确返回的 reasoning，不把等待提示伪装成思考内容。Reasoning 首个 delta 到达后展开；收到 `reasoningCompleted` 时自动折叠，未发送该事件的兼容服务以首个正文 delta 兜底。

| 问题 | 决议 |
|---|---|
| 长 Think 性能 | 完整内容在独立 buffer 累积，UI 最多约每 150ms 或累计 256 字发布一次快照 |
| 文本选择 | 流式 Think 不创建 `SelectionOverlay`；完成态与历史回看恢复文本选择 |
| 终态收口 | 成功、取消、失败都 flush；只有 reasoning、尚无正文时取消也保留 assistant 消息 |
| 模型兼容 | Provider 未返回 reasoning 时只显示通用等待态，不生成虚假 Think |
| 历史持久化 | JSON 继续使用 `ChatMessage.reasoning`；独立 SQLite 追加 `v2-chat-message-reasoning`，保留既有数据 |

**已更新**：`Starcat/Features/AI`、`Starcat/Shared/Services/DiskChatHistoryStore.swift`、`StarcatTests/StreamingMarkdownSnapshotTests.swift`、`StarcatTests/DiskChatHistoryStoreTests.swift`、`docs/功能实现总览.md`

### 4.8 RAG 主动联网与统一审计

**决定（2026-07-14）**：RAG 联网分为 GitHub 结构化实时查询与用户显式开启的 External Search，两者都必须在请求发出前创建可展开的“联网搜索”执行步骤，完成后自动折叠。

| 场景 | 决议 |
|---|---|
| 最新 Issues / PR / Release 等高置信 GitHub 实时问题 | 执行层在 Planner 漏报时补齐结构化请求，不能用 README 中的旧链接代替现场数据 |
| Composer 未开启联网 | 不执行普通 Web Search；GitHub 结构化请求继续沿用 `repo × resource` 确认 |
| Composer 开启联网 | 视为本轮明确联网授权；允许 External Search，并自动批准本轮 GitHub 结构化请求 |
| 实时问题未取得成功非空的网络证据 | 拒绝用本地旧内容生成确定性答案，明确反馈联网失败或无结果 |
| 私有仓库 | 只有开启“允许私有仓库参与外部搜索”后，仓库身份才可进入 External Search query |

External Search 复用 Settings 已配置的 AnySearch、Tavily、Exa 或 Brave Search；审计保存 Provider、查询摘要、结果数、耗时与最多 5 条标题/URL，不保存网页正文。GitHub 临时正文仍只在本轮 Prompt 中使用；普通 Web 正文不写 `rag_message_remote_contexts`，因此不新增数据库迁移。

**已更新**：`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/知识库RAG专项进度.md`、`docs/功能实现总览.md`

### 4.9 RAG Metadata 动态事实索引

**决定（2026-07-14）**：项目 Metadata 与笔记分片保持独立。Metadata 保留本地可得的易变原始事实（计数、时间、Release、Health、OpenSSF 等），仅进入 FTS5 关键词检索，不生成 embedding。

| 项 | 决议 |
|---|---|
| 数据来源 | 只聚合本地缓存表，不为 Metadata 重建发网络请求 |
| 变更处理 | GitHub 同步、标签/状态/入库、Release/Health/OpenSSF 缓存写入按 repo 合并后刷新 |
| 检索 | FTS 可召回 `keyword_only` Metadata；向量检索只读 `ready` 的 README / 笔记 / 摘要等语义分片 |
| 数据格式 | 保留精确原值；缺失字段省略，不写 `Unknown` 或 bucket |

**已更新**：`Starcat/Core/RAG`、`Starcat/Core/Sync/ReleaseRepository.swift`、`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/功能实现总览.md`

### 4.10 RAG 后台生成期间的会话切换性能边界

**决定（2026-07-15）**：会话选择属于高优先级交互，必须在第一次异步读取前提交选择意图；后台回答继续运行，但不得继续把流式状态投影到已离开的中栏。SQLite 是唯一真源，窗口内允许使用有界 LRU 展示缓存和低优先级预热，以空间换取最近会话同帧切换。

| 问题 | 决议 |
|---|---|
| 会话选择 | `selectedConversationID` 在首次 `await` 前更新；缓存未命中立即显示轻量加载态，不保留上一会话正文 |
| 缓存一致性 | 最近 24 个持久化会话快照使用 LRU；消息、摘要、标题、分组或置顶写入后失效对应项 |
| 流式正文 | 高频 UI 只发布 `StreamingMarkdownSnapshot`；完整字符串只在完成、失败或取消收口时物化 |
| 自动滚动 | 中栏使用非惰性 `VStack` + 永久 bottom sentinel；跟随时由 `.defaultScrollAnchor(.bottom, for: .sizeChanges)` 校正流式增长与折叠，用户上滚后关闭尺寸锚定；当前 `ScrollViewReader` 只处理历史恢复、滚底按钮与大纲，proxy 只允许被单次 MainActor 调度短暂捕获，禁止持久保存或直接修改 `NSClipView` offset |
| 历史布局 | 打开历史会话只渲染最新 2 轮，每次手动加载更早 10 轮并恢复原视口；窗口不改变 Prompt、引用、复制、导出或持久化使用的完整消息数组 |
| 布局 | 流式滚动禁止调用 `layoutSubtreeIfNeeded()` 强制整棵消息树同步布局 |
| 派生展示 | 大纲、引用和 Markdown 固定正则不得随每个流式 revision 重算或重编译 |
| Debug | 关闭时不读文件；开启时异步加载且不阻塞正文，解码结果使用有界会话缓存，历史文件不删除 |
| 标题 | 保留首轮并行生成与最终持久化，禁止逐字符修改全局会话数组 |

**已更新**：`Starcat/Features/RAG`、`Starcat/Shared/Components/ScrollFollowTail.swift`、`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/审查报告-会话切换性能.md`、`docs/功能实现总览.md`

### 4.11 Weekly 多来源、AI 情报与置顶服务边界

**决定（2026-07-15）**：取消独立 `starcat-intelligence-api`。HelloGitHub、AI 情报、阮一峰周刊、ZRead、Show HN 统一归 `starcat-weekly-api`，在 Starcat「探索 → 周刊」按来源筛选。weekly-api 从三源专属表重构为固定来源目录 + 通用来源事件 + 持久化 enrich 队列；`supports/starcat-site/_local-admin` 负责来源同步和多项目置顶运营。

| 项 | 决议 |
|---|---|
| 来源边界 | `weekly / zread / discovery / hellogithub / ai_intelligence` 为首期固定来源；禁止 skill 自由创建分类 |
| HelloGitHub | 由 weekly-api crawler 抓取；官方 periodical pages 回填历史，featured API 做增量 |
| AI 情报 | repo-local skill 解析、搜索、核验整批文本后，以 `ai_intelligence` 一次提交仓库列表 |
| 写入时机 | POST/Collector transaction 只入队；commit 后主动唤醒 Worker，不同步调用 GitHub |
| 后台处理 | Worker 启动扫描一次、主动唤醒优先、每 15 分钟兜底；负责 enrich、租约恢复、最多 3 次重试与剔除 |
| Starcat | 继续复用 Weekly API 与本地 bulk 缓存；来源筛选、图标、详情和置顶排序改为通用扩展模型 |
| 置顶 | weekly-api 保存多个全局有序置顶；只在符合当前来源/语言/状态筛选的结果内优先排序 |
| 控制台 | `_local-admin` 增加来源状态、HelloGitHub 回填和置顶管理，不建设公网后台 |

**方案**：`docs/2-产品/需求讨论/正式方案/Weekly多来源扩展与AI情报采集正式方案.md`

### 4.12 RAG 附件支持边界

**决定（2026-07-16）**：当前 RAG 工作台正式支持文本、Markdown、JSON 与源码附件。PDF 与图片虽在底层处理器保留未接入分支，但真实文件选择器、发送链路和当前 DoD 均不包含这两类格式；不得把底层可达或历史测试误写为已交付产品能力。

| 项 | 决议 |
|---|---|
| 当前支持 | 文本、Markdown、JSON、源码；作为本轮临时上下文，不写入索引、notes 或 CloudKit |
| 当前不支持 | PDF、图片；文件选择器不暴露，发送前按不支持格式阻断 |
| 底层分支 | `RAGAttachmentProcessor` 的 PDF/vision 路径仅视为未来能力，不进入当前验收与发布承诺 |
| 后续启用 | 必须重新完成产品方案、UTType 入口、Provider 能力边界、隐私提示、i18n、自动化与真实 UI 验收 |

**已更新**：`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/知识库RAG专项进度.md`、`docs/4-工程进度/知识库RAG专项/RAG收尾与稳定性-checklist.md`、`docs/功能实现总览.md`

### 4.13 RAG Embedding 异步写回一致性

**决定（2026-07-16）**：Embedding 请求跨越网络 `await`，chunk id 可能在 source diff 或人工覆盖后继续复用，因此不得仅凭 id 写回。`rag_chunks` 通过追加 `v11-rag-embedding-claim` 获得 `embedding_claim_id`；请求前在 SQLite writer 事务内 claim，返回后仅在 id、`content_hash`、pending 状态和 claim id 全部匹配时写 ready/failed。

| 项 | 决议 |
|---|---|
| Schema | 只追加 v11 migration，不修改已发布的 `v7-knowledge-rag` 建表 SQL |
| 正文变化 | source 重建、人工覆盖和恢复原文都必须清空旧 claim 与旧向量 |
| Provider 异常 | 返回数量不一致或含空向量时整批失败并释放 claim，禁止错位或部分写回 |
| 并发语义 | 后领取者可接管同一 pending chunk；旧请求因 claim 不匹配静默丢弃结果 |

**已更新**：`Starcat/Core/RAG/RAGChunkRepository.swift`、`Starcat/Core/RAG/KnowledgeRAGIndexBuilder.swift`、`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/RAG收尾与稳定性-checklist.md`、`docs/功能实现总览.md`

### 4.14 RAG 自托管检索后端回退语义

**决定（2026-07-16）**：`fallbackToSQLite` 同时约束 Meilisearch/Qdrant 的查询与索引同步。开启时，普通外部错误或查询空命中允许回退本地；关闭时，配置、查询与同步错误必须原样向上传播。`CancellationError` 表示用户或上层任务已取消，无论开关值都禁止触发本地回退。

| 项 | 决议 |
|---|---|
| 查询 | 两种外部 Provider 通过同一显式开关决定是否包装 SQLite fallback |
| 同步 | 索引同步复用统一错误策略；允许回退时记录错误但不阻断本地索引完成 |
| 无效配置 | 回退关闭时在 Service 装配边界抛错，禁止静默改成本地 Provider |
| 取消 | Meilisearch、Qdrant 查询与同步都必须继续抛出 `CancellationError` |

**已更新**：`Starcat/Features/RAG/Core/RAGBackendConfiguration.swift`、`Starcat/Features/RAG/Core/RAGExternalSearchProviders.swift`、`Starcat/Core/RAG/KnowledgeRAGIndexBuilder.swift`、`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/4-工程进度/知识库RAG专项/RAG收尾与稳定性-checklist.md`

### 4.15 RAG 聚合元数据快照缓存一致性

**决定（2026-07-16）**：Planner、Generator 与 Inspector 共用数据库修订版本缓存，不直接复用 UI 当前值。`v12-rag-metadata-revision` 通过相关表触发器在业务写事务内推进修订号；相同版本与 embedding model 的并发聚合只执行一次。

| 项 | 决议 |
|---|---|
| Schema | 只追加 v12 migration，不回写已发布的 `v7-knowledge-rag` 或 v1 |
| 一致性 | Repo、知识库边界、标签、摘要、RAG chunk/override 写入与修订号同事务提交或回滚 |
| 时间口径 | 快照包含滚动 30 天统计，即使修订号不变也最多复用 60 秒 |
| 账号隔离 | 用户数据库切换前关闭工作台并清空 cache 与 in-flight load，禁止相同 revision 跨库命中 |

**已更新**：`Starcat/Features/RAG/Core/KnowledgeBaseMetadataSnapshot.swift`、`Starcat/Core/Database/Migrations/DatabaseMigrationsV1.swift`、`docs/3-设计/详细设计/01-数据库设计.md`、`docs/3-设计/详细设计/30-本地RAG设计.md`

### 4.16 RAG Wiki 链接与仓库 Metadata

**决定（2026-07-17）**：不抓取 Wiki 正文。DeepWiki、ZRead、CodeWiki 的公开收录链接通过统一 cache-first 后台任务补齐，并写入仓库现有唯一 `metadata:0` 分片。

| 项 | 决议 |
|---|---|
| 网络边界 | 私有仓库不探测；详情、搜索、Repo AI、Companion 与后台扫描统一使用 `WikiContextService` |
| 缓存策略 | fresh 只读，stale 先用旧值后刷新，miss 先返回空后排队；小并发、repo 去重、切库取消 |
| 索引 | `KnowledgeRAGIndexBuilder` 只读磁盘缓存；cache 变化按 repo 刷新 Metadata，不新增 schema 或 Wiki source |
| Prompt | 没有独立 `{metadataSection}`；最终命中仓库必带完整 Metadata，缺失才精简兜底，不新增 hit/citation |
| 预算 | 先保留最终仓库完整 Metadata，再裁剪普通分片；Metadata 无法完整容纳时跳过整个仓库 |
| 管理 | Metadata 可查看和编辑，但 UI、ViewModel/domain、Repository 均禁止下架与永久删除 |
| 链接 | 知识库 Metadata 行和 LLM Markdown 回答均可点击，且只打开校验后的 `http` / `https` URL |

**已更新**：`Starcat/Features/AI/WikiContextService.swift`、`Starcat/Features/AI/WikiKnowledgeBackfillCoordinator.swift`、`Starcat/Core/RAG`、`Starcat/Features/RAG`、`docs/3-设计/详细设计/30-本地RAG设计.md`

### 4.17 阅读状态与知识库归属的单向联动

**决定（2026-07-21）**：阅读状态与知识库归属保持独立，只允许“真正进入使用中”单向触发自动入库，禁止移出知识库或结束使用状态时反向联动另一状态。

| 用户操作 | 决议 |
|---|---|
| 非 `using` → `using` | 如果尚未入库，自动设为 `.inLibrary` |
| 重复保存 `using` | 不覆盖用户明确设置的 `.outsideLibrary` |
| 从知识库移出 | 只更新 `libraryState`，即使仍为 `using` 也不降级为 `read` |
| `using` → `read` / `unread` | 只更新阅读状态，不自动移出知识库 |

**已更新**：`docs/2-产品/需求讨论/正式方案/Starred与知识库正式方案.md`、`docs/3-设计/详细设计/38-Starred与知识库改造详细设计.md`

### 4.18 精选发布台的权限与发布边界

**决定（2026-08-13，2026-08-14 补充）**：Starcat 新增仅限维护者使用的「精选发布台」。入口与执行层都按 GitHub 数字用户 ID 校验，管理员密钥只保存在本机安全凭据存储中；项目识别按 `starcat-weekly-import` 的 AI 工作流执行，并与 weekly-api 发布状态机严格分离。

| 项 | 决议 |
|---|---|
| 当前权限 | 仅 GitHub 用户 ID `20341123` 可见、可执行；不能只依赖隐藏入口 |
| 后续开放 | 权限策略集中在单一 policy，未来可替换为服务端角色，不把身份判断散落到 View |
| 输入边界 | 允许自然语言、网页、GitHub 地址和多项目混合清单；AI 先拆分实体和检索式，再做外部证据与 GitHub 核验 |
| 仓库核验 | 只允许本轮 GitHub 已核验候选或手工 canonical 核验结果；`needs_review` 必须人工确认，`not_found` 不凑替代项目 |
| Weekly 隔离 | 打开窗口和 AI 识别不调用 Weekly；甄别成功后才激活管理员连接、分类和发布状态恢复 |
| 来源分类 | 动态读取人工分类，并允许管理员通过 `POST /internal/sources` 创建新分类 |
| 发布协议 | `POST /internal/imports` 一次提交全部已确认仓库，随后轮询 `GET /internal/imports/{batch_id}` 到终态 |
| 服务端去重 | 人工事件键按 `source_code + owner/repo` 稳定生成；同来源同仓库由 weekly-api 更新/过滤，识别阶段不查询 Weekly |
| 密钥边界 | 管理员密钥不写源码、日志、UserDefaults 或文档，只通过安全凭据存储读写 |
| 窗口 | 使用单实例独立 macOS Window；关闭窗口不取消已提交批次的状态追踪 |

**专项文档**：`docs/4-工程进度/精选发布台专项/需求说明.md`、`docs/4-工程进度/精选发布台专项/技术设计.md`

### 4.19 External Agent Runtime 多后端边界

**决定（2026-08-16，2026-08-21 扩展为多后端 POC，2026-08-22 补齐 Codex 工具桥）**：不推翻 DeepSeek Harness，也不让任一外部框架替换全部 Agent。Starcat 保留 `LoopAgentRuntime`，新增统一 `ExternalAgentRuntimeHost + Provider Adapter`；Codex App Server 与 DeepSeek Harness 都是可切换后端。Weekly、Repo Insight、Alternatives 可在 Loop 与 Codex 间切换；Untagged 等带审批写入的 Agent 继续锁定 Loop；General / Research POC 可在 Codex 与 DeepSeek 间切换。

| 项 | 决议 |
|---|---|
| UI | 继续使用 Starcat 原生 Workspace、Composer、Run Surface 与 Inspector |
| 路由 | `AgentDefinition.runtimePolicy` 声明允许后端；`AgentRuntimeRouter` 解析用户偏好，禁止 View 按 Agent ID 分支；用户显式选择外部后端后禁止静默回退 Loop |
| 模型路由 | 工作台统一按 `Runtime → Provider → Model → Reasoning` 选择；Codex Provider 来自本机 `config.toml`（依赖 `env_key` 的路由因凭据隔离不可选），DeepSeek Harness Provider/Model 来自 Starcat「设置 → AI」中已验证的 OpenAI-compatible 配置 |
| 运行形态 | Codex 使用用户已安装并登录的 `codex app-server`；DeepSeek 使用外部安装的固定 `0.1.1rc1` wheel，macOS arm64 需要主文件、`-rg` 与 `-spawn-helper`，不进入 App / DMG |
| 控制协议 | 共用 newline-delimited JSON-RPC Host；Codex 与 DeepSeek 各自维护握手、事件、停止和 capability adapter |
| 领域数据 | 外部 Runtime 禁止直接访问 SQLite；Codex 通过 App Server `dynamicTools` 仅调用 definition allowlist 内的 Starcat 自动只读工具，schema 与权限由宿主二次校验；DeepSeek 的临时 Loopback MCP Bridge 仍需单独完成 token / repo scope 验证 |
| 默认工具 | Codex 首批开放 Weekly、Repo Insight、Alternatives 已有只读工具；写入、审批、高成本工具不进入外部 Runtime。产品化时仍须硬禁用 Shell、FS、Terminal、Job、Browser 与 Subagent；prompt 约束不视为安全隔离 |
| Loop 预算 | 通用默认仍为 32 次；Weekly、Repo Insight、Alternatives 按 definition 提升到 96 次。外部 Runtime 不复用本地 Loop 的 32 次工具预算 |
| 插件 | 禁止运行时安装任意 npm / pnpm 或社区插件；只允许编译期锁定、审计并登记 manifest / License 的白名单依赖 |
| 渠道 | 只允许 Direct arm64 实验构建；签名、Notarization、进程组清理与凭据隔离通过前不得发布；Intel 未有验证 carrier 前隐藏入口 |
| 数据库 | 不持久化外部 Session、鉴权或原始协议 payload；产品化 Trace 只保存脱敏后的 Starcat 运行投影，并通过新的 `registerVN` 追加表结构 |
| 退出门禁 | 无法可靠 cancel / approval、必须嵌入 Web UI、必须直读 SQLite 或无法清理 Sidecar 时判定 NO-GO |
| 更新 | 安装脚本固定 wheel 版本与 Starcat Cordis Profile；禁止运行时 `latest`、`npx`，Runtime 与 Starcat Direct 独立升级 |

**方案**：`docs/3-设计/详细设计/59-ExternalAgentRuntime多后端POC技术方案.md`；DeepSeek adapter 的上游评估继续保留在 `58-DeepSeekHarness集成评估与POC技术方案.md`

---

## 五、🟢 启动期决议（已确认）

以下问题在 MVP 启动会议（2026-05-30）确认，写入此处作为基线，开发过程中如需调整需要更新本节。

### 5.1 测试策略

| 项 | 决议 |
|---|---|
| 测试框架 | **Swift Testing**（`import Testing` / `@Test` / `#expect`） |
| MVP 覆盖率目标 | 不强制百分比，但 Core 层（Database/Network/Sync/Keychain）必须有单测 |
| 数据库集成测试 | 用内存数据库（`DatabaseQueue(path: ":memory:")`），跑 Migration + CRUD + FTS5 |
| UI 测试 | MVP 阶段不写 |

### 5.2 数据 Migration 方案

| 项 | 决议 |
|---|---|
| 版本号策略 | GRDB `DatabaseMigrator`，使用语义化字符串标识（`"v1-initial"`, `"v2-undo-star"`, ...） |
| 降级支持 | **不支持降级**，新版本只允许向上迁移 |
| 失败提示 | 迁移失败时弹窗提示用户，提供"导出当前数据库 + 联系支持"选项；不允许静默重置 |
| **正式版后（2026-07-12 起）** | **禁止直接改 `v1-initial` 建表 SQL**；加字段 / 建表一律追加 `registerVN`（`ALTER TABLE` / `CREATE`），保护已发布用户本地数据 |
| MVP v1 范围 | repos / starred_repos / tags / repo_tags / repo_notes / readmes / saved_searches / sync_state / tag_stats_cache + repos_fts |
| 后续版本 | 以 `DatabaseMigrations.registerAll` 中已注册的 v2+ 为准；详见 `docs/3-设计/详细设计/01-数据库设计.md` §1.3 |

### 5.3 依赖注入架构

| 项 | 决议 |
|---|---|
| DI 方案 | **SwiftUI Environment + Protocol**，不引第三方 DI 库 |
| 实现模式 | 每个 Service 定义 `protocol`（如 `GitHubAPIServiceProtocol`），通过 `EnvironmentKey` 注入；Mock 实现用于单测和预览 |
| 单例 | 仅 `DatabaseManager` / `KeychainManager` / `Logger` 这类无状态基础设施允许单例 |

### 5.4 CloudKit 开发环境

| 项 | 决议 |
|---|---|
| MVP 阶段 | **不接入 CloudKit**，所有数据本地 SQLite |
| 数据层抽象 | UserData repository protocol（`TagRepository` / `NoteRepository` 等），CloudKit 实现在 v1.1+ 落地 |
| 同步开关 | 设置页"iCloud 同步"开关默认关闭，未实现前显示"即将上线" |

### 5.5 AI 成本设计（P1 才生效）

| 项 | 决议 |
|---|---|
| 缓存过期 | AI 摘要默认 **30 天不过期**，用户可手动重新生成 |
| 配额显示 | 设置页显示本月 AI 调用次数 + 剩余配额（自建服务模式），BYOK 模式不显示 |
| 批量预估 | 批量操作前预估"将调用 X 次 AI"，用户确认后才执行 |

### 5.6 导入导出兼容性

| 项 | 决议 |
|---|---|
| MVP 导出格式 | Starcat 自有 JSON（含 repo metadata + tags + notes + status）|
| 第三方导入 | MVP 仅支持 Starcat JSON 导入；OhMyStar/Astral 兼容延后到 v1.1 |
| 冲突策略 | 导入时检测重复 repo_id，按"保留本地 / 用导入文件覆盖 / 合并字段"三选项让用户决定 |

### 5.7 国际化策略

| 项 | 决议 |
|---|---|
| UI 语言 | 跟随系统语言；MVP 首发支持**简体中文 + 英文** |
| 18 语言扩展 | 后续多语言版本扩展为 `en`、`zh-Hans`、`zh-Hant`、`ja`、`ko`、`de`、`fr`、`es`、`pt-BR`、`it`、`ru`、`nl`、`pl`、`uk`、`tr`、`vi`、`id`、`ar`；未通过翻译、CI 与 UI/RTL 验收的语言不得进入正式选择器，详见 `docs/3-设计/详细设计/48-18种语言本地化扩展详细设计.md` |
| 字符串管理 | 使用 String Catalog (`.xcstrings`)，Xcode 15+ 原生支持 |
| AI 摘要语言 | 设置页提供"AI 输出语言"选项，默认跟随 UI 语言 |
| README 渲染 | 不翻译原文（除非用户启用 README 翻译功能，见功能清单 2.x） |

### 5.8 工程基础设施

| 项 | 决议 |
|---|---|
| 包管理 | Swift Package Manager（SPM） |
| 图片库 | **Kingfisher 7.0+**（纯 Swift、API 简、避免 OC 依赖） |
| Keychain 封装 | **KeychainAccess 4.2+**（避免裸写 Security framework） |
| 日志 | **OSLog 原生**，不引 swift-log；按 category 拆分（network/database/sync/auth/ai/ui） |
| Lint | **SwiftLint** 作为可选 Build Phase，违规不阻断编译，先用 warning |
| Git Hooks | 暂不强制 pre-commit hook |

### 5.9 品牌/标识

| 项 | 决议 | 备注 |
|---|---|---|
| 应用名 | Starcat | |
| Bundle ID | `com.starcat.app.store` | App Store target；Direct target 使用 `com.starcat.app.direct` |
| URL Scheme | `starcat://callback` | OAuth 回调 |
| OSLog subsystem | `com.starcat.app` | |
| 一级域名 | `starcat.ink` | Direct 官网、下载、Sparkle appcast 与相关公开入口使用该域名；App Store 合规页面继续使用 `dong4j.app/starcat/*` |
| OAuth App | 已注册并转移至 `starcat-app`，Client ID 为 `Ov23li4suXj1nNsWtHHG` | 保持 `starcat://callback` 与 Device Flow；迁移不轮换 Client Secret、不撤销现有用户令牌 |

### 5.10 知识库 RAG 的 RepoContext 深度思考

| 项 | 决议 |
|---|---|
| 开启条件 | 只有恰好一个显式知识库项目时可开启；附件数量不限，项目范围失效时自动关闭 |
| Composer 状态 | 与联网开关相同，按会话保存在进程内 Composer 草稿；发送前冻结请求快照 |
| 目标选择 | 目标只能来自用户唯一显式项目，Planner 不得选择或扩大代码读取范围 |
| Prompt 协议 | 使用独立 `{repoContextSection}`，不合并 `{evidenceSection}`；不保留旧模板兼容双轨 |
| Token 边界 | 不使用 chunk evidence budget/topK/cap；仍受 `aiRepoContextTokenBudget` 与模型总窗口约束 |
| 历史与索引 | 只保存 commit/hash/token 等审计快照，不保存 XML 正文，不写 `rag_chunks` 或 CloudKit |
| 对外边界 | XML 只进入用户选定的 chat Provider；不进入 External Search query，私有仓库授权规则不变 |
| 可解释性 | 时间线、Plan、Debug、引用与 Evidence 均使用独立 RepoContext 类型和展示 |

### 5.11 知识库 RAG 的双语关键词 OR 召回

**决定（2026-07-17）**：普通搜索继续使用 `FTSQuery.sanitize` 的多词 AND；知识库 RAG
拆分 `semanticQuery` 与 `keywordQueries`，中文问题默认生成中英文核心词，关键词分支使用
安全 FTS5 OR。repo 范围只由候选 id 和显式上下文约束，不把项目名加入分词。

| 项 | 决议 |
|---|---|
| 查询职责 | `semanticQuery` 用于 embedding/vector/Rerank；`keywordQueries` 用于 FTS5/Meilisearch |
| 双语边界 | Planner 同时生成中文概念词与英文技术表达；FTS5 不翻译，跨语言语义由 embedding 负责 |
| FTS5 语义 | 每项按不可信字面文本转义并以 OR 连接；普通搜索 AND 不变 |
| 仓库范围 | `.only/.prefer/.exclude` 继续由 repo id 强制执行；repo 名不加入关键词 |
| 兼容 | 旧 JSON 缺省为 `[]`；旧自定义 Prompt 使用 `semanticQuery` 的有界 OR token 降级 |
| 可解释性 | Plan 展示实际关键词；Trace 保存安全 FTS5 表达式；漏斗区分 0 命中、失败和跳过 |

**已更新**：`docs/3-设计/详细设计/29-关键词与全文检索设计.md`、
`docs/3-设计/详细设计/30-本地RAG设计.md`、
`docs/4-工程进度/知识库RAG专项/知识库RAG专项进度.md`

### 5.12 快捷键与应用命令边界

**决定（2026-07-21，2026-07-22 三次修订）**：新增 `⌘R` 刷新当前仓库列表/详情、`⇧⌘K` 打开知识库 RAG 工作台、`⇧⌘A` 展开当前仓库详情页底部 AI 面板，并移除“关于 Starcat”的 `⌘I`；五个应用命令均允许改键、逐项启停和恢复默认。快捷键只负责路由，必须复用现有执行链，不能绕过登录、Pro、分发渠道、当前选择或 Debug 门禁。

| 项 | 决议 |
|---|---|
| 设置归属 | 快捷键 Section 继续留在“通用”；提供五项录制框、总开关、逐项开关和恢复默认；AI 发送方式不受应用快捷键开关控制 |
| 上下文 | AI 使用右栏实际渲染且符合现有门禁的仓库；Settings 前台时沿用主窗口最后有效详情；不自动选择第一条或使用 hover 对象 |
| 刷新 | 主窗口优先 first responder 所在列，否则使用最后操作列；详情不可刷新时回退当前列表；一次只执行一个现有刷新动作 |
| RAG | 只打开/前置工作台，不自动创建会话或发送问题 |
| AI | 只展开当前仓库详情页底部 AI 面板，不自动生成摘要；`RepoAIWindowController` 是该面板内部“在独立窗口中打开”的附属展示形态，不是快捷键或搜索等外部入口 |
| 冲突 | 五项保存键位始终唯一，关闭状态仍参与校验；`⌘A` / `⇧⌘M` 等真正固定键位继续保留 |
| 暂缓 | Agent 工作台仍受 Debug flag 控制；RAG `⌘N` 等空会话复用规则后再评估 |

**已更新**：`docs/3-设计/详细设计/46-快捷键与应用命令设计.md`、`docs/3-设计/详细设计/08-设置页面设计.md`

### 5.13 公开仓库分享链接与 Universal Links

**决定（2026-07-21）**：公开仓库统一分享 `https://starcat.ink/r/{owner}/{repo}`。
网页首屏由现有 `starcat-sharing-api` 服务端渲染 OG metadata 与动态 PNG；阿里云
Nginx 承接域名、缓存和反向代理。已安装 Starcat 时由 Universal Links 定位仓库，
未 Star 的仓库只做会话内预览，不自动 Star、不写标签/笔记或本地仓库表。

| 项 | 决议 |
|---|---|
| 链接身份 | 只使用公开 `owner/repo`，不暴露发送者账号或本地数据 |
| 服务边界 | 扩展现有 `starcat-sharing-api`，不新增 Cloudflare Worker 或 Fly App |
| OG | HTML 首包包含 OG/Twitter metadata；PNG 固定 `1280×640`，异常返回非泄漏 fallback |
| 客户端 | App Store / Direct 共用 AASA；本地命中直接定位，未命中拉公开 metadata 形成瞬时 Repo |
| 权限 | 基础链接免费；私有仓库不分享；AI 分享页继续按既有 Pro 规则 |
| 生产配置 | sharing-api 复用 `GITHUB_TOKENS` 池并保持 always-on；Associated Domains 上线前更新 provisioning profile |

**已更新**：`docs/2-产品/需求讨论/正式方案/仓库分享链接与OG预览正式方案.md`、
`supports/docs/R-01-sharing-api-改造方案.md`、`supports/docs/R-01-总体设计.md`

### 5.14 “我的项目”与 Private / 组织仓库授权边界

**决定（2026-07-29）**：星标模块新增固定一等分类“我的项目”，范围为当前用户个人拥有仓库和通过组织成员身份可访问的组织仓库。项目归属与 Star、知识库关系正交，使用独立 `user_projects` 关系表和追加迁移 `v17-my-projects`，复用现有 Repo 列表、详情、洞察与 Star History。

**修订（2026-07-29）**：GitHub 收紧普通第三方 Stargazers 列表后，仓库管理员和协作者仍可使用授权凭据调用 `/repos/{owner}/{repo}/stargazers`，并通过 `application/vnd.github.star+json` 读取当前 Stargazers 的 `starred_at`。因此“我的项目”必须优先直连 GitHub 重建历史；Public 非项目仓库才走 Discovery，Private / Internal 历史只在本机聚合和保存，禁止发送到 Starcat 公共服务。

**授权流程修订（2026-07-29）**：GitHub App 已启用安装期间 OAuth 用户授权，回调固定为 `starcat://github-app/callback`。用户点击“连接 GitHub App”后，在同一次 GitHub 浏览器流程中完成安装范围选择和只读授权；主 OAuth 登录的 Device Flow 保持原样。GitHub App Web Flow 的 code 交换与 refresh token 轮换需要 Client Secret，macOS 客户端无法将其视为真正机密，因此只用于用户令牌流程，并继续严禁在客户端保存权限更大的 GitHub App private key。

| 项 | 决议 |
|---|---|
| Public 项目 | owner / collaborator 使用项目关系对应的 OAuth 或 GitHub App 凭据直连 Stargazers；无项目权限的普通 Public 仓库继续走 Discovery |
| Private / Internal | 使用可选 GitHub App，申请 Repository Metadata read + Contents read；Stargazers 结果仅按天聚合后保存本机，不保存用户列表 |
| 授权流程 | 安装 + GitHub App Web Flow 合并为一次浏览器操作；主 OAuth 登录 Device Flow 不变 |
| 禁止方案 | 不把现有 OAuth 扩为 `repo` scope，不把 GitHub App private key 放入客户端；Client Secret 不视为安全凭据 |
| 组织范围 | 只展示用户通过 `organization_member` 可访问且 GitHub App 已获准的仓库；待审批和部分授权必须显式展示 |
| 数据模型 | 新增用户与 Repo 的项目关系，不新增 `repos.is_owned`，Project upsert 必须保留既有 Star / Tag / Note / LibraryState |
| 隐私 | Private 项目不进入 Discovery、External Search、公开分享、CloudKit 或默认 JSON 导出；GitHub Stargazers 响应只在内存聚合，数据库不保存用户身份 |
| UI | Sidebar 只增加单一“我的项目”入口；个人 / 组织 / 可见性 / 权限放入中栏筛选，避免组织树撑大 Sidebar |

**已记录**：`docs/3-设计/详细设计/51-我的项目整体落地方案.md`

### 5.15 仓库洞察 XML 作为 AI / RAG 共用上下文

**决定（2026-07-30）**：仓库洞察页面、仓库 AI 摘要 / 对话与知识库 RAG 共用同一份结构化洞察快照和 `insights.xml` Artifact。洞察真实数据仍由既有 Provider、SQLite 缓存与 Star History Repository 生成，不允许各消费端重复抓取或各自拼接一份统计文本。

| 项 | 决议 |
|---|---|
| 数据真源 | `RepositoryInsightsDocument` 同时保存结构化快照、稳定 `sourceHash`、合法 XML 与 `xmlHash` |
| 生成触发 | 洞察页和仓库 AI 可正常预热缺失数据；RAG 只允许 cache-only 准备，禁止为一次问答额外联网 |
| RAG 形态 | 作为非向量化仓库级特殊上下文，不新增 `RAGChunkSource`，不写 `rag_chunks`，不参与普通分片统计 |
| Prompt | 使用独立 `{repositoryInsightsSection}` 和 token 预算；合法 XML 按完整节点投影，不做字符级截断 |
| 管理 | 知识库详情可查看、复制、下载、删除和重新生成；洞察 XML 只读，删除只移除 Artifact，不删除洞察缓存或 Star History |
| 历史 | 会话只保存 repo、`sourceHash`、`xmlHash`、生成时间和 token 审计；三者匹配当前 Artifact 时才允许回放正文 |
| 隐私 | XML 不含 Issue / PR 标题、安全公告正文或 Stargazer 身份；不进入 embedding、CloudKit 或 External Search query |

**已记录**：`docs/3-设计/详细设计/30-本地RAG设计.md`、`docs/3-设计/详细设计/49-洞察中心详细设计.md`

### 5.16 知识库 RAG 无向量配置时的可用边界

**决定（2026-07-30）**：Embedding 是混合召回增强能力，不是知识库 RAG 的启动前提。未配置向量模型、缺少向量 API Key 或候选仓库没有当前模型向量时，Starcat 仍生成并保留本地文本分片，通过 SQLite FTS5 / Meilisearch 使用仓库元数据、README、笔记和 AI 摘要，并继续按既有 cache-only 规则附加 RepoContext XML 与仓库洞察 XML。

| 项 | 决议 |
|---|---|
| 索引构建 | 文本分片写入即视为关键词索引可用；无有效 Embedding 客户端时跳过向量请求并正常完成，不把本轮标记为失败 |
| 关键词范围 | 知识库内全部未排除文本分片均可参与 FTS，不受 `pending / ready / failed / stale / keyword_only` 限制 |
| 向量范围 | 只读取当前模型 `ready` 且有非空向量的分片；候选范围没有可用向量时不生成 query embedding |
| 特殊上下文 | RepoContext XML 与仓库洞察 XML 继续保持非向量化 Artifact，不写 `rag_chunks`；在仓库范围明确时由既有阶段附加 |
| UI | 无有效向量配置时显示非阻塞“仅关键词”提示和配置入口；不得禁用发送或弹出“必须先配置向量模型”阻塞提示 |
| 外部后端 | 无 Embedding 时只允许同步 Meilisearch 文本索引，不得用空模型清空或重建 Qdrant |
| 状态口径 | “可检索覆盖”按文本分片计算；pending / failed / stale 只表达向量增强健康度，可与关键词可用状态重叠 |

**已记录**：`docs/3-设计/详细设计/30-本地RAG设计.md`

### 5.17 Agent Run Surface 的过程与结果呈现边界

**决定（2026-08-14，2026-08-22 扩展为 Runtime 原生 Trace）**：Agent 工作台中栏采用“过程可追踪、结果可阅读”的双层结构。`AgentMessage`、Approval 与 Artifact 继续保存会话和产物事实；新增 `AgentTraceEvent` 保存各 Runtime 实际发出的计划、推理摘要、工具生命周期、错误、重试与终态。展示层只按稳定事件 ID 合并 start/update/completed，不再用固定业务阶段推测 Agent 过程。运行中、失败、重试和审批默认展开，完成和历史完成态默认折叠，最终 Markdown / Artifact 仍在中栏直接阅读。

| 项 | 决议 |
|---|---|
| 事实边界 | 不改 Runtime 执行顺序；`AgentTraceEvent` 与消息、审批、产物分表持久化，历史 Run 必须恢复同一事件顺序 |
| Runtime 边界 | UI 共享时间线组件，但 Codex、DeepSeek Harness 与 Built-in Loop 分别映射自己的原生事件；项目数量、类型和顺序不得固定 |
| 工具展示 | 同一 provider event / `toolCallID` 合并生命周期；相邻同类执行只能在没有推理、错误、重试等边界时聚合，未知事件安全降级 |
| 过程折叠 | 运行中展开；审批、失败定位关键活动；完成和历史完成态折叠；用户手动选择优先 |
| 展开详情 | 可展示脱敏后的用户可读 input、output、耗时、Attempt 与错误恢复信息；原始协议 JSON、凭据、环境变量和未解释日志不进入普通产品 UI |
| 最终结果 | `.markdown` 在中栏富文本渲染；禁止正文三处重复，技术审计与最终内容保持分区 |
| 推理边界 | 普通 UI 不展示或伪造模型隐藏思维链；只展示 Runtime 明确提供的 reasoning summary、plan 与用户可见 commentary |
| Inspector | 当前承载 Knowledge Audit、Artifact metadata 与导出；原始工具事实继续持久化但不在主阅读层展开 |
| 结构化产物 | 图表、对比与 diff 由真实场景分阶段增加版本化 payload，禁止直接执行任意 HTML |

**已记录**：`docs/3-设计/详细设计/57-Agent工作台与统一能力层详细设计.md`

### 5.18 匿名数据贡献、自研推荐与 Star History 服务边界

**决定（2026-08-24 修订）**：Starcat 后续增加默认关闭、用户主动开启的数据贡献能力。客户端当前只贡献匿名公开 Star 全量快照；推荐训练由 `starcat-recsys-trainer` 承担，在线查询长期统一由 `starcat-recommend-api` 承担，`/api/v1` 保持 SimRepo，`/api/v2` 读取自研 ServingBundle，不再创建 `starcat-recsys-api`。Star History 仍独立规划为 `starcat-history-api`。

| 项 | 决议 |
|---|---|
| 同意 | 推荐贡献与 Star History 贡献使用两个独立 Toggle，默认关闭；不并入现有匿名遥测 |
| 1.4.0 提示 | 1.4.0 及以后为每个已登录 GitHub 账户显示一次贡献授权 Sheet；未登录延后、已开启跳过、关闭视为暂不参与，之后仍可在设置页修改 |
| 推荐字段 | 随机账户级 `participant_id`、完整快照 ID、公开 `repo_id`、可空 `starred_at`；不上传真实 GitHub 身份 |
| History 字段 | 公开 `repo_id`、UTC 日期、`stars_count`、固定 `snapshot` 精度；群体聚合后才进入公共查询 |
| 禁止数据 | Private / Internal、Token、login/user ID、标签、笔记、搜索、点击、AI/RAG 内容、README/代码均不上传 |
| 数据来源 | Starcat opt-in 数据优先持续更新；不足时用 GH Archive BigQuery bootstrap；GitHub API 只补公开 repo metadata |
| 推荐算法 | 时间衰减 co-star + Metric Learning + 内容向量冷启动 + 元数据校正 + MMR；SVD 只作 baseline/回退 |
| 服务边界 | Collection 负责公开 Star 快照接收与训练导出；Trainer 负责 ingest/训练/评估/发布；Recommend API 只读 ServingBundle；History 独立负责观测、估算、聚合和查询 |
| 删除 | 当前公开 Star 贡献不实现服务端删除；关闭开关停止未来上传并清理本地待发送任务，不扩大到用户私有数据 |
| 迁移 | Recommend API 新增 v2 自研接口且保持 v1 不变；先独立验证再 shadow/灰度，最终只删除 SimRepo Provider，不删除统一 Recommend 服务 |
| 私有历史 | “我的项目”有权限时仍只在本机直连 GitHub Stargazers 并聚合；公共 History 服务只处理公开 repo |

**已记录**：`docs/3-设计/详细设计/60-Starcat数据贡献与数据平台详细设计.md`、`61-Starcat自研仓库推荐系统详细设计.md`、`62-Starcat自研星标历史服务详细设计.md`

**实施收口（2026-08-24）**：第一阶段只实现推荐训练需要的公开 Star 完整快照。客户端仅提供一个默认关闭的贡献 Toggle，上传状态和失败对用户完全静默；旁路失败不得影响 Stars 同步和其他功能。独立 `starcat-collection-api` 负责接收、最新快照存储与训练导出，不接入 `starcat-api`，不实现 History、状态查询和服务端删除。`starcat-recsys-trainer` 通过内部接口主动 Pull，并把校验后的 Bundle 发布到独立 `starcat-recommend-api`；Starcat Direct 通过新增 v2 接口消费自研推荐。当前实施以 `63-Starcat公开Star数据静默上报与Collection服务详细设计.md` 为准，60/62 中的 History 规划暂不进入代码。

### 5.19 Awesome 发现栏目与来源管理边界

**决定（2026-08-24）**：Awesome 作为“探索”中紧跟周刊的新模式；周刊选中时左栏下半部分显示语言，Awesome 选中时改为用户已启用的 Awesome 来源。Starcat 精选来源由 `_local-admin` 内容管理并通过 `starcat-discovery-api` 发布和集中解析，用户自行勾选；用户自定义来源只在客户端添加、解析和保存，不上传公共服务。

| 项 | 决议 |
|---|---|
| 首次进入 | 第一次点击 Awesome 自动打开卡片式来源选择 Sheet；只有点击“完成”才结束首次配置 |
| 管理入口 | 独立按钮紧跟 Awesome 名称，参考“我的项目”行；子分类底部不再放“管理来源” |
| 精选来源 | App 不硬编码来源业务数据；Discovery API 下发名称、图片、介绍、仓库、项目数和同步状态 |
| 自定义来源 | 仅接受公开 GitHub Repo，在客户端本地核验和解析，不上传 Discovery |
| 解析边界 | 精选来源服务端解析，自定义来源客户端解析；只把已核验 GitHub Repo 放入本期列表，外部链接不伪装 Repo |
| 数据关系 | Repo 按 `gh_repo_id` 去重并保留多来源证据；Awesome 与 Star、知识库、Tags、Notes、“我的项目”关系正交 |
| 服务边界 | Awesome 不并入 Weekly，也不塞进现有 Discovery bulk；使用独立来源目录和单来源 entries 接口 |

**已记录**：`docs/2-产品/需求讨论/正式方案/Awesome发现栏目与来源管理正式方案.md`

---

## 六、启动开发前的解决顺序

```
第一轮（阻断性 ✅ 已解决）：
  ✅ macOS 最低版本 → macOS 15 Sequoia
  ✅ OAuth Scope → ["read:user", "public_repo"]
  ✅ 数据库循环外键 → 已移除

第二轮（架构级 ✅ 已解决）：
  ✅ README 渲染 → WebView
  ✅ Swift 版本 → 5.9 + @Observable
  ✅ 语义搜索 → BYOK Provider 生成 embedding + 本地 SQLite 缓存 + Swift cosine
  ✅ 增量同步 → 定期全量 + 手动刷新
  ✅ AI 功能 → MVP 不含，Release 订阅 P1

第三轮（设计补全 ✅ 已确认）：
  ✅ 测试策略 → Swift Testing，Core 层必须有单测
  ✅ 数据迁移方案 → GRDB DatabaseMigrator，不支持降级
  ✅ 依赖注入架构 → Environment + Protocol，不引第三方
  ✅ CloudKit 开发环境 → MVP 不接入，repository protocol 抽象
  ✅ 工程基础设施 → SPM + Kingfisher + KeychainAccess + OSLog + SwiftLint(可选)
  ✅ 品牌/标识 → starcat（域名 TBD）

第四轮（产品决策 ✅ 已确认）：
  ✅ 目标平台 → macOS 优先，watchOS 待定
  ✅ BYOK vs 自建服务 → BYOK 需要 Pro
  ✅ 定价方案 → 免费/Pro/BYOK 三档
```

---

*审查日期：2026-05-29*
*审查范围：docs/ 目录下全部 15 份文档*
*解决方案整合：2026-05-29*
*启动期决议补充：2026-05-30*
*18 语言扩展决议补充：2026-07-24*
