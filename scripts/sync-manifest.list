# 显式同步清单（每行一个路径，相对仓库根目录）
#
# 核心原则：
#   - 已由主仓库 git 管理的文件 → 不在此列出，靠 git push/pull 同步
#   - git 不管理但两台机器都要用的文件 → 在此列出，靠 sync-untracked.sh 同步
#
# 约束：
#   - 不应包含 .DS_Store / .build/ / .codegraph/ 等（见 sync-exclude.list）
#   - 独立 git 仓库目录不在此列出（见 sync-exclude.list）
#
# 注意：supports/ 下的独立 git 仓库的 .env 是 gitignore 的，需要在此逐条列出。
# 其他 supports/ 运维文件（scripts/、docs/、AGENTS.md 等）已由主仓库 git 管理，
# 不再重复同步。

# === 含明文密钥的关键文件（同步后对端 chmod 600） ===
Configs/Secrets.xcconfig
notes.md
.claude/settings.local.json
sparkle-private-key

# === supports/ 下独立 git 仓库的 .env（gitignore 不管，需跨机器同步） ===
supports/starcat-discovery-api/.env
supports/starcat-license-api/.env
supports/starcat-recommend-api/.env
supports/starcat-sharing-api/.env
supports/starcat-trending-api/.env
supports/starcat-weekly-api/.env
supports/starcat-wiki-api/.env
